import test from 'node:test';import assert from 'node:assert/strict';import * as THREE from 'three';import {Physics} from '../src/core/physics.ts';import {CharacterController} from '../src/character/controller.ts';import {addWellSurfaceColliders} from '../src/world/higasato/blockouts.ts';
for(const fps of [30,60,120])test(`well blocks character at ${fps} render FPS`,async()=>{const p=await Physics.create();p.addStaticBox(new THREE.Vector3(0,-.15,0),new THREE.Vector3(3.2,.15,3.2));addWellSurfaceColliders(p,0,0,0);const c=new CharacterController(p,new THREE.Vector3(0,.1,2.6));p.step(1/60);let acc=0;for(let f=0;f<fps*3;f++){acc+=1/fps;while(acc>=1/60-1e-10){c.update(1/60,{axis:{x:0,y:1},cameraYaw:0,walk:true,jumpPressed:false,jumpHeld:false});p.step(1/60);acc-=1/60;}}assert(c.position.z>1.2&&c.position.z<1.6,JSON.stringify(c.position));assert(Math.abs(c.position.y)<.15);assert(c.grounded);p.world.free();});
