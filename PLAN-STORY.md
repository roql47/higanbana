# 피안화 (彼岸花) — 스토리 통합 설계 v1.0

> 스토리보드 `higanbana_full_storyboard_act1_fin.md`(ACT 1~35 + 엔딩 4종 + FIN)를
> **히간바나 v0.12 빌드 위에 얹는** 설계. 맵·동선·목표·요괴 공략·실패/체크포인트·엔딩 분기·컷신·에셋을 확정한다.
>
> 핵심 발견: **기존 게임 루프가 그대로 1부다.** v0.12의 "공물을 모아 신사에 바치면 탈출"은
> 스토리보드에서 히간누시가 플레이어를 속이는 가짜 퀘스트(ACT 5~23)와 동일한 문장이다.
> 기존 빌드를 버리는 게 아니라, 기존 빌드가 **"속는 구간"** 이 되고 그 위에 진상·복원·선택이 얹힌다.

작성일: 2026-08-20 · 기반: [PLAN-HORROR.md](PLAN-HORROR.md) v0.12 · 상태: 각색 13건 **잠정 채택**("일단 진행", 2026-08-20 — 개별 이의는 §0 번호로) · **S0 완료 · S1 진행 중**(새 맵 `world/higasato` + 프롤로그)

---

## 0. ⚠️ 컨펌 필요 — 각색 13건

스토리보드와 다르게 가거나, 스토리보드가 비워둔 곳을 채운 결정. **번호로 답해주면 된다** (예: "1~5 OK, 6은 B안").

| # | 각색 | 왜 필요한가 | 권장 |
|---|---|---|---|
| 1 | **공물 교체**: 기존 신도 5공물(신주·쌀·소금·물·사카키) → 스토리보드 7공물(방울·머리빗·동전·게다·거울·봉인패·사요) | 공물 하나하나가 원혼의 미련과 1:1로 묶여야 2부가 성립. 배치 구역은 기존 5곳을 재활용(2.3절) | 채택 |
| 2 | **기존 요괴 편입**: 팔척귀신·여우 요괴·도로타보·까마귀를 "봉인의 파수꾼"으로 계속 사용 (스토리보드에 없는 요괴) | 추격 AI·모델·순찰 시스템 전량 재사용. 스토리보드 ACT 16-9의 "공물을 지키기 위해 접근자를 쫓아내는 존재"가 정확히 이 자리다. ACT 17의 "이번에는 공격하지 않는다" 반전이 더 세진다 | 채택 |
| 3 | **피안화 이원화**: 자생 군락 = 안전지대(기존 유지) / 히간누시가 피우는 "유인 길" = 금기 二 위반 장치(신규) | 기존 설계(피안화=안전지대·길 앵커)와 스토리보드(피안화 길=죽음)가 정면 충돌. 정적 군락 vs 플레이어 쪽으로 고개를 돌리며 줄지어 피는 길로 시각 구분 | 채택 |
| 4 | **기존 폐가 → "할머니의 집"**: 자매가 축제 때 머문 외가로 확정. **사요의 일기**(진엔딩 조건인데 스토리보드에 위치 미정)를 이 집 벽장에 배치 | 실내·벽장·장지문이 이미 구현돼 있음. "죽은 할머니"가 미오를 유인한 이유(외가)도 설명됨 | 채택 |
| 5 | **공물 순서 반고정**: 1~3 고정(사당→폐교→우물) → 4·5 자유(묘지·여관) → 6 저택(4·5 완료 시 개방) → 7 신사 | 기억 파편·방송 이벤트가 순번에 묶여 있어 완전 자유는 불가. 완전 고정은 지루. 게이트는 "UI가 다음 목표를 하나씩만 준다"(=히간누시가 조종 중이라 디제제틱) | 채택 |
| 6 | **광원**: A안 — 손전등으로 전면 교체 / **B안 — 손전등으로 시작 → ACT 5 신사 앞에서 배터리 사망 → 초칭 획득** | 스토리보드는 손전등 명시, 기존 게임 정체성은 초칭(빛 3단=감지 다이얼·초칭 점등·초칭오바케). B안이면 코드 100% 유지 + "이 마을은 전기가 아니라 불의 규칙"이라는 연출 획득 | **B안** |
| 7 | **주인공 = 미오**: 기존 여행자 캐릭터를 미오(16세, 교복+가방+사진)로 재의상 또는 Tripo 재생성 | 애니 9클립·컨트롤러 그대로. 외형만 교체 | 채택 |
| 8 | **과거 시점 = 1인칭**: 프롤로그·기억 파편·ACT 16 전반(어린 미오)을 1인칭으로, 현재는 3인칭 유지 | 6세 시점(낮은 눈높이)의 공포 연출 + 어린 미오 풀 애니메이션 부담 절감. "기억 속 = 자기 눈"이라는 문법 | 채택 |
| 9 | **ACT 16 축약**: 30~40분 풀 플레이어블 → **플레이어블 비네트 3개(15~20분) + 기록 몽타주**. 군중 87명은 개별 모델이 아니라 **얼굴 없는 안개 실루엣 셰이더** | 참사 6개 구역 전부를 플레이어블로 만들면 사실상 게임을 두 개 만드는 스코프. 실루엣 군중은 "잊힌 사람들"이라는 주제와도 맞음 | 채택 |
| 10 | **죽음 규칙 변경**: v0.12 D8(즉사→스테이지 재시작) → **지장보살 체크포인트제** | 10~14분 스테이지용 규칙을 4시간+ 서사 게임에 그대로 쓰면 이탈. 6절 | 채택 |
| 11 | **최종장 종 타이머 구체화**: 기본 6분마다 1성 + 최종장 사망 시 +1성. 7성 시점에 봉인 4/6 미만이면 「귀향」 강제 | 스토리보드의 "일정한 간격으로 울리며 붕괴 단계를 알린다"를 수치로 확정 | 채택 (타이머 자체가 싫으면 회수) |
| 12 | **컷신 전부 인게임 실시간** — 프리렌더 영상 0개 | 웹 배포에서 동영상은 용량 즉사. 룩 일관성·수정 용이. 8절 | 채택 |
| 13 | **봉인패 = 강제 소멸 도구**: 공물 6(봉인패)을 2부에서 원혼에게 "쓰면" 즉시 소멸(전투 스킵) — 단 진엔딩 영구 상실 | 스토리보드의 "강제로 소멸시키는 선택도 가능"을 아이템 하나로 구현. 편한 길=나쁜 길 구조 | 채택 |

이 외 스토리보드의 어색한 지점을 채운 소결정(컨펌 불요, 이견 있으면 지적):
- **금기 一("해가 진 뒤 공물을 옮기지 말 것")** — 게임이 항상 밤이므로 "공물 소지 중 = 운반 상태(파수꾼 강화)"로 재해석 (3.2절)
- **미오가 공물 6을 받침대에 안 놓는 이유** — 받침대 인터랙트가 "작동하지 않고" UI가 「지하로 가져와라」로 바뀜. 조작 자체가 서사(이미 의심 중)와 일치 (4.5절)
- **명부 87명 낭독** — 전원 낭독은 지루. 수집률에 따라 낭독 연출이 요약·확장 (4.3절)
- **폭력 사망 16명(회관) 기록 위치** — 회관 건물을 새로 만들지 않고 광장 창고 + 촌장 저택 회의록에 분산 (4.3절)
- **참사 연도** — 스토리보드는 「10년 전」만 말하고 연도를 안 준다. ACT 4 의 공고판은 날짜를 **적어야** 하는 물건이라
  「二〇一五年 九月 二十三日」(추분)로 확정했다. 현재는 2025년 가을. 다른 연도가 좋으면 `higasato/speaker.ts` 한 줄이다

---

## 1. 전체 구조 — 35 ACT → 게임 4부

### 1.1 파트 구분

| 부 | ACT | 내용 | 플레이 성격 | 기존 v0.12와의 관계 |
|---|---|---|---|---|
| **프레임** | 1~5 | 프롤로그·버스·돌비석·마을 진입·가짜 퀘스트 개시 | 연출 + 튜토리얼 | 신규 (진입로·버스·비석) |
| **1부 수집장** | 6~15 | 공물 6개 수집 — 매번 "탐색→획득→추격→운반→봉납" | **기존 게임 루프 그대로.** 봉납할수록 세계가 나빠짐 | v0.12가 곧 1부. 공물·요괴에 서사가 붙음 |
| **막간 진상장** | 16~23 | 과거 플레이어블 + 신사 지하 + UI의 배신 | 비네트 3개 + 연출 | 신규 |
| **2부 복원장** | 24~26 | 같은 맵 재방문, 공물 반납 + 미련 해결. 요괴가 조력자로 | 자유 순서 퍼즐 + 시간 압박 | 맵 재사용, 상태만 "피안 침식" |
| **최종장** | 27~35 | 선택 → (진루트) 백귀야행 보스 3페이즈 → 탈출 → 에필로그 | 보스 + 추격 시퀀스 + 연출 | 신규 |

### 1.2 난이도 곡선의 재해석

v0.12의 "공물 수 = 난이도" 표는 그대로 **봉인 해제 단계**가 된다. 수치 재사용, 명분만 교체:

| 봉납 수 | 파수꾼 | 속도(플레이어 3.6) | 월드 이벤트 (스토리보드 동기화) |
|---|---|---|---|
| 0 | 팔척 1, 배회 | 3.2 | 축제 흔적·스피커 방송 (ACT 4) |
| 1 방울 | 팔척 활성 | 3.4 | 땅 울림, 「……하나」, 돌계단 틈 피안화 새싹 (ACT 7) |
| 2 머리빗 | 순찰 반경 확대 | 3.6 | 본전 긁는 소리, **가로등 일부 붉게** (ACT 9) |
| 3 동전 | 감지 ×1.3 | 3.6 | **하늘 암적색·달 소멸**, 벽에서 피안화, 방송 두 목소리 충돌 (ACT 11) |
| 4 게다 | 여우 요괴 합류 | 3.9 | 본전 문 틈 + 흰 손 (ACT 12) |
| 5 거울 | 전역 사냥 | 3.9 | 기억 대량 회복, 거울들에 여인 (ACT 14) |
| 6 봉인패 | — 저택에서 과거로 강제 전환 | — | ACT 15→16 |

---

## 2. 맵 필드 구성

### 2.1 배치 원칙 — 갈래길 5개에 스토리 목적지를 하나씩

v0.12 맵 개편으로 만든 갈래길 5개(`ROUTES[]`)가 그대로 스포크가 된다. **각 길의 끝에 스토리 구역 하나씩** — 길을 외우는 재미(기존)와 "그 길엔 그 요괴"(기존 순찰 앵커)가 서사 구역과 묶인다.

| 길 (기존) | 스토리 목적지 (신규/개조) | 담당 파수꾼 (기존) |
|---|---|---|
| ① 참배로 | 중앙 신사(허브) + 중턱 서측 골목의 **공동우물** | 팔척귀신 |
| ② 논두렁길 | **할머니의 집**(기존 폐가 승격) + 논 | 팔척·도로타보 |
| ③ 뒷산 오솔길 | **무연불 묘지**(기존) 경유 → **오래된 사당** | 여우·까마귀 |
| ④ 돌계단 뒷길 | **촌장의 저택**(신사 동편, 마을 최고지) | 여우 요괴 |
| ⑤ 대숲길 | 광장 경유 → **폐교**(동쪽 끝) | 팔척귀신 |
| 광장 남측 골목 | **폐여관** | — (실내형) |

### 2.2 전체 지도

```
                         N  뒷산 능선 (맵 경계)
   ┌──────────────────────────────────────────────────────┐
   │            [오래된 사당]                                │
   │              │                    [촌장의 저택]          │
   │   ③뒷산 오솔길│         ┌─────────┐    │④돌계단          │
   │              │         │ 신사 경내 │────┘                │
   │   [무연불 묘지]│         │본전▼지하  │                     │
   │   [피안화 군락]│         └────┬────┘   [광장·야구라]       │
   │              │      센본토리이 │        │  └[창고·기록]    │
   │   [할머니의 집]│(벽장:일기)     │        [폐여관]           │
   │        │     │              │         │                │
   │   ②논두렁길   │   [공동우물]──┤①참배로  │⑤대숲길           │
   │        │  ┌──┴───┐  (골목)   │        │      [폐교]     │
   │        └──│ 논    │─────────┼────────┴───────┘         │
   │           │(흙둑) │         │                           │
   │           └──────┘      [돌비석]                        │
   └───────────────────────────┼──────────────────────────┘
                            스폰(남)
                               │  삼나무 흙길 (인트로 전용 통로)
                          [버스 종점]  ← 맵 밖 별도 미니 씬
```

### 2.3 구역 명세

**개조** (기존 구역에 스토리 부여):

| 구역 | 기존 | 추가/변경 |
|---|---|---|
| 신사 경내 | 배전·본전·초즈야·코마이누 | **받침대 7개 반원 + 중앙 석판**(퀘스트 개시), 본전문(굳게 잠김→틈→개방), **지하 입구** |
| 무연불 묘지 | 묘석·웅크림 은신 | 돌탑·**이름 지워진 아이 무덤**(게다), 공기놀이 소리, 아이 귀신, **석등 점화 퍼즐**(2부), 반복 루프 연출 |
| 할머니의 집 | 폐가(소금·짚신·벽장) | 가족사진 액자, **벽장 = ACT 16-13 사요가 미오를 숨긴 곳**, 2부에 **사요의 일기** 출현. 소금·짚신 보상 유지 |
| 논 | 흙둑 미로·도로타보 | 2부: 우물의 여자 성불 후 **지하 수로 입구**가 배미 하나에 열림 |
| 광장·야구라 | 축제 무대 | **방송 스피커**(전 맵 이벤트 음원), 창고 = 회관 기록물 보관(폭력 사망 16명 명부) |

**신규**:

| 구역 | 위치 | 공간 설계 | 핵심 연출 |
|---|---|---|---|
| **버스 종점 + 삼나무 흙길** | 맵 남측 밖, 별도 미니 씬 | 버스 실내(정적) → 60m 외길 | 내리면 뒤에 버스·도로가 없다(뒤돌기 트리거로 소품 스왑). 인트로 후 재진입 불가 |
| **돌비석** | 스폰 북쪽 20m, 참배로 초입 | 이끼 낀 비석 1 + 공터 | 표면 닦기 인터랙트 → 三禁 각인 → "미오야" 목소리 → **강제로 세 번째 금기 위반** |
| **오래된 사당** | ③길 끝, 산중턱 | 4×4m 사당 + 툇마루, 실내 천장 높음(대들보) | 방울 획득 → 문 잠김 → 로쿠로쿠비 첫 추격 (5.3.1) |
| **폐교** | 동쪽 끝, ⑤길 너머 | **2층 목조 교사**: 복도 L자 2개·교실 3·음악실·방송실·교무실·준비실. 복도 폭 2.2m(카메라 검증 필요) | 칠판 날짜·출석 방송·거울 위치 왜곡·머리빗(피아노 안) → 시선 추격전 (5.3.2) |
| **공동우물** | 참배로 중단 서측 골목 | 지상 우물가 + **수직 샤프트 12m**(밧줄) + 바닥 수공간·제단 | 끊긴 금줄, 벽의 「내 아이가 아니다」 반복 각인, 동전 획득 → 우물의 여자 (5.3.3) |
| **폐여관** | 광장 남측 골목 | 2층 실내. **불탄 상태(현실)/온전한 상태(거울 속)** 이중 재질·프랍 세트 | 큰 거울 = 과거 축제가 비침. 거울 퍼즐 하우스 (5.3.5) |
| **촌장의 저택** | ④길 위, 신사 동편 언덕 | 대형 저택: 현관·큰방·서재 + **지하 기록실**. 문 부순 흔적 | 문서 3종 + 봉인패 → 저택이 10년 전으로 → 환영 주민 수색 스텔스 (5.3.6) |
| **신사 지하** | 본전 아래 | 긴 계단(끝이 안 보이는 연출: 안개+반복 세그먼트) → **검은 문 공동**(보스 무대, 반경 ~18m) | 피안화 뿌리 벽면, 받침대 7의 "진짜" 원형, 히간누시 |

### 2.4 1부 월드 연출 — "끝나지 않은 축제" 디테일

ACT 4의 생활 흔적은 기존 `scares.ts`(1회성 연출 시스템)에 항목 추가로 구현:

- 찻잔 김(파티클), 빈집 TV 잡음(포인트라이트 플리커+노이즈 텍스처), 현관의 젖은 게다, 바람 없는 풍경 소리(기존 풍경 샘플), 골목 끝 사람 실루엣→시선 돌리면 소멸(기존 놋페라보 연출 계열)
- 마을 스피커 방송: 봉납 0·3·5회 시점에 전 맵 방송 (10년 전 날짜 고지 → 공물 회수 경고 → 두 목소리 충돌)

### 2.5 2부 월드 상태 — "피안의 밤"

같은 맵, 상태 전환 하나로:

- 하늘: **거대한 붉은 달**(신규 스카이 텍스처) + 암적색 그라데이션
- 피안화가 길·지붕·벽에서 증식(인스턴스 수 3배, 성장 애니), 10년 전 축제 프랍(노점 등불 켜짐)이 현재 폐허와 **깜빡이며 겹침**(프랍 세트 A/B 크로스페이드)
- **피안 잠식 지대**: 맵의 30%가 붉은 안개 지대 — 안에서는 가짜 귀환자(안개 실루엣)가 이름을 부른다(금기 三 집중 구간). 성불이 진행될수록 지대가 줄어든다 = "안전 구역이 줄어든다"의 역방향 보상
- 신사 종이 주기적으로 울림 (6.4 타이머)

---

## 3. 맵 동선

### 3.1 원칙 — 허브 앤 스포크, **왕복이 곧 게임**

신사(허브)에서 스포크로 나가 공물을 얻고 돌아온다. **가는 길 = 탐색·퍼즐(안전 편), 오는 길 = 운반 상태 추격(위험 편)**. 같은 길을 두 번 걷지만 상태가 달라 다른 게임이 된다. 복귀는 항상 대안 루트가 있다(갈래길 5개의 존재 이유).

### 3.2 운반 상태 — 금기 一의 시스템화

공물을 들고 봉납 전까지:

- 파수꾼 감지 배율 ×1.5, 마츠리바야시 근접 레이어가 한 단계 항시 상승("공물이 운다")
- 공물 전용 사운드: 방울은 뛰면 울리고(소음 이벤트), 거울은 요괴 위치를 힐끗 비추고(보상), 게다는 무해 — **공물마다 운반 개성**
- 잡히면 사망이 아니라 **강탈 → 원래 자리로 되돌아감**(다시 가야 함). 파수꾼이 "죽이는 게 아니라 돌려놓는다"는 복선 (5.2)

### 3.3 1부 동선 표

| # | 공물 | 진입 동선 | 내부 플레이 | 복귀(추천/대안) | 게이트 | 예상 |
|---|---|---|---|---|---|---|
| 튜토리얼 | — | 버스 종점→흙길→돌비석→참배로→마을(ACT 4 연출 존)→신사 | 이동·초칭(손전등)·조사·인벤·사진 학습 | — | — | 12분 |
| 1 | 붉은 방울 | 신사→③뒷산 오솔길(묘지 앞 통과, 아이 웃음 복선)→사당 | 기억 파편 →획득→로쿠로쿠비 실내 추격→탈출 | ③ 그대로 복귀(첫 운반 튜토리얼) / 묘지 가로질러 ② | UI가 사당만 지시 | 14분 |
| 2 | 붉은 머리빗 | 신사→①→광장→⑤대숲길→폐교 | 폐교 탐색·기록 수집·거울 학습→획득→시선 추격전 | ⑤ / 운동장 담 넘어 ④돌계단 | 봉납 1 후 교문 사슬이 "끊어져 있음" | 20분 |
| 3 | 동전 세 닢 | 신사→①참배로 중단→서측 골목→우물 | 하강(안전)→제단·기록→획득→상승 추격 | ①(최단이지만 팔척 순찰 피크) / ②논두렁길 우회 | 봉납 2 후 금줄이 "풀려 있음" | 15분 |
| 4 | 아이의 게다 | ②→할머니의 집 경유(사진 떡밥)→③ 진입 or ③ 직행→묘지 | 아이들 미로(비적대)→게다 획득→전원 정지 연출 | ③ / 피안화 군락 통과(안전지대 체험) | 4·5 자유 | 15분 |
| 5 | 깨진 거울 | 광장→남측 골목→폐여관 | 거울 퍼즐 하우스→획득→긴 기억(ACT 14, 강제) | ⑤ / ④ | 4·5 자유 | 20분 |
| 6 | 제문과 봉인패 | 광장→④돌계단→저택 | 문서 3종(이름·희생 조건)→획득→**과거 전환** | 복귀 없음 — ACT 16 진입 | 4·5 완료 시 저택 문 개방 | 15분+ACT16 |

- 봉납 왕복이 회마다 다른 길을 쓰도록 UI 문구가 아니라 **파수꾼 순찰 배치**로 유도한다(기존 앵커 시스템 활용): 예) 봉납 3 이후 ①에 팔척 고정 배치 → 자연히 ②·④를 개척
- 4·5를 자유로 두는 이유: 봉납 3 직후 방송 충돌(ACT 11)로 플레이어가 처음 의심하는 시점 — "시키는 대로 안 하는" 여지를 주는 구간이 서사와 맞는다

### 3.4 2부 동선 — 자유 순서 + 조력자가 길을 연다

6곳 자유 방문(잠식 지대·종 타이머가 압박). **성불할 때마다 ACT 26의 조력이 실제 단축로/안전화로 구현**:

| 성불 | 열리는 것 | 동선 효과 |
|---|---|---|
| 유리(폐교) | 교문·복도를 닫아 파수꾼 차단 | ⑤대숲길 안전화 |
| 우물 모자 | **우물 바닥~신사 지하 수로** 개방 | 신사 서측 단축 + **최종장 진입 복선** |
| 묘지 아이들 | 석등 자동 점등 | ③뒷산 오솔길 안전화 |
| 여관 주인 | 여관 1층 거울 ↔ 광장 거울 통로 | 무너진 골목 우회(2부에서 광장 골목 붕괴) |
| 로쿠로쿠비(사당) | 긴 목으로 끊어진 다리 연결 | ② 수로 단축 |
| 촌장 | 저택 앞 가짜 귀환자 무리 차단 | ④돌계단 안전화 |

최종장 신사 진입 시, 성불 수에 따라 열려 있는 길이 달라진다(0~2: 정면 ①뿐·최고 난도 / 3~5: 선택지 / 6: 전 루트 + 백귀야행 호위 연출).

---

## 4. 플레이어 목표 — 퀘스트 설계

### 4.1 이중 목표 구조 — UI가 히간누시다

- **1부 UI(=히간누시)**: 시스템 폰트, 기계적 명령문. 「~를 찾아라」「~를 가져와라」
- **2부 UI(=사요)**: ACT 23에서 UI가 깨진 뒤, **손글씨체**로 재구축. 「~를 돌려놓자」「~의 이름을 찾아주자」— 명령이 아니라 청유
- **최종장**: UI 없음(목표 표시 자체가 사라짐 — 자립). 종 카운트만 화면에 남는다

이 3단 변화가 "퀘스트 UI 자체가 반전 장치"라는 핵심을 시각화한다.

### 4.2 목표 전량 표 (표면 문구는 실제 UI 문안)

| 구간 | 표면 목표(UI 문안) | 실제 달성 조건 | 서브 목표(선택) |
|---|---|---|---|
| ACT 2~3 | 「히가사토로 가는 길을 따라가라」 | 흙길 통과, 비석 조사 | 사진 확인(인벤 튜토리얼) |
| ACT 4~5 | 「마을 중앙의 신사로 가라」→「7개의 공물을 찾아라 0/7」 | 석판 조사 | 생활 흔적 5종 조사(도감) |
| ACT 6~7 | 「오래된 사당을 조사하라」→「방울을 신사로 가져와라」 | 방울 봉납 | — |
| ACT 8~9 | 「폐교에서 두 번째 공물을 찾아라」 | 머리빗 봉납 | 교무실 일지·**크레용 글·그림**(준비실), 명부 이름 +17 |
| ACT 10~11 | 「우물 아래를 조사하라」 | 동전 봉납 | 벽 각인, 명부 +9 |
| ACT 12 | 「공동묘지에서 공물을 찾아라」 | 게다 봉납 | 아이들 대화 3종(사요 언급), 명부 +11 |
| ACT 13~14 | 「폐여관의 거울을 조사하라」 | 거울 획득(봉납은 컷신 후) | 거울 속 축제 관찰 3지점, 명부 +14 |
| ACT 15 | 「촌장의 저택을 조사하라」 | 봉인패 획득 | 문서 3종(전부 필수), 명부 +16 |
| ACT 16 | (UI 정지 — 과거에는 퀘스트가 없다) | 비네트 3개 통과 | 참사 기록 열람 |
| ACT 17~23 | 「마지막 공물을 찾아라」→글리치→「나를 꺼내줘」 | 본전 진입(받침대 봉납 불가 처리) | — |
| ACT 24~25 | 손글씨 「공물을 원래 자리로 돌려놓자 0/6」 | 반납 6 + 미련 해결 0~6 | 명부 잔여 +12(요괴 습격분, 잠식 지대)·+8(실종자, 피안 잠식 지대 심부) |
| ACT 25-x | 구역별: 「유리의 이름을 찾아주자」 등 (7.4 판정) | 원혼별 성불 조건 | 사요의 일기(할머니의 집) |
| ACT 27~ | (UI 소멸) 종 카운트만 | 선택 → 보스 3페이즈 → 탈출 런 | — |

### 4.3 명부 87명 — 수집 시스템

- 기록물 프랍 약 **26종**(일지·명부·위패·편지·회의록·병원 기록…) → 조사 시 이름이 **명부 화면**(인벤 탭)에 등록. 구역 합계가 스토리보드 집계와 일치: 폐교 17 / 우물 9 / 여관 14 / 묘지·산길 11 / 폭력(광장 창고+저택) 16 / 요괴 습격 12 / 피안 실종 8 = **87**
- 요괴 습격 12·실종 8은 **2부 잠식 지대에서만** 얻는다(2부에 필드로 나갈 이유)
- 촌장 성불 의식(ACT 25-6)에서 낭독 연출: 수집 100%면 이름이 화면을 가득 채우며 생전 모습 스틸이 스치고, 미달이면 빈 줄이 남는 연출 — **빈 줄이 눈에 보이는 것 자체가 진엔딩 힌트**
- **사요의 일기**: 2부에만 할머니의 집 벽장 안(10년 전 미오가 숨었던 자리)에 출현. "잊히는 게 무서웠다"는 ACT 30 대사의 근거 문서

### 4.4 히간누시 UI 변조 이벤트 목록

| 시점 | 연출 |
|---|---|
| 봉납 3 (ACT 11) | 방송 두 목소리 충돌. **UI는 멀쩡** — "UI만은 정상"이 오히려 복선 |
| 저택 문서 1 (ACT 15) | 카운터가 0.5초 깨졌다 복구 (「7개의 공물을 찾아라」 글자 순서가 섞였다 돌아옴) |
| ACT 17 신사 앞 | 3단 변조: 「마지막 공물을 찾아라」→「마지막 봉인을 없애라」→「나를 꺼내줘」. 원혼들이 길을 막는 연출과 동시 |
| ACT 18 지하 | 히간누시가 손을 내밀면 목표가 강제 표시 「마지막 공물을 바쳐라」 + 확인 버튼이 화면을 따라다님(거부 = 뒤로 걷기) |
| ACT 23 | 지금까지의 모든 목표 문구가 역재생→취소선→붕괴. 손글씨 UI 재구축 |

글리치 구현: UI는 DOM이므로 CSS 셰이더 대신 **문자 치환·자간 붕괴·색수차 이중상**으로 처리(비용 낮음).

---

## 5. 요괴 기믹 · 파훼법

### 5.1 2계층 로스터

| 계층 | 구성 | AI | 역할 |
|---|---|---|---|
| **필드 파수꾼** (기존) | 팔척귀신·여우 요괴·도로타보 (+까마귀·놋페라보·초칭오바케·지장 연출) | 기존 FSM 그대로 | 스포크 사이 필드의 상시 긴장. **정체 = 봉인을 지키는 쪽** |
| **구역 원혼** (신규) | 로쿠로쿠비·유리·우물의 여자·묘지 아이들·(여관=거울 자체)·(저택=환영 주민) | 구역 한정 전용 로직 | 스토리 보스. 1부 적 → 2부 성불 대상 → 최종장 조력 |

**파수꾼 정체 반전의 심기**: 1부 내내 파수꾼은 플레이어를 죽이는 게 아니라 (a) 공물 소지 시 강탈·원위치, (b) 비소지 시에만 사망 연출. 로쿠로쿠비의 "놓고 가", 잡혔을 때 가방부터 노리는 모션 — 전부 ACT 17 "이번에는 공격하지 않는다"의 복선.

### 5.2 필드 파수꾼 — 기존 규칙 + 수정 3줄

- **팔척귀신**: FSM·감지·소금 기존 그대로. 수정: `GRAB` 분기 — 공물 소지 시 강탈(공물이 원위치로, 플레이어는 그 자리 생존) / 비소지 시 사망→체크포인트. 2부: 적대 해제, 잠식 지대 경계에 서서 **가짜 귀환자를 노려보는** 배치 연출
- **여우 요괴**: 봉납 4부터 신사 경내·④. 2부: 신사 진입로의 문지기 — 성불 3 이상이면 길을 비켜준다(고개를 숙임)
- **도로타보**: 논 전용 기존 그대로(즉사 아님·밀어냄). 2부: 수로 입구를 파낸 자국과 함께 침묵 — "논을 돌려받았다"
- **오니비(신규, 파수꾼 아님 — 히간누시 측)**: 금기 二 장치. 피안화 유인 길 위를 떠다니는 도깨비불(기존 반딧불이 파티클 개조). 따라가면 유인 길이 등 뒤에서 닫히고 잠식 안개+가짜 귀환자 조우, 최악은 절벽 낙사(묘지 뒤 산길). **파훼: 애초에 따라가지 않는 것.** 유도된 경우 초칭을 끄면 오니비가 길을 잃는다(빛에 반응하는 습성 — 빛=리스크 규칙의 역이용)

### 5.3 구역 원혼 6종

#### 5.3.1 로쿠로쿠비 — 사당 (공물 1)

- **기믹**: 방울을 집으면 문이 잠기고 천장에서 목이 내려온다. **몸통은 제단 뒤에 고정, 목만 대들보를 타고 추적** — 실내 전체가 목의 사정권이지만 꺾이는 각도에 한계가 있다. 방울 소지 중 달리면 방울이 울려 목이 즉시 방향을 잡는다(금기 一 예습)
- **파훼**: ① 웅크려 이동(방울 무음) ② 목이 대들보를 감아 도는 동안 사각(툇마루 아래·제단 뒤)으로 ③ 장지문 3개 중 진짜 출구는 **종이가 바람에 떨리는 문** — 나머지는 열면 벽
- **실패**: 잡히면 방울을 빼앗기고 사당 밖으로 던져진다(사망 없음 — 첫 보스는 관대하게). 방울은 제단에 되돌아가 있다
- **2부**: 공격 안 함. 어린 미오의 환영 대화(ACT 25-1) 후 방울을 제단에 놓으면 긴 목으로 금줄을 감아 봉인 완성

#### 5.3.2 얼굴 없는 학생(나츠메 유리) — 폐교 (공물 2)

- **기믹**: **바라보면 정지, 시야를 벗어나면 접근**(발소리 없음 — 접근 신호는 형광등 깜빡임과 책상 긁는 소리뿐). 복도 거울에는 **실제와 좌우 반전된 위치**로 비친다 → 처음엔 공포, 규칙을 알면 등 뒤 확인 도구. 머리빗 획득 후에는 천장·창밖에도 출현(정지 규칙은 유지)
- **파훼(추격전)**: ① 뒷걸음 이동 + 초칭 약불로 배터리(초) 관리 ② 거울로 등 뒤 파악 ③ 교실을 가로질러 시야를 끊지 말 것 — 끊기는 순간 대시해 온다. 시야 유지 상태로 사물함에 들어가면 반드시 발각(기존 은신 규칙 "은신은 도망의 마무리")
- **실패**: 잡히면 화면 가득 얼굴 없는 얼굴 + 「내 이름…」 → 교사(校舍) 입구 체크포인트. 사망 카운트 +1
- **2부(성불)**: 단서 3종(교무실 명부·졸업사진·방송실 녹음테이프) 수집 → **방송실에서 이름을 방송** 「나츠메 유리」 → 복도의 그녀가 멈춤 → 접근해 머리빗으로 빗겨주는 인터랙트 → 얼굴 복원(텍스처 스왑) → 머리빗을 음악실 피아노에 반납

#### 5.3.3 우물의 여자 — 공동우물 (공물 3)

- **기믹**: 수직 공간. 하강은 안전, **동전을 집는 순간 물에서 일어선다**. 그녀의 위치 = 물소리(첨벙·물방울 반향). 잡히면 **얼굴을 확인하고** 「너는 아니야」 — 첫 회는 던져버림(낙하 피해·아래로 리셋), **2회째부터 즉사**
- **파훼(상승 추격)**: ① 샤프트 벽감(니치) 3곳에 몸을 붙여 시선 끊기 ② 주운 조약돌을 아래로 던져 물소리 유인(기존 던지기 재사용) ③ 밧줄 구간은 무방비 — **그녀가 물속에 잠긴 타이밍**(물소리가 잦아들 때)에만 오른다
- **실패**: 2회째 접촉 즉사 → 우물가 체크포인트
- **2부(성불)**: 단서 2종(병원 기록=여관 구역, 하루의 위패=묘지) → 우물에서 가짜 목소리가 부를 때 **동전을 정확한 간격으로 3회 투척**(가짜 목소리가 갈라지는 사운드 큐에 맞춰) → 환영 소멸 → 진짜 하루 등장 → 동전 반납

#### 5.3.4 묘지의 아이들 — 공동묘지 (공물 4)

- **기믹**: 비적대. 공격 대신 **길 표식(석등 불·돌탑)을 옮겨** 묘지를 무한 루프로 만든다. 잘못된 방향으로 나가면 같은 묘열이 반복(공간 재배치). 웃음소리가 나는 쪽 = 항상 가짜
- **파훼**: **여섯 지장보살이 바라보는 방향**이 진짜 길(기존 "움직이는 지장" 연출의 재활용 — 세는 게 아니라 시선을 읽는다). 게다를 집으면 아이들이 일제히 정지하고 사요를 언급(핵심 떡밥)
- **실패**: 사망 없음. 루프에 시간을 뺏기는 동안 파수꾼(여우)이 묘지에 진입해 온다 — 지연 = 위험
- **2부(성불)**: 기록(아버지의 사망 위치=③산길) 확인 → **석등을 지장의 시선 순서대로 점화**해 진짜 귀로를 만든다 → 아버지 영혼 등장 → 게다를 무덤 앞에 반납

#### 5.3.5 폐여관 — 거울의 집 (공물 5, 원혼 없음·공간이 보스)

- **기믹**: **현실(불탄)/거울 속(온전)** 이중 상태. 거울에만 보이는 문·복도가 있다 — 현실에서 그 자리는 그을린 벽이지만 통과 가능한 곳이 섞여 있다(거울로 판별). 거울 속 축제 인파 중 몇몇이 거울 너머의 미오를 **본다**(3회 고정 이벤트). 여기서 처음으로 **이름 부름 + [대답한다] 프롬프트**가 뜬다(금기 三 시스템, 5.5)
- **파훼**: 손거울(획득 전엔 벽거울)로 현실-거울을 대조하며 이동. 순서: 로비 큰 거울(과거 관찰) → 2층 복도(거울로 뚫린 벽 찾기) → 불탄 객실(화장대) → 깨진 손거울 획득 → 긴 기억(ACT 14) 강제 재생
- **실패**: 사망 없음. 거울 속 시선 이벤트에서 대답하면 잠식 +1과 함께 여관 전체가 10초간 화재 상태로 점멸(퇴로 암기 강제)
- **2부(성불)**: 아내의 편지 4통 수집(여관 각층) → 로비 거울 앞에서 낭독 → 가짜 아내는 같은 말만 반복(대비 연출) → 여관 주인이 깨닫고 거울을 깨는 컷 → 거울 조각을 화장대 봉인 홈에 반납

#### 5.3.6 촌장의 저택 — 환영 주민 (공물 6)

- **기믹**: 봉인패를 드는 순간 저택이 10년 전으로. **횃불 든 환영 주민들의 수색 스텔스**(광원 = 시야콘 시각화, 기존 senses 재사용). 목적은 탈출이 아니라 **잡히는 것** — 어떻게 플레이해도 최후엔 벽장에 숨고, 벽장 문틈 시점 그대로 ACT 16 과거로 전환된다
- **파훼**: 정식 파훼 없음(연출형). 오래 버틸수록 문서 낭독 보이스가 더 재생됨(보상)
- **실패**: 없음 — 잡혀도 벽장 전환으로 수렴

### 5.4 히간누시

- **1부**: 전투 없음. UI·방송·목소리로만 존재(4.4)
- **최종 보스(ACT 28, 진루트 한정) 3페이즈** — 스토리보드 그대로, 판정만 명세:

| 페이즈 | 플레이 | 실패 조건 | 실패 시 |
|---|---|---|---|
| 1 가짜 기억 | 가짜 사요의 원망 대사 → 지금까지 수집한 기억 문장 4택(정답 「네가 여섯 살이었잖아」) | 오답 2회 | 페이즈 재시작 + 종 +1성 |
| 2 죽은 자의 부름 | 사방에서 이름 부름 + [대답한다] 프롬프트 연타 유혹. **종 3성까지 무대응 이동**(발밑이 무너지는 원형 무대) | 프롬프트를 누름 / 낙하 | 페이즈 재시작 + 종 +1성 |
| 3 봉인의 재구성 | 여섯 봉인의 빛기둥을 중앙으로 잇는 이동 퍼즐(순서 = 봉납했던 순서 — 자기 죄를 자기 손으로 되감기) | 팔 휩쓸기 피격 3회 | 페이즈 재시작 |

- 비주얼: 인간형(기모노+피안화 얼굴) → 3페이즈에 거대형(지붕 뚫는 상체 + 치마 아래 팔 다수 = 본 몇 개 + 셰이더 팔)

### 5.5 금기 3종 = 시스템 3종

| 금기 | 시스템 | 위반 시 |
|---|---|---|
| 一 해 진 뒤 공물을 옮기지 말 것 | **운반 상태**(3.2) — 게임 전체가 위반의 연속이라는 구조 자체가 서사 | 파수꾼 강화 (상시) |
| 二 피안화 길을 따라가지 말 것 | **유인 길 + 오니비**(5.2) — 자생 군락(안전)과 시각 구분: 유인 길 꽃은 플레이어 쪽으로 고개를 돌리고 일렬로 핀다 | 잠식 지대 조우·낙사 위험 |
| 三 이름 불려도 대답하지 말 것 | **[E] 대답한다 프롬프트** — UI(=히간누시)가 버튼을 들이민다. 누르면 위반 | 잠식 +1, 가장 가까운 파수꾼이 등 뒤 12m로 텔레포트, 화면 가장자리 피안화 성장 |

프롤로그·돌비석의 "……언니?"는 강제 위반(잠식 1로 시작) — **회차 플레이에서만 침묵 선택지가 생긴다**(7.5).

---

## 6. 실패 조건 · 체크포인트

### 6.1 실패 결과 총표

| 원인 | 결과 | 사망 카운트 |
|---|---|---|
| 팔척·여우에게 접촉(공물 소지) | 공물 강탈 → 원위치. 생존 | — |
| 팔척·여우에게 접촉(비소지) | 사망 연출(피안으로 끌려감) → 체크포인트 | +1 |
| 로쿠로쿠비 | 방울 강탈 + 사당 밖 방출 | — |
| 얼굴 없는 학생 | 사망 → 교사 입구 | +1 |
| 우물의 여자 1회차 | 낙하 리셋 | — |
| 우물의 여자 2회차+ | 즉사 → 우물가 | +1 |
| 도로타보 | 밀어냄 + 위치 노출 | — |
| 오니비 유인 → 절벽 | 즉사 → 직전 지장 | +1 |
| 잠식 지대에서 대답 3연속 | 가짜 귀환자에게 끌려감(사망 취급) | +1 |
| 최종장 보스 페이즈 실패 | 페이즈 재시작 | 종 +1성 |
| 최종 탈출 런에서 뒤돌아봄(ACT 32, 카메라 조작으로 뒤를 봄) | — 즉사 아님. 화면이 1초 하얗게 + 사요 「뒤돌아보지 마!」 1회 경고, 2회째 귀향 엔딩 분기 | — |

### 6.2 체크포인트 — 지장보살 참배

- **수동 세이브**: 각 구역 입구의 지장보살에 [참배] 인터랙트(합장 모션 1.2초 — 무적 아님, 긴장 유지). 슬롯 1 + 자동 3
- **자동 세이브**: 봉납/반납 직후, 실내 진입 직후, ACT 전환 시
- 사망 시: 마지막 체크포인트로. **인벤토리·기록물·월드 상태는 유지**(다시 줍기 없음) — 잃는 것은 위치와 시간뿐. 공포 게임에서 반복 노동은 공포가 아니라 짜증이다
- 지장 = 아이들의 수호신이라는 전승과 일치(세이브 포인트의 디제제틱 명분). 기존 jizo 모델 재사용

### 6.3 숨은 카운터 (HUD 비표시)

```
answered   금기 三 대답 횟수      → 연출 강도·회차 대사 변화
deaths     사망 횟수             → 히간누시 조롱 대사 변주
banished   봉인패 강제 소멸 횟수   → 진엔딩 게이트 (1 이상이면 차단)
```
표시는 오직 화면 가장자리 피안화 덩굴의 양(잠식). 수치 UI를 안 보여주는 것이 이 게임의 HUD 철학(기존 디제제틱 원칙 유지).

### 6.4 최종장 종 타이머

- 2부 시작과 함께 종 0성. **6분마다 +1성**(밸런스 변수). 최종장 사망·보스 페이즈 실패도 +1성
- 매 성마다: 전 맵 종소리 + 화면 펄스 + 잠식 지대 5% 확장
- **7성 시점에 봉인 4/6 미만 → 「귀향」 강제 엔딩.** 4/6 이상이면 종이 멈추고 "마지막 선택"으로 (7절)
- 6봉인 완료 시 즉시 종 정지. 성불 1회마다 −1성 보상(시간을 벌어주는 구조 = 미련 해결이 시간상으로도 이득)

---

## 7. 선택 · 엔딩 분기

### 7.1 추적 플래그

```
seals      0~6    반납된 공물 수
regrets[6] bool   원혼별 성불 (사당·폐교·우물·묘지·여관·저택)
roster     0~87   명부 이름 수
diary      bool   사요의 일기
banished   0~6    봉인패로 강제 소멸시킨 원혼 수
bell       0~7    최종장 종
choice     1~4    마지막 선택
```

### 7.2 분기 로직

```
if (히간누시의 손을 잡음 or bell==7 && seals<4)   → ENDING A 귀향 (BAD)
choice 메뉴 표시 조건: seals>=4
  1 사요의 손을 잡고 문을 연다                    → ENDING A 귀향
  2 미오가 대신 봉인이 된다                       → ENDING B 대신
  3 공물을 복구하고 사요를 남긴다 (seals==6 필요)   → ENDING C 망각
  4 누구도 제물로 남기지 않는다                    → TRUE 피안화
    표시 조건: seals==6 && regrets 전부 && roster==87
              && diary && banished==0
```

- 선택지 4는 조건 충족 시 **3번 아래에 긁힌 글씨로 서서히 나타난다**(손톱으로 새기는 SFX — 엔딩 크레딧 후 4번째 금기와 수미상응)
- 조건 미달로 4가 안 보일 때: 3을 고르면 C로 가되, **비어 있는 네 번째 줄의 공백 자체가 렌더링된다**(회차 유도)

### 7.3 엔딩 4종 연출 요점

| 엔딩 | 조건 요약 | 연출 (전부 인게임) |
|---|---|---|
| A 귀향 | 수락 or 시간 초과 | 아침 버스(인트로 버스 씬 재사용!) → 터널 암전 → 옆자리 얼굴이 피안화 → 차창 밖 피안화 만개 |
| B 대신 | 선택 2 | 피안화 벽 분리 → 구조 씬(어른 사요 모델 불요 — 10년 전 모습 그대로라는 설정) → 꽃밭의 어린 미오 |
| C 망각 | 선택 3 | 귀가 후 사진에서 사요 소멸(사진 텍스처 스왑 — 인트로부터 쓴 사진 프랍의 최종 변주) → 「나는 기억할게」 |
| TRUE | 선택 4 | ACT 28 보스 3페이즈 → 29~32 탈출 런(아래) → 33~35 에필로그 |

**TRUE 탈출 런(ACT 29~32)**: 무너지는 마을을 사요와 함께 달리는 강제 질주 시퀀스. 프롤로그와 반대 구도(이번엔 미오가 앞). 조작은 이동만, 낙하물 회피. **두 갈래 길에서 피안화 길을 스스로 고르는 것**이 마지막 인터랙트(금기 二의 의미 반전 — 잘못 고르면(검은 안개) 사요가 손을 당겨 정정해준다, 실패 없음). ACT 30~31 대화는 시퀀서, ACT 32는 "뒤돌아보지 않기"(6.1) 후 빛으로.

### 7.4 2부 성불 판정 3단

| 상태 | 조건 | 결과 |
|---|---|---|
| **성불** | 단서 수집 + 해결 행동 + 반납 | regret=true, 조력자 개방(3.4), 종 −1성 |
| **방치 반납** | 단서 없이 물건만 반납 | seal은 인정, 원혼이 흉측화해 해당 구역이 잠식 지대로(재진입 위험). regret=false |
| **강제 소멸** | 원혼에게 봉인패 사용 | 즉시 seal 인정 + 구역 안전. **banished+1 = 진엔딩 영구 차단.** 가장 편한 길이 가장 나쁜 길 |

### 7.5 회차 요소

- 클리어 후: 엔딩 크레딧(생전 사진 8매 — 성불 여부에 따라 매수 변동) → 암전 → **4번째 금기 스팅어**(전 엔딩 공통) → 「……미오야.」
- 2회차: 돌비석에서 [침묵한다] 선택지 출현(잠식 0 시작), 히간누시 UI 문구가 미묘하게 초조해짐, 명부·일기 위치 유지(수집 이어하기)

---

## 8. 컷신 연출 — 결정: 전부 인게임 실시간

### 8.1 근거

| | 프리렌더 영상 | 인게임 실시간 |
|---|---|---|
| 용량 | 1080p 1분 ≈ 30~80MB — **웹 배포 즉사** | 0 (에셋 재사용) |
| 룩 | 렌더 파이프라인 이질감 | 본편과 동일 (밤·안개·포스트FX 공유) |
| 수정 | 재렌더·재인코딩 | 파라미터 수정 즉시 |
| 리스크 | 외부 툴 의존 | 시퀀서 구현 1회 (8.3) |

유일한 예외 후보였던 프롤로그도 1인칭 인터랙티브(각색 8)로 해결. **동영상 파일은 만들지 않는다.**

### 8.2 연출 3등급 — "조작을 최대한 빼앗지 않는다"

| 등급 | 정의 | 예 |
|---|---|---|
| **A 인터랙티브** | 조작 유지, 세계가 연기한다 | ACT 4 생활 흔적, 방송, UI 변조, 봉납 반응, 원혼 성불 절차 대부분 |
| **B 시퀀서** | 조작 회수, 카메라·배우 타임라인. 스킵 가능(홀드) | 재회(19), 사요 회상(20), 최후 대화(30~31), 엔딩 |
| **C 오버레이** | 화면 위 텍스트·사진·포스트FX (3D 없음) | 기억 파편 플래시, 신문 기사(21), 크레딧 |

원칙: B는 최소화(전체 35 ACT 중 B 순수 컷신은 ~12분 분량). 대사 이벤트는 걷는 중 자막+보이스로 처리(워킹 시뮬 문법).

### 8.3 시퀀서 요구 사양 (신규 시스템, 1회 구현)

```
src/story/sequencer.ts   트랙: 카메라 스플라인 / 배우(위치+애니 클립) / 대사 자막 /
                         사운드 / 포스트FX / 프랍 상태. JSON 타임라인, 스킵·일시정지
src/story/dialogue.ts    자막 UI(화자 색·글자 속도·선택지), 보이스 훅
src/story/flags.ts       7.1 플래그 저장/로드 (localStorage)
src/story/quests.ts      4.2 목표 상태기계 + UI 문구 (히간누시체/사요체)
```

### 8.4 컷신 전량 표

| ACT | 장면 | 등급 | 비고 |
|---|---|---|---|
| 1 프롤로그 | 빗속 도주 | **A(1인칭)** | 강제 질주+시선만. 사요는 손·뒷모습만(모델 부담↓). 「보지 마!」에서 시선 강제 |
| 2 버스 | 종점 | B 1분 + A | 버스 실내 정적 모델. 하차 후 뒤돌기 = A |
| 3 비석 | 금기·목소리 | A | 조사 인터랙트 + 강제 위반 |
| 5 신사 | 석판·퀘스트 개시 | A | UI 등장 자체가 연출 |
| 6~15 각 공물 | 획득·기억 파편 | A + C | 파편은 C(적색만 남긴 탈색+그레인) 1인칭 플래시 |
| 7·9·11·12 봉납 | 「하나…둘…」 | A | 카메라 살짝 지하 방향 틸트, 조작 유지 |
| 14 긴 기억 | 방울을 집은 아이 | **A(1인칭 과거)** 4분 | 어린 미오 시점 조작(걷기만). 할머니 얼굴 갈라짐이 이 게임 최고 호러 컷 |
| 16 진상 | 비네트 3개 | A | 8.5 |
| 17 원혼들의 만류 | 길목 배치 | A | 각 원혼이 길가에 서서 대사. 걸으며 듣는다 |
| 18~19 지하·재회 | 히간누시·사요 | B 3분 | 첫 풀 시퀀서 씬 |
| 20 마지막 밤 | 사요의 결단 | B 4분 | 과거 신사. 16-13에서 이어짐 |
| 21 기사 | 신문·거짓말 | C | 오버레이 타이포그래피 |
| 22~23 배신 | UI 붕괴 | A | 4.4. 조작 유지 상태에서 UI가 무너지는 게 핵심 |
| 25-x 성불 6종 | 각 해결 | A + B 30초 | 절차는 A, 소멸 순간만 B |
| 27 선택 | 4지선다 | A | 선택 UI 자체가 무대 |
| 28 백귀야행 | 보스 | A(플레이) + B 2분 | 원혼 집결은 B, 페이즈는 플레이 |
| 29~32 탈출 | 질주·이별 | A + B 5분 | 30~31 대화만 B |
| 33~35 에필로그 | 새벽·1년 후·돌무덤 | B 5분 | 학교 앞 씬은 교복 NPC 2~3 실루엣으로 최소화 |
| 크레딧 후 | 4번째 금기 | B 40초 | 비석 클로즈업 + 새기는 소리 |

### 8.5 ACT 16 구성안 (각색 9)

| 비네트 | 시점 | 플레이 | 길이 |
|---|---|---|---|
| 16-2 방울 | 어린 미오 1인칭 | 축제 인파(실루엣) 사이를 걸어 할머니를 따라간다 — 유일하게 밝고 따뜻한 색의 구간. 방울을 집는 순간까지 | 6분 |
| 16-4 폐교 | 사요 3인칭 | 미오를 찾아 참사 중의 학교를 가로지른다(스물넷 세기 장면을 목격). 전투 없음, 회피만 | 6분 |
| 16-13 신사 | 사요 3인칭 | 미오를 벽장에 숨기고(할머니의 집), 신사까지 데려가는 이동 — 프롤로그의 반대편 시점 | 5분 |

사이사이 = C등급 기록 몽타주(우물 9명·여관 화재 14명·회관 16명 등은 촌장 문서+음향+실루엣 스틸로). 87명 집계표(16-11)는 지하 문서 열람 UI로.

---

## 9. 에셋 제작 계획

### 9.1 캐릭터 (Tripo `scripts/tripo/*` 파이프라인)

| 캐릭터 | 방식 | 리깅 | 우선순위 |
|---|---|---|---|
| 미오 (교복·가방) | 기존 character.glb 재의상 or 재생성 → 기존 9클립 리타게팅 | ✔ | **S1** |
| 사요 (10년 전 교복) | Tripo 신규 | ✔ (동행·조작·컷신) | S4 |
| 어린 미오 (6세) | Tripo 신규 — 1인칭 채택 시 팔·환영 NPC용 최소 | ✔ 간이 | S3 |
| 히간누시 인간형 | Tripo (흰 기모노, 얼굴=피안화 — 얼굴만 별도 메시) | ✔ | S4 |
| 히간누시 거대형 | 상반신 정적 + 팔 셰이더 군체 | 본 소수 | S6 |
| 로쿠로쿠비 | 머리+몸통 Tripo 정적, **목=코드 튜브 스플라인**(까마귀 교훈: 리깅 프리셋으로 안 되는 형태는 코드로) | ✖ | S1 |
| 유리 (얼굴 없음/복원) | 기존 yokai-noppera.glb 재의상 + 얼굴 텍스처 2벌 | ✔ | S2 |
| 우물의 여자 | Tripo (젖은 기모노·긴 손가락) | ✔ | S2 |
| 아이 귀신 (베이스 1 + 배리에이션 2) | Tripo | ✔ 1회 | S3 |
| 하루·묘지 아버지·여관 주인·아내·촌장·할머니 | Tripo — 여관 주인·촌장만 풀 리깅, 나머지 간이(부유·페이드 위주) | 부분 | S3~S5 |
| 주민 군중 | **모델 0 — 안개 실루엣 인스턴싱 셰이더** | ✖ | S4 |
| 기존 유지 | 팔척·키츠네·도로타보·까마귀·지장 | — | — |

리깅 ~9체 × 65크레딧 ≈ 585 + 정적 ~10체. Tripo 예산 **약 900~1,100크레딧**.

### 9.2 건축·구역

| 대상 | 방식 | 비고 |
|---|---|---|
| 폐교 | **모듈러 코드**(복도·교실 그리드는 코드가 유리) + Tripo 소품 | 최대 신규 공정. 창밖으로 운동장·달이 보이는 개구부 필수 |
| 폐여관 | 모듈러 코드 + **재질·프랍 2벌**(불탄/온전) | 이중 상태가 본체 |
| 촌장 저택 | 기존 폐가(houseMaterials 파이프라인) 확장 | 지하 기록실 포함 |
| 우물+샤프트 | 코드(원통) + Tripo 우물가 | 수위 애니 |
| 사당 | Tripo 정적 1 | 실내 대들보 구조만 코드 보강 |
| 신사 지하·검은 문 | 계단 코드 + **검은 문 Tripo 랜드마크** | 문이 이 게임의 최종 이미지 |
| 버스+정류장 | Tripo 정적 | 인트로+엔딩 A 재사용 |
| 돌비석 | Tripo or 스컬프 + 각인 디컬 | 금기 텍스트는 텍스처 아닌 디컬(4번째 금기 추가용) |

### 9.3 프랍 (공물·키 아이템)

| 프랍 | 비고 |
|---|---|
| 공물 7: 방울·머리빗·동전 3·게다·손거울·봉인패+제문·(사요의 방울 장식) | 인벤 아이콘 + 월드 모델 + 받침대 위 모델. 방울 장식은 에필로그 클로즈업 견딜 품질 |
| 받침대 7 + 중앙 석판 | 봉납/공백 2상태 |
| 가족사진 | 텍스처 3벌(훼손/소멸/복원) — 인트로부터 에필로그까지 관통하는 프랍 |
| 기록물 26종 | 공용 모델 4종(책·낱장·수첩·위패) + 열람 UI 텍스트 |
| 학교 가구 세트·여관 세트·저택 세트 | 모듈러 |
| 명부 수첩·등불(백귀야행 87개 인스턴싱) | 등불에 이름 텍스처 |

### 9.4 VFX·셰이더·기술 과제

| 항목 | 난도 | 비고 |
|---|---|---|
| **거울 이중 상태 렌더** | ★★★ 최대 기술 리스크 | 1안: 스텐실 포털. 2안(권장): 거울 = 별도 RT 카메라로 "온전 상태 프랍 세트"만 레이어 렌더. 여관 실내 한정이라 부하 통제 가능. **S3에서 프로토타입 먼저** |
| 피안화 성장 애니 | ★ | 인스턴스 스케일 커브 |
| 유인 길(꽃이 고개 돌림) | ★ | 버텍스 셰이더 |
| 붉은 달·하늘 전환 | ★ | 스카이 텍스처 2벌 크로스페이드 |
| 잠식 비네트(가장자리 피안화) | ★ | 오버레이 텍스처 + 마스크 |
| 안개 실루엣 군중 | ★★ | 빌보드+노이즈 소산 |
| 꽃잎 디졸브(성불) | ★★ | 파티클 + 디졸브 맵 |
| 화재(기억 속 여관) | ★★ | 기존 등불 불꽃 확장 |
| UI 글리치 | ★ | DOM/CSS (4.4) |
| 로쿠로쿠비 목 | ★★ | TubeGeometry 스플라인 실시간 |
| **실내 다층 내비게이션** | ★★ | 현 navgrid 는 지형 자동 샘플링(야외 전용) — 폐교·여관 2층, 저택은 계단이 있어 못 쓴다. 범용 실내 내비 대신 **손으로 깐 웨이포인트 그래프**로 간다(실내는 전부 복도형). 유리는 "안 볼 때만 이동" 규칙이라 순간 스텝으로 회피, 저택 주민은 저작 순찰만 |
| 세이브/로드 | ★★ | localStorage JSON — S0 에서 구현 (`story/flags.ts`) |

### 9.5 오디오 (기존 45개 + 신규 ~35개)

- 신규 그룹: 종(대·소)·마을 방송(라디오 필터)·학교(형광등·책상·피아노 단음)·우물 반향·화재·군중 웅성·손톱로 새기는 소리·꽃잎 소산·버스
- **대사 방침**: 자막 기본. 보이스는 3단 — ① 방송·출석·녹음테이프 등 "기계를 거친 목소리"는 TTS+필터로 제작 가능(디제제틱하게 변명됨, 즉시 착수 가능) ② 히간누시·사요 핵심 라인은 후일 녹음 검토 ③ 비언어 발성(숨·웃음·허밍)은 프리 샘플. Freesound 파이프라인(`audio:fetch`) 재사용

### 9.6 UI

퀘스트 표시(2서체: 시스템체/손글씨체 — 웹폰트 2종)·글리치 상태·명부 화면·기록물 열람·선택지(긁힌 4번 연출)·자막·종 카운터·참배 저장 표시. HUD 최소 원칙 유지.

---

## 10. 로드맵 + 스코프 가드

| Phase | 내용 | 완료 판정 |
|---|---|---|
| **S0 기반** ← **1차 구현 (2026-08-20)** | 컨펌 반영 → sequencer·dialogue·quests·flags·**세이브** 구현. 미오 재의상 | 목표 표시→저장→로드 루프 |
| **S1 수직 슬라이스** | 프롤로그~ACT 7 통짜: 버스·흙길·비석·손전등→초칭·사당·로쿠로쿠비·봉납 1·월드 반응 | **처음 30분을 남에게 시켜본다** |
| S2 | 폐교+우물 (ACT 8~11), 유리·우물의 여자, 방송 충돌 | 봉납 3 |
| S3 | 묘지+여관+저택 (12~15), **거울 렌더 프로토타입 선행**, 아이들·거울 하우스·환영 스텔스 | 봉납 5+봉인패 |
| S4 | ACT 16 비네트 3 + 지하·재회·배신 (17~23) | 1부 완결 |
| S5 | 복원장 (24~26): 2부 월드 상태·성불 6종·명부·잠식 지대·종 | C 엔딩 도달 가능 |
| S6 | 최종장 (27~35): 선택·보스·탈출 런·엔딩 4종·크레딧·회차 | 전 엔딩 |

**예상 플레이타임**: 1부 ~2h + 진상 ~40m + 2부 ~1.5h + 최종장 ~40m ≈ **4.5~5h** (v0.12의 ~20배 스코프).

**스코프 가드 3선**:
1. S1이 재미없으면 S2로 안 넘어간다 — 루프(탐색→획득→추격→운반→봉납)는 S1에서 전부 검증된다
2. 각 Phase는 그 자체로 "끝이 있는 빌드"(S2까지만 만들어도 봉납 3 데모가 성립)
3. 거울 렌더(최대 기술 리스크)는 S3 첫 주에 스파이크 — 실패 시 폴백: 거울은 고정 시점 RT 3장(움직이는 거울 세계 포기, 정지 화면으로 연출)

---

## 11. 위험 요소

| 위험 | 대응 |
|---|---|
| 스코프 (솔로 4.5h 게임) | Phase 게이트(10절). 최악 컷라인: 2부 성불을 3종으로 축소(유리·우물·촌장), 엔딩 C/TRUE 조건 완화 |
| 거울 렌더 | S3 스파이크 + 폴백 |
| 아역(어린 미오·아이들) 모델 불쾌한 골짜기 | Tripo 스타일라이즈 프롬프트 + 어두운 조명/실루엣 위주. 1인칭 각색(8)이 노출 자체를 줄임 |
| 대사량 (스토리보드 ~300라인) | 자막 기본(9.5). 회화 씬은 걷는 중 재생으로 체감 길이 절감 |
| 성능 (실내 6동 추가) | 실내는 존 로드(기존 `house.contains` 패턴 확장), 잠식 지대 파티클 LOD |
| UI 반전이 "불친절"로 읽힐 위험 | 1부 UI는 기능적으로 완벽하게 정직(길 안내·마커 정확) — 배신은 문구의 의도이지 사용성이 아니다 |

## S1 구현 노트 — 스토리 맵을 **새로 설계** (2026-08-20, 2차)

> 1차에서 기존 맵(`world/village`)의 지형에 부지를 패드로 덧붙였다가 되돌렸다.
> 사용자 지시: **기존 지형·맵·오브젝트는 그대로 두고, 원래 만들었던 것처럼 새로 재설계한다.**

**왜 덧붙이기가 실패했나** — 기존 맵의 지형 함수는 "다섯 공물·10~14분 루프"에 맞춰 잡혀 있다.
거기에 스토리 부지를 얹으니 산자락 한복판에 건물이 섰다(실측: 버스 종점 자리 **경사 67°**, y 10.7 m).
길과 부지의 높이가 어긋나 진입로가 절벽이 됐다. **부지는 나중에 얹는 게 아니라 지형 설계에 처음부터 들어가야 한다.**

**새 패키지** `src/world/higasato/` (기존 `world/village/` 는 `git checkout` 으로 원상복구, 무손상)

| 설계 원칙 | 내용 |
|---|---|
| ① 사이트 우선 | `SITES` 가 한 곳의 진실 — 지형 선반·논 제외·식재 제외·구조물 배치가 전부 여기서 파생 |
| ② 선반 높이는 길에서 | 부지 y = **그 부지로 이어지는 길의 원지형 높이**. 길과 부지가 같은 높이라 진입로가 절벽이 되지 않는다 |
| ③ 산이 부지를 피한다 | rim/hill 함수의 시작 거리를 부지 바깥으로 잡는다. 지형과 싸우지 않는다 |
| ④ 세 단 구성 | 하단(논·들 z 40~) · 중단(마을 z −10~40) · 상단(신사 언덕 z −10~) |

- 크기 200×200. 참배로(척추) 1 + 갈래길 4, **길마다 목적지 하나**(§2.1 그대로):
  ①참배로→신사(도중 우물) ②논두렁길→할머니의 집 ③뒷산길→묘지→사당 ④돌계단→저택 ⑤대숲길→여관→폐교
- 논은 서남쪽 한 덩어리(배미 7). 배미 생성이 **평탄화되는 길 전부 + 부지 전부**를 피한다(기존은 참배로만 피했다)
- 논두렁길 좌표 = 흙둑 격자 중심선. 어긋나면 길이 논에 빠진다
- 기존 모듈(논·토리이·삼나무·대숲·묘지·집·광장·석등·신사·피안화·안개)은 **파일을 건드리지 않고 재사용** — `asGround` 어댑터 1개로 넘긴다
- 식재가 건물 터를 비우는 방법: 삼나무·대숲은 `pathDist < clear` 로 공터를 만든다 → **부지 안에서 그 거리를 0 으로 돌려주는 프록시**를 넘긴다(모듈 무수정)

**검증(수치)**: 부지 10곳 전부 평탄(최대 경사 ≈ 0.0) — 종점 y 0.1(덧붙이기 때 10.7·67°) · 사당 4.3(산중턱 선반) · 저택 10.7 · 신사 9.4.
**나브그리드 BFS(스폰 기준)**: 스토리 부지 11곳 + 배미 7/7 **전부 도달**. 통행 셀 12,040. 60 fps.
논 배미 7 · 벼 8,196 · 토리이 30 · 삼나무 595 · 대나무 765.

**씬 전환**: 기본 `?scene=higasato`(스토리 맵) · `?scene=village`(기존 공포 맵 v0.12 그대로) · `sandbox`/`playground`(v0.8 초원).

### 마을 골목 · 민가 (2026-08-20) `higasato/minka.ts`

새 맵의 첫 판에서 **마을 한복판(논과 광장 사이)이 빈 들판**이었다. 골목망과 민가로 채웠다.

**골목 6줄** (`ground.ts` `LANES`) — 갈래길과 **따로** 둔다. `ROUTES` 는 요괴 순찰·길 선택의 단위(다섯 갈래)라
게임 로직이 참조하는데, 골목을 거기 섞으면 "길이 열한 개"가 되어 순찰 배분이 무너진다. 지형 평탄화·발밑
소리·식재 제외에는 둘 다 필요하므로 `ALL_PATHS` 로 합쳐 쓴다.
본거리 · 우물 골목 · 뒷골목 · **막다른 골목**(도망치다 잘못 들면 끝) · 서쪽 골목 · 남쪽 골목.

**민가**는 폐가(`village/house.ts`)와 역할이 다르다 — 폐가는 **들어가는 집** 한 채(실내·벽장·장지문 실루엣),
민가는 **지나가는 집**이다. 실내를 만들지 않는 대신 바깥에 전부를 건다:
石場建て(주춧돌 위에 뜬 마루) · 下見板張り(아래 널판 / 위 흙벽, 그 경계선이 집의 허리) · 縁側(툇마루 + 장지문,
몇 칸은 **문이 빠져 검은 구멍**) · 깊은 처마 0.8~1.0 m · 茅葺(우진각·두껍다) / 板葺石置(맞배·누름돌) 두 지붕형.
재질은 폐가의 **절차적 PBR 세트를 그대로 재사용**(`makeHouseMaterials` 는 캐시) — 알베도만이 아니라 노멀·ARM 이
있어야 점광원 하나가 요철을 스칠 때 실물로 읽힌다. 골목 소품: 판담·장작더미·물확, 처마 등불 5개.

**배치 규칙**(전부 실측으로 잡은 것): 좌우를 번갈아 놓으면 같은 쪽 간격이 두 배로 벌어져 마을이 아니라
띄엄띄엄한 농가가 된다 → **좌우를 따로 훑는다**. 거절된 자리가 다음 집을 밀어내면 부지를 스칠 때마다 골목 한
구간이 통째로 비므로 → **거절 시 간격을 넘기지 않는다**. 길과의 거리는 **회전된 사각형** 기준 —
바운딩 박스 최대변으로 재면 비켜 서 있는 집까지 거절해 마을이 절반이 된다. 자기 골목은 이미 물러나 앉았으니
건너뛰고 **다른 길 전부**를 검사한다(안 하면 교차점에서 옆 골목으로 집이 튀어나와 길을 막는다).

**성능**: 재질별 전량 병합 — 집 10채 + 소품이 **드로우콜 7**. 그림자는 받기만 하고 만들지 않는다.
**검증**: 골목 중심선 146/146 통행 · 집 10/10 콜라이더 차단 · 스토리 부지 10곳·배미 7/7 도달 · 60 fps.

### ⚠️ 나브그리드 버그 수정 — 건물이 요괴를 막지 못하고 있었다

민가를 넣고 도달성을 재보다 발견했다. **Rapier 는 새로 추가한 콜라이더를 다음 `step()` 에서야 브로드페이즈에
넣는다.** 나브그리드는 그 전에 구워지므로 `intersectionsWithShape` 가 건물을 하나도 못 봤다 —
실측: 민가 12채가 막은 셀 **0개**, 할머니의 집·신사 본전도 **통행 가능**으로 기록. 요괴가 집과 신사를 뚫고
다니는 상태였고, **기존 맵에서도 마찬가지**였을 것이다(구 맵의 통행 셀 감소는 경사 판정 몫이었다).
→ 굽기 직전 `physics.step(1/240)` 한 번. 수정 후 집 10/10·신사·폐가 전부 차단, 길·부지 도달성은 유지.

## ACT 1 재작성 — 「보여주기」에서 「플레이」로 (2026-08-20)

> 방침 확정(사용자): **ACT 를 처음부터 하나씩 제대로 구현한다. Phase 를 늘리더라도 디테일하게.**
> 그래서 ACT 1 을 45초짜리 카메라 스플라인에서 **플레이 가능한 1인칭 도주**로 다시 만들었다.

왜 다시 만들었나: 이 ACT 의 문장은 *누군가의 손을 잡고 진흙길을 달리고 있다* 이다.
카메라가 지나가면 그건 관람이고, 플레이어가 달려야 그 문장이 성립한다.

**`story/firstPerson.ts`** — 1인칭 리그
- 눈높이 **1.05 m**(여섯 살). 어른 1.6 m 보다 55 cm 아래에서 세상이 보이는 것이 프롤로그의 공포다
- **잡힌 손**: 미오의 팔이 앞으로 뻗고 그 손목을 다른 손이 감아쥔다. 사요는 끝까지 화면에 없다
  (스토리보드: "손의 주인은 보이지 않는다"). 손가락이 손목을 **가로질러 감기는** 형태가 핵심
- 달리는 몸: 상하 2배음 + 좌우 1배음 + 롤. 손은 리듬에 맞춰 앞으로 당겨진다
- **뒤를 못 보게 한다**: 정면에서 66° 를 넘기면 마우스가 무거워지고 135° 에서 멈춘다.
  금기 三을 "규칙"이 아니라 **손끝의 저항**으로 먼저 가르친다

**`story/pursuers.ts`** — 보이지 않는 추격자
- 모델을 만들지 않는다. 돌아보면 안 되는 규칙 때문에 곁눈으로만 보게 되고, 안 보이는 만큼 수가 많아 보인다.
  **횃불 빛무리 5개**(그림자 없음, 라이트는 절반만 — 다섯 개를 더 켜면 밤 셰이더가 통째로 재컴파일된다)
- 압박 규칙: 플레이어보다 **조금 느리다**. 달리면 멀어지고 멈추면 붙는다. 잡히지는 않는다(프롤로그니까) —
  대신 가까울수록 화면 가장자리가 붉게 조여든다(`.dread`)

**`story/act1.ts`** — 진행은 시간이 아니라 **거리**로 센다(느리게 달려도 대사를 놓치지 않고, 빨리 달려도 밀리지 않는다).
구간은 z 78 → z 16 의 **62 m**(2차에서 확정, 아래 「ACT 1 2차」 참고). 표의 거리는 62 m 기준이고
지형이 바뀌면 **비율로 환산**된다.
| 거리 | 사건 |
|---|---|
| 0 m | 암전 위 숨소리·빗소리 → 시야가 열린다 · 「누군가의 손이 내 손을 잡고 있다」 |
| 5 m | **번개 ①** (아주 멀다 — 천둥은 5 초 뒤) |
| 10 / 18 / 26 m | 뒤쪽 「미오!」 「거기 서!」 「아이를 잡아!」 (합성 외침 + 자막, 음량은 추격 거리를 따른다) |
| 24 m | **번개 ②** — 붉은 피안화 길이 처음 통째로 드러난다 |
| 35 m | **미오가 스스로 고개를 돌리기 시작한다**(흐느낌) → 「보지 마!」 → 시선이 강제로 꺾인다 |
| 42 m | **진흙에 넘어진다** → 「일어나!」 · 추격자가 11 m 까지 붙는다 |
| 46 m | 「비에 젖은 붉은 피안화가 길을 따라 끝없이 이어진다」 |
| 51 m | **번개 ③** (바로 위) — 도리이가 실루엣으로 선다 |
| 54 m | 「멀리 신사의 도리이가 보인다」 |
| 62 m | 모든 소리가 **끊긴다** → **손이 눈앞에서 빠진다** → **댕—** → 암전 → 「언니……?」 「미오야.」 (침묵) 「피안화가 피어 있는 길은…」 |

- 플레이어가 스스로 끝까지 돌아봐도 같은 「보지 마!」가 발동한다
- ACT 1 동안 HUD 전부 숨김(목표도 소금도 없던 밤이다), 발밑은 자갈이 아니라 **진흙·빗물**로 강제
- 뒷걸음질(S)만 막는다 — 추격자에게 걸어가는 선택지는 주지 않는다

**`sfx.shout()`** 추가 — 보이스 샘플이 없으므로 합성. 말소리로 들리게 하는 건 음높이가 아니라
**포먼트**(F1 700 / F2 1200)다. 톱니 성대음 + 밴드패스 둘 + 피치 상승-하강, 멀리서 나는 소리라 로우패스로 고역을 깎는다.
`near` 로 사요의 바로 옆 외침(소녀 음역·고역 살림)을 구분한다.

**요괴는 프롤로그에 나오지 않는다** (사용자 리포트: "act1에 왜 귀신이 바로 나와?")
— ACT 1 은 **10년 전 밤**이다. 팔척귀신도 여우도 도로타보도 아직 없고, 그날 미오를 쫓은 건
마을 사람들이다. 그런데 요괴는 씬 생성과 함께 순찰을 돌고 있어 프롤로그 도중에 보였다.
→ `setYokaiActive()` 스위치: 프롤로그 동안 **갱신·표시·마츠리바야시를 전부 끄고**, ACT 2 가 끝나
현재로 돌아온 순간 켠다. `?skip=intro` 면 처음부터 켠다. 연출형 요괴(놋페라보·초칭오바케)도 같은 게이트 안이다.

**검증**: 시작 → 4.5초 뒤 W → 3.60 m/s 로 62 m 주파 → 거리 비트 정상 발화 → 「보지 마!」 →
종·암전·금기 → 타이틀 → ACT 2 → 조작 인계까지 한 번에 확인. 프롤로그 중 요괴 표시 `false`,
인계 후 `true`. 60 fps.
**잡은 버그**: `moveAxis` 는 전방이 **+y**(W)인데 부호를 반대로 봐서 전진을 막고 있었다 ·
정류장 표지가 뒷면을 보여 글자가 좌우 반전 · 시작부터 잠식이 0.5 로 떠 있어 근접의 의미가 없었다 ·
**프롤로그에 요괴가 돌아다녔다**(위) · **손이 잘렸다** — 원인 셋: ① 좌표를 손으로 박아 둬서 세로 창에서
가로 화각이 좁아지면 프레임 밖으로 나갔다 → **화면비에서 역산** ② 팔 뿌리(소매)가 화면 안에 있어
원기둥 **끝면이 카메라를 정면으로 보고 파란 원반**이 됐다 → 뿌리를 화면 아래로 빼고 pitch(+)로 손끝만
들어 올린다 ③ 초칭이 꺼진 밤이라 팔에 닿는 빛이 없어 새까맸다 → 아주 옅은 자발광(젖어 달빛을 받는 살)

### ACT 1 2차 — 「달릴 수 있다」에서 「그 밤에 있다」로 (2026-08-20)

1차는 *플레이할 수 있게* 만들었다. 2차는 그 위에 **스토리보드가 적어 놓은 것들을 실제로 화면·소리에 올렸다.**
출발점은 한 문장이었다: 스토리보드 ACT 1 에는 그림이 두 개 있는데(*끝없이 이어지는 붉은 피안화 길* ·
*멀리 보이는 신사의 도리이*) **둘 다 자막으로만 존재했다.** 왜 없었는지 파 보니 원인이 세 겹이었다.

**① 꽃이 실제로 없었다** `world/village/higanbana.ts`
`Higanbana` 는 센본토리이 터널 구간을 비우는데, 그 범위가 **구 마을 좌표(s 44~101)로 박혀** 있었다.
히가사토의 터널은 s 102~143 이므로, 하필 **ACT 1 이 달리는 구간이 통째로 제외**되고 있었다.
→ 터널 범위·군락 위치·**붉은 길 구간**을 옵션으로 빼고 `higasato/index.ts` 가 `toriiS0/toriiS1` 로 넘긴다.
같은 숫자를 두 곳에 적어 두면 또 어긋나므로 토리이 시작·끝 호길이를 **공개 필드 하나**로 만들었다.
붉은 길은 도리이에 가까워질수록 덤불 간격이 좁아지고 길가에 붙는다(균일하게 깔면 화단이지 길이 아니다).
군락(안전지대)도 이 맵의 `flower` 부지 위로 옮겼다 — 구 마을 좌표 그대로면 논 한복판이었다.

**② 밤이 너무 어두웠다** `world/timeOfDay.ts`
`rainNight` 은 발밑조차 안 보이는 **순검은 화면**이었다. 범인은 밝기가 아니라 **대비**다:
`BrightnessContrastEffect` 는 `(c−0.5)(1+k)+0.5` 이므로 `c < 0.5 − 0.5/(1+k)` 가 통째로 0 이 된다.
k 0.16 → **6.9 % 이하가 전부 순검정**이었고 비 오는 밤의 지면이 딱 그 아래였다.
실측: 달빛을 0.85 → 30 으로 올려도 화면이 그대로 검었다.
→ 대비 0.16 → 0.05(검정점 2.4 %), 노출 0.60 → 0.74, 반구광 0.30 → 0.52, 환경광 0.30 → 0.40,
안개 0.030 → 0.024. 어둠은 이제 **밝기가 아니라 안개**가 만든다 — 발밑은 보이고 20 m 앞은 안 보인다.

**③ 앞을 열어 줄 사건이 없었다** `world/lightning.ts` (신규)
번개는 그 두 그림을 **1/10 초 동안 보여주고 다시 지운다.** 계속 보이는 것보다 오래 남는다.
- **새 광원을 만들지 않는다** — 라이트를 더 켜면 밤 셰이더가 통째로 재컴파일된다(횃불에서 이미 겪었다).
  이미 있는 달빛·반구광 세기를 순간적으로 올렸다 되돌린다. 복원은 기억해 둔 값이 아니라 **더한 만큼을 뺀다**
  (번쩍이는 중에 시간대가 바뀌면 옛 값을 되돌려 조명이 어긋난다)
- 파형은 **되돌이 방전 3연**(강·중·약). 한 번 번쩍이면 "전등이 켜졌다"이지 번개가 아니다
- **밤의 번개는 밝기가 아니라 대비다.** 반구광을 크게 올렸더니 사방이 고르게 떠서 화면이 우유가 됐다
  → 방향광에 몫을 몰아준다(+9 / +1.2). 그래야 삼나무와 도리이가 **실루엣**으로 선다
- 빛나는 건 지면이 아니라 **공기**다 — 안개색을 청백으로 34 %까지 끈다
- `prefers-reduced-motion` 이면 진폭을 40 %로 줄이고 되돌이 방전을 뺀다(광과민 발작 위험)
- `sfx.thunder(gain, dist)`: 크랙(고역, 가까울 때만) + 럼블(로우패스가 닫히며 굴러가는 꼬리).
  **시차**로 거리를 만든다 — dist 1 ≈ 2 km ≈ 6 초 뒤

**들리는 것** `audio/sfx.ts`
스토리보드 ACT 1 의 **첫 줄이 "거친 숨소리"** 인데 그게 자막으로만 있었다.
- `breath(level, dt, {rate})` — 여섯 살은 성도가 짧아 공명이 높다(rate 1.55). 숨은 셋이 겹친다:
  달리는 만큼 + 온 거리만큼 지쳐서 + 뒤가 붙은 만큼. 암전 구간에도 돈다(이미 한참 달려온 뒤니까)
- `sob()` — 울음은 멜로디가 아니라 **끊긴 들숨**이다. 점점 짧아지는 들숨 2~3 번 + 목이 잠긴 훌쩍임.
  "울며 뒤를 돌아보려 한다"를 자막 대신 소리로
- `heartbeat(level, dt)` — 지쳐서 뛰는 심장과 **무서워서** 뛰는 심장은 다르다. 이건 추격 근접도만 본다
- `setRain(false, 0)` — 「갑자기 모든 소리가 끊긴다」를 1.5 초에 걸쳐 줄이면 그건 *비가 그친 것*이다
- 뒤쪽 외침의 음량이 `pursuers.voice`(거리)를 따른다

**손이 하는 일** `story/firstPerson.ts` — 프랍에서 **장치**로
- **끌림**(`setTug`): 처지면 팔이 앞으로 늘어나고 손목이 꺾이며 몸이 실제로 끌려간다(0.9 m/s).
  1.7 초를 넘기면 사요가 재촉한다(「뛰어!」「미오야, 빨리!」「놓지 마!」— 최대 3 회).
  멈추는 걸 금지하는 게 아니라 **멈추면 끌리게** 해서 스스로 달리게 만든다
- **넘어짐**(`stumble`): 진흙에 미끄러진다. 눈높이가 꺼지고(0.62 m) 화면이 기울고 리듬이 사라진다.
  이때 **손은 눈이 내려간 만큼 위로** 간다 — 사요는 그대로 서 있으니 매달린 그림이 된다.
  잡히지는 않지만 추격자가 11 m 까지 붙는다. 실패가 아니라 **거리가 좁혀지는 사건**이다
- **손을 놓는다**(`releaseHand`): 손가락이 먼저 풀리고(40 %) 손목에서 손끝 쪽으로 미끄러져 빠진 뒤
  팔이 떨어진다. 암전 뒤에 조용히 감추면 「언니……?」가 이유를 잃는다 — 눈앞에서 놓쳐야 한다
- 자발광 0.10 → 0.05: 프리셋이 밝아진 뒤로는 팔에 실제 빛이 닿는다(그대로 두니 분필처럼 하얬다)

**끝맺음**
정적 → **손이 빠진다** → 댕— → 암전 → 금기. 빗소리 즉시 컷 · 횃불 소등(`pursuers.snuff`) ·
숨·심장 정지 · 예약된 천둥 취소. 그리고 **남은 자막이 끝날 때까지 기다린다** — 거리로 짠 비트는
플레이어가 느리면 뒤로 밀리므로, 마지막 줄이 종소리에 잘리는 일을 도착선에서 한 번에 막는다.

**⚠️ 잡은 버그 넷** (전부 인게임 실측)
| 증상 | 원인 |
|---|---|
| 62 m 를 달리면 길에서 **8 m 벗어나** 논밭을 가로질렀다 | 이동 기준이 **시선**이었다. 참배로가 굽는 만큼 계속 어긋난다 → ACT 1 은 `forwardYaw`(길)를 기준으로 달린다. 손을 잡힌 아이는 방향을 정하지 않으므로 연출에도 맞다 |
| 추격자가 계속 붙어 있고 속도가 3.6 → **2.7** 로 떨어졌다 | `nearestRoad` 가 **재사용 객체**를 돌려주는데 `heightAt` 이 같은 버퍼를 쓴다. 사이에 낀 `pursuers.update`(횃불마다 heightAt)가 값을 *횃불 근처*로 바꿔 놓아, 길 보정이 플레이어를 **뒤로** 끌었다 → 필요한 값만 즉시 복사 |
| 「멀리 신사의 도리이가 보인다」인데 **도리이 기둥이 화면 절반**을 채웠다 | 끝점이 경내 코앞이었다 → 시작을 z 78 로 내리고 끝을 z 16 로. 62 m 는 대사 여섯 줄이 밀리지 않는 최소 길이다(3.6 m/s 면 한 줄이 9 m 를 먹는다 — 48 m 로 줄였더니 마지막 두 줄이 종소리에 겹쳤다) |
| 검은 화면(3 초) 동안 추격자가 30 m 를 다 좁혀 시야가 열리자마자 붙어 있었다 | 암전 구간에 추격을 돌리고 있었다 → `intro` 상태를 나눠 숨만 돌린다 |

**그 밖에**: 잠식(dread)에 제곱을 씌우고 상한을 0.75 → 0.62 로(선형이면 달리는 내내 붉은 화면이었다) ·
추격자 최대 거리 42 → 34 m(전력으로 달리면 뒤가 통째로 비었다: 실측 30 → 37 m) ·
길 밖으로 벗어난 추격자 위치를 외삽(안 하면 길 초입에 붙박인다) · 비트 표를 **길이에 비례**해 환산 ·
`vite.config.ts` 가 `PORT` 를 읽는다(같은 저장소에서 두 세션이 dev 서버를 띄울 때).

**검증**(전 구간 인게임, `__dbg.step` 결정적 스텝): 암전·숨 → 3.6 m/s 로 62 m, 길 이탈 **0.4~0.9 m** ·
비트 발화 0.7 / 13.7 / 20.3 / 28.3 / 40.5(보지 마) / 42.3(넘어짐) / 46.4(피안화) / 57.3(도리이) m ·
번개 3 회(달빛 1.05 → 11.3) · 정적 → 손 빠짐 → 종 → 금기 4 줄 → 타이틀 → ACT 2 → `chapter act03` 인계 ·
조명·안개·잠식·번쩍임 전부 원복 · 60 fps.
**미검증**: 실제 마우스로 뒤를 돌아봤을 때의 저항감(스크립트로는 강제 뒤돌아봄만 확인), 오디오 청감.

### 프롤로그 템포 조정 (2026-08-21)

사용자 지적 셋: **① 컷신 진행이 느리다 ② 손 잡고 가는 게 더 달리듯이 빨라야 한다
③ 버스에서 내린 뒤 시간대를 밤으로 해야 하나?**

**달리기 `SPEED_MUL = 1.2`** (3.6 → **4.32 m/s**). ACT 1 은 목숨 걸고 도망치는 밤인데
평소 달리기 속도로는 산책으로 보였다. 상수 하나만 두고 **추격자 속도·숨·손 리듬·「처졌다」 판정을
전부 여기서 역산**한다 — 숫자를 바꿔도 압박의 세기와 리듬이 그대로 따라온다.
(처음 ×1.32=4.75 로 잡았다가 "너무 빠르지 않냐"는 지적에 ×1.2 로. 어린아이 전력질주는 3.5~4.5 m/s 다.)

**구간 62 → 70 m** (z 86 → z 16). 빨라진 만큼 늘려야 대사가 안 밀린다 — **자막 길이(초)가 곧 거리**라서
4.32 m/s 에서는 한 줄이 화면에 떠 있는 동안 6~16 m 를 간다.

**밀린 줄은 다음 줄에 잘린다.** 실측에서 「보지 마!」와 「일어나!」가 **0.4 초 간격**으로 겹쳐
앞의 것이 0.4 초만 떴다(`trip()` 이 `dialogue.clear()` 를 부른다). → 넘어짐을 뒤쪽 목소리 사이의
**빈자리(24 m)** 로 옮기고, 스토리보드가 한 문단으로 적은 두 문장(*피안화 길* · *도리이*)을
**원문대로 한 줄로 합쳤다**. 결과: 모든 줄이 제 길이를 다 쓴다.

**앞뒤를 줄였다.** 도입은 순서대로 기다리면(3 초 암전 → 2.4 초 페이드) 달리기까지 5.4 초를 가만히 본다
→ **겹친다**: 1.5 초 암전 → 시야가 열리기 시작 → 0.7 초 뒤 조작 인계(아직 어둑할 때 이미 달리는 중).
끝은 손 놓기를 자막과 겹치고 각 대기를 줄여 17.5 → **12 초**.

**ACT 1 실측 타임라인** (인게임 초, `setTimeout` 을 시뮬 시계로 돌려 정확히 계측)
| t | 거리 | |
|---|---|---|
| 0.1–2.6 | — | 거친 숨소리. 빗소리. (암전) |
| 2.7–5.6 | 1.6 m | 누군가의 손이 내 손을 잡고 있다 |
| 5.8–7.3 | 15 m | 「미오!」 |
| 7.9–9.3 | 24 m | **넘어짐** → 「일어나!」 |
| 10.4–11.9 | 30 m | 「거기 서!」 |
| 12.7–14.4 | 40 m | 「아이를 잡아!」 |
| 15.4–17.3 | 52 m | 「보지 마!」 |
| 17.3–20.9 | 60 m | 피안화 길 + 도리이 (한 줄) |
| 21.0–23.0 | 70 m | 갑자기 모든 소리가 끊긴다 → 손 빠짐 → **댕—** |
| 25.0–34.6 | — | 「언니……?」 「미오야.」 「피안화가 피어 있는 길은…」 |

ACT 1 ≈ **35 초** · 타이틀 ≈ 4 초 · ACT 2 ≈ 24 초 → 인계까지 약 **63 초**.

**③ ACT 2 시간대 — 해질녘 도착 → 컷신 중 밤** (사용자 선택)
스토리보드는 ACT 2~5 의 시간을 적지 않았다. 시골버스 종점이 한밤중일 리 없다는 것보다
더 중요한 이유가 있다: 이렇게 두면 「**버스가 없다 · 아스팔트 도로도 사라졌다**」와
**해가 사라진다**가 같은 사건이 된다 — 히가사토가 현실에서 떨어져 나가는 순간을 조명이 같이 말한다.
- 하차: `dusk` · 초칭 **꺼짐**(아직 밝다)
- t 12.2 s(「버스가 없다」 줄): `night` 로 **8.5 초 보간**
- t 17.6 s: 초칭 점등 — 이 게임에서 등불이 처음 켜지는 순간이 생겼다
- 컷신 직후 `setTime('night')` 로 한 번 더 못 박는다(스킵으로 건너뛰어도 인계는 반드시 밤)
- 밸런스(초칭·감지·요괴)는 전부 밤 기준 그대로 — 밝은 구간이 컷신 안에서 끝나기 때문에 손댈 게 없다

**덤으로 잡은 것**: ACT 2 마지막 두 컷이 **금줄 게이트를 코앞에서** 잡아
「그 자리에는 오래된 흙길과 빽빽한 삼나무 숲만 남아 있다」에 흙길도 숲도 없었다(기둥이 화면을 갈랐다).
→ 시선을 게이트 **너머**(참배로 s 16 · 30)로. 마지막 컷은 정류장 표지가 정확히 화면 한가운데를
막고 있어서 카메라를 표지 북서쪽으로 뺐다 — 미오는 프레임 밖이고, 「……사람을 찾으러 왔어요」가
**아무도 없는 길 위로** 얹힌다.

**검증**: 전 구간 결정적 스텝 재생 — 최고 속도 4.32 m/s · 자막 겹침 0 · dusk(2.6) → night(1.5) 보간 ·
초칭 0 → 2 · 인계 시 `chapter act03` · HUD 복귀 · 조명 원복.

### 템포 2차 · 오후 3시 하차 (2026-08-21)

사용자 지시 넷: **① 일어날 때 살짝 느리게 ② 손 잡고 달리는 부분 25 % 빠르게
③ 시간대를 조금 밝게 ④ 버스에서 내린 시간은 오후 3시쯤.**

**① 넘어졌다 일어나는 동작** `firstPerson.ts`
세 단계의 몫을 상수로 뽑고 비율을 바꿨다 — **넘어지는 건 순식간이고 일어나는 건 느리다.**
길이 1.6 → 2.2 s, 단계는 `FALL 0.13 · DOWN 0.17 · 나머지가 일어남`.
실측(눈높이 m): 1.03 → 0.41 이 **0.29 s**(낙하), 0.37 s 엎어져 있음, 0.41 → 1.03 이 **1.2 s**(전엔 0.8 s).
속도도 같은 곡선으로 돌아온다(0 → 5.4 를 1.2 s 에 걸쳐) — 벌떡 일어나 튀어나가지 않는다.

**② 달리기 ×1.2 → ×1.5 → ×1.7 → ×2.0** (4.32 → 5.4 → 6.12 → **7.2 m/s**). 구간 70 → **72 m**(z 86 → z 14).

속도를 올릴 때마다 비트가 깨졌다. **자막이 화면에 떠 있는 시간이 곧 거리**이기 때문이다 —
1.5 초짜리 한 줄이 4.3 m/s 에서는 6 m 를 먹지만 7.2 m/s 에서는 **11 m** 를 먹는다.
구간은 72 m 뿐이라 뒷줄들이 밀리고, 밀린 줄은 다음 줄에 잘린다. 두 번은 비트 표를 손으로 다시 짰다.

세 번째에 **자막 진행 속도를 달리기 배율에 묶었다** (`Dialogue.setRate`, `SPEED_MUL / BEAT_REF`).
타자 속도와 표시 시간에 동시에 걸리므로 **한 줄이 먹는 거리가 고정**된다 —
이제 `SPEED_MUL` 한 줄만 고치면 비트 표는 그대로다. 실측: ×1.7 → ×2.0 에서 각 줄이 시작되는
거리가 16.7 / 27.3 / 32.4 / 41.7 / 53.7 / 65.3 m — 이전(18.1 / 27.3 / 32.5 / 41.8 / 52.3 / 64.1)과 사실상 같다.
달리기가 끝나면 `setRate(1)` 로 되돌린다 — **암전 위의 금기 세 줄은 천천히 읽혀야 한다.**

⚠️ 이제 한계는 비트 간격이 아니라 **가독성**이다. ×2.0 에서 가장 긴 줄(38자)은 다 찍히는 데 1.2 초,
읽을 시간이 **1 초**뿐이다. 더 밟으려면 그 줄을 쪼개거나 덜어내야 한다.

⚠️ **체감이 배로 뛴 이유가 따로 있었다.** 흔들림 주기에 `topSpeed` 를 한 번 더 곱하고 있어서
이동이 25 % 빨라질 때 화면 흔들림까지 15 % 빨라졌다. 1인칭 체감 속도는 **눈높이(1.05 m·아이)와
화각(66°)이 이미 크게 부풀린다** — 거기에 흔들림까지 얹으면 곱으로 붙는다. 곱하기를 제거했다.
(사용자 리포트: "25 %로 한 거 맞아? 25배 같은데")

⚠️ **시작점이 벽에 막혔다.** 구간을 늘리려고 출발을 z 92 로 내렸더니 2 m 만에 멈췄다 —
금줄 게이트는 「온 길로는 돌아갈 수 없다」를 위한 **의도된 벽**이다. 게이트 안쪽(z 86)이 최남단이다.

**③ 「시간대를 조금 밝게」는 ACT 1 얘기였다** (내가 ACT 2 로 잘못 알아들었다 — 사용자 재지적:
"처음 뛰는 컷 너무 어둡다니까 시간이"). `rainNight` 을 한 단계 더 올렸다:
달빛 1.05 → **1.9** · 반구광 0.52 → 0.95 · 환경광 0.40 → 0.72 · 노출 0.74 → 0.90 ·
비네트 0.58 → 0.46 · 안개 0.024 → **0.017**(색도 0x111a28 → 0x1c2a44 로 들어 올려 원경이
검정이 아니라 **비안개**로 읽히게). 하늘 돔·달 크기도 조금씩.
이제 길·논둑·민가·지장·피안화가 번개 없이도 전부 보인다 — 어둠은 여전히 안개가 만든다.
번개는 달빛 1.9 → 9.8(약 5배)이라 밝아진 바탕에서도 그대로 사건이다.

**④ 하차 시간 = 오후 3시** — `afternoon` 프리셋 신규
밝은 데서 시작해야 「버스가 없다 · 아스팔트 도로도 사라졌다」와 **해가 사라진다**가 같은 사건이 된다.
24 초 만에 3시에서 밤이 되는 건 물리적으로 불가능하고 **그게 요점이다.**
피안(9월 하순) 오후라 해가 서남서로 기울어 있고(고도 34° · 방위 −80°) 빛이 살짝 노랗다 —
`dusk`(−108°)로 자연스럽게 이어진다. 채도는 거의 안 올리고 안개를 조금 남겨 **밝지만 즐겁지 않게**.

해는 **두 단계로 무너진다.** 오후에서 밤으로 곧장 보간하면 색이 그냥 어두워질 뿐 노을을 지나가지
않아 "저녁이 왔다"로 안 읽힌다:
| 컷신 t | |
|---|---|
| 0 s | `afternoon` · 초칭 **꺼짐**(대낮이다) |
| 8.4 s | 미오가 **뒤를 돌아보는 줄** → `dusk` 로 6 초 보간. 돌아보는 동안 등 뒤에서 해가 진다 |
| 15.0 s | `night` 로 7 초 보간 |
| 19.0 s | **초칭 점등** — 이 게임에서 등불이 처음 켜지는 순간 |
| 직후 | `setTime('night')` 로 못 박음 (스킵해도 인계는 반드시 밤) |

**ACT 1 실측 타임라인** (인게임 초 / 거리)
| t | m | |
|---|---|---|
| 0.0–2.6 | — | 거친 숨소리. 빗소리. (암전) |
| 2.6–5.6 | 1.7 | 누군가의 손이 내 손을 잡고 있다 |
| 5.6–7.1 | 18 | 「미오!」 |
| 7.4–8.7 | 27 | **넘어짐** → 「일어나!」 |
| 9.6–11.1 | 32 | 「거기 서!」 |
| 11.5–13.1 | 42 | 「아이를 잡아!」 |
| 13.9–15.7 | 55 | 「보지 마!」 |
| 15.8–18.7 | 66 | 피안화 길 + 도리이 |
| 18.9–20.8 | 72 | 정적 → 손 빠짐 → **댕—** |
| 22.8–32.4 | — | 「언니……?」 「미오야.」 「피안화가 피어 있는 길은…」 |

ACT 1 ≈ **32 초** · 타이틀 4 초 · ACT 2 24 초 → 인계까지 약 **60 초**. 자막 겹침 0.

**검증**: 결정적 스텝(+`setTimeout` 을 시뮬 시계로 대체)으로 전 구간 —
최고 속도 5.40 m/s · 넘어짐/일어남 0.29 s / 1.2 s · afternoon(달빛 2.9) → dusk → night(1.5) ·
초칭 0 → 2 · `chapter act03` 인계 · HUD 복귀.

### ACT 2 제작 — 「버스 안」이 통째로 빠져 있었다 (2026-08-21)

스토리보드 ACT 2 는 **낡은 시골버스 내부**에서 시작한다. 그런데 구현은 「종점이야.」부터였다 —
장면 목적 셋 중 **둘이 그 안에서** 일어나는데(현재의 미오 소개 · **훼손된 사진**) 둘 다 없었다.
특히 세 번째는 이 게임의 전제다: **미오는 언니의 얼굴을 기억하지 못한다.**

**`story/photo.ts` — 가족사진** (이 ACT 의 핵심 이미지)
캔버스로 그린다. 텍스처 에셋을 만들지 않는 이유는 비용이 아니라 **훼손이 사진의 일부여야** 하기
때문이다 — 나중에(ACT 30) 얼룩이 걷히는 연출을 하려면 얼굴과 얼룩이 같은 그리기 코드 안에 있어야 한다.
`damaged: 0` 을 주면 온전한 사진이 나온다.
- 도리이 앞에 선 두 사람. 어린 미오는 **푸른 원피스**(ACT 1 의 젖은 소매 0x3a4a63 과 같은 옷),
  사요는 흰 여름 유카타. 손을 잡고 있다 — 이 게임 전체가 그 한 줄에서 나온다
- **순서가 연출이다**: 사요의 얼굴을 그린 뒤 얼룩으로 지우고, **머리카락만 다시 그린다.**
  머리 실루엣이 남아 있어야 "사진이 낡았다"가 아니라 **"저 사람 얼굴만 없다"** 로 읽힌다
- 뒷면에 연필 글씨 — 「미오 여섯 살」은 읽히고 **사요의 이름 줄만 번져 있다**
- 잡은 것: 얼룩을 인화지색(밝은 크림)으로 칠했더니 **전구처럼 빛났다** → 종이색보다 어둡게 ·
  사요의 유카타를 어둡게 뒀더니 뒤편 삼나무 띠와 명도가 같아 **돌덩이**가 됐다 → 흰 유카타 ·
  팔을 허공에 선으로 그었더니 막대기가 걸쳐진 것으로 보였다 → 어깨에서 나와 가운데서 맞잡게

**`world/bus.ts` — 버스 내부**
히가사토는 200 m 정사각이고 참배로는 z 94 에서 시작한다 — **버스가 달릴 길이 없다.**
그래서 영화의 방법을 쓴다: **버스는 서 있고 창밖이 흐른다.** 지형 밖(y +300)의 자립 세트라
지형·콜라이더·요괴와 완전히 무관하다.
- 좌우 벽을 **띠로 쪼개** 창 네 칸을 비운다(박스에서 구멍을 도려내려면 CSG 가 필요한데 과하다).
  처음엔 투명판을 벽 앞에 세워 "구멍"을 만들려 했는데, 당연히 벽은 그대로 막혀 있었다
- 창밖은 삼나무 실루엣 **두 줄**(가깝고 빠른 줄 + 멀고 느린 줄). **시차**가 없으면 벽지가 미끄러진다.
  나무를 개별 오브젝트로 두지 않고 **UV 를 흘린다**
- 흔들림 두 겹: 노면(저주파 롤·피치)과 엔진(고주파 미세 진동). 정차하면 엔진만 남는다
- **기사는 모델을 만들지 않는다** — 시트에 가린 어깨와 뒤통수 실루엣뿐이고,
  「기사는 백미러로 미오를 오래 바라보다」는 **거울 속 눈 두 개**로 성립한다.
  얼굴을 만들면 오히려 약해진다. 바라보는 동안에는 **깜빡이지 않는다** — 그게 신호다

**`story/act2.ts` — 1인칭 (각색 8 의 예외)**
각색 8 은 *과거 = 1인칭 · 현재 = 3인칭* 인데 여기만 예외로 둔다. 이유는 하나: **사진은 손에 있다.**
3인칭으로 잡으면 관객이 "사진을 보는 사람"을 보게 되고 정작 봐야 할 얼룩은 화면에 없다.
그리고 이 예외가 다음 컷을 벌어 준다 — 문이 열리고 내려서는 순간 카메라가 뒤로 빠지며
**현재의 미오를 처음 보여준다**(장면 목적 ①).

조작을 뺏지 않는다. 마우스로 사진을 기울일 수 있고(기울이면 얼룩이 빛을 받는다),
기사가 부르면 **고개를 들 수 있다** — 안 들면 3 초 뒤 미오가 스스로 든다.
ACT 1 의 「보지 마!」와 같은 문법이다. 고개를 드는 순간 백미러 안에서 눈이 이쪽을 본다.

**`FirstPerson` 확장**: `configure({ eye, fov, hand, bob, resistFrom, maxTurn, anchor })`.
`anchor` 는 카메라를 **컨트롤러 대신 그 자리에** 둔다 — 앉은 장면은 물리 캡슐을 따라갈 이유가 없고,
버스는 지형 밖이라 컨트롤러를 옮기면 그대로 떨어진다.

**`sfx`**: `voice(gain, 'low'|'girl')` — 외침이 아니라 **평서문**이다(피치가 처음부터 끝까지 내려온다,
음절 2~3 개로 끊는다) · `busEngine` 디젤 두 겹(폭발 주기 + 차체 울림) · `busBrake` · `busDoor`

**⚠️ 잡은 버그 셋**
| 증상 | 원인 |
|---|---|
| 뒷벽을 보고 앉아 있었다 | 카메라 전방은 yaw θ 에서 (−sinθ,0,−cosθ). 버스 앞(+z)을 보려면 **θ = π** 인데 0 으로 뒀다 |
| 「고개를 든다」가 아무 일도 일으키지 않았다 | 플래그만 세우고 시선을 옮기는 코드가 없었다 → 백미러 각도로 1.5 초 유도 |
| 고개를 들었더니 **창밖**을 봤다 | 오른쪽(+x)을 보려면 yaw 를 **키워야** 하는데 부호를 반대로 잡았다 |
| 실내 천장이 통째로 초록 | `hemiGround`(아래에서 올라오는 반사광)가 올리브라 아래를 보는 면을 물들였다 → 채도를 뺀 흙색 |

**프롤로그 전체 타임라인** (인게임 초, 결정적 스텝 계측)
| | |
|---|---|
| 0 – 32 s | ACT 1 「붉은 꽃밭」 |
| 32 – 36 s | 타이틀 《彼岸花》 |
| **37 – 70 s** | **ACT 2a 「버스 안」** — 사진 2줄 · 「학생.」 · 대화 4줄 · 백미러 · 정차 |
| 75 – 98 s | ACT 2b 「종점」 — 하차, 오후 3시 → 노을 → 밤 |
| ~100 s | 조작 인계 (`chapter act03`) |

**검증**: 전 구간 결정적 재생 — 자막 26줄 순서·겹침 0 · 사진 판독 가능 · 백미러 눈 표시/정지 ·
문 열림 · 인계 시 밤/HUD 복귀 · 60 fps.
**미구현(다음)**: 사진을 **인벤토리에 넣고 「사진 확인」**(스토리보드 ACT 2 말미의 게임플레이 시작 목록) ·
기사의 목소리 실제 샘플 · 버스가 떠나는 것을 3인칭으로 보여주기(지금은 자막).

### ACT 1 사운드·속도 3차 (2026-08-21)

**① 심장 소리를 뺐다.** 숨·빗소리·발소리·뒤쪽 외침이 이미 겹쳐 있는 구간이라 저역이 한 겹 더
깔리면 다른 소리를 다 먹는다. 심장은 **정적이 있는 장면**의 것이다 — 요괴 추격(`matsuri.ts`)에 남긴다.

**② 재생속도 +30 %** (×2.0 → **×2.6**, 7.2 → **9.36 m/s**). 자막이 배율을 따라가므로(v2.2)
비트 표는 그대로다 — 각 줄이 시작되는 거리가 0.9 / 16.4 / 27.4 / 32.5 / 41.7 / 56.5 / 68.2 m 로
이전(0.8 / 16.7 / 27.3 / 32.4 / 41.7 / 53.7 / 65.3)과 거의 같다. ACT 1 28.6 → **26.2 초**.

다만 **긴 줄 둘은 글자를 줄여야 했다.** 배속은 표시 시간을 줄이지 글자 수를 줄이지 않는다 —
×2.6 에서 38자 줄은 읽을 틈이 0.7 초였다. 스토리보드 문장을 다듬어 옮겼다:
- 「누군가의 손이 내 손을 잡고 있다. 진흙길을 달린다.」 → 「누군가의 손이 나를 끌고 달린다.」
- 「비에 젖은 붉은 피안화 길이 끝없이 이어진다. 멀리 신사의 도리이가 보인다.」 → 「붉은 피안화 길이 끝없이 이어진다. 멀리 도리이가 보인다.」

**③ 발소리를 카메라 흔들림에 물렸다** — 「불규칙하게 들린다」의 원인이 둘이었다:
1. 발소리는 **달리기 클립의 발목 높이**로 났는데, 클립 재생 배속이
   `clamp(speed / runClipSpeed, 0.6, 1.8)` 로 **1.8 배에서 잘려** 있다. ACT 1 은 그보다 빠르게
   이동하므로 소리가 몸보다 느려지고, 게다가 1인칭에서는 **그 몸이 보이지도 않는다**
2. 화면에서 유일하게 보이는 걸음은 **카메라의 상하 흔들림**인데, 그건 완전히 다른 리듬이었다

→ 착지 = `bobY` 가 최저인 순간(sin = −1)이므로 `bobT*2 + π/2` 가 2π 를 넘을 때마다 한 걸음을 낸다
(`FirstPerson.onStep`). 1인칭 동안 애니메이터 쪽 발소리는 끈다.
넘어져 있는 동안에도 **계수는 계속 돌린다** — 소리만 죽이고 계수를 멈추면 일어나는 순간
밀린 한 걸음이 박자 밖에서 터진다(실측으로 0.28 초 간격 중에 0.10 이 하나 끼어 있었다).

**실측**: 간격 0.266 ~ 0.284 초(평균 **0.274**, 최대 편차 **0.010 초** = 60 Hz 프레임 하나),
좌우 `RLRLRLRLRLRLRLRLRLRLRLRLRL` 완전 교대, 심장 소리 호출 **0 회**.

### ACT 3·4 제작 — 「세 가지 금기」와 「끝나지 않은 축제」 (2026-08-21)

ACT 1·2 를 하나씩 제대로 만든 방침을 이어, 프레임부(ACT 1~5)의 남은 두 장을 채웠다.
지금까지 ACT 3·5 는 S1 의 자리표시자였다 — E 한 번에 자막 네 줄이 흐르고 끝났다.

#### ACT 3 「세 가지 금기」 — **모르고 어기게 만든다** (`story/act3.ts`)

이 ACT 의 마지막 한 줄이 게임 전체를 건다: *플레이어는 아직 모르지만, 미오는 게임 시작 직후
세 번째 금기를 어겼다.* 그래서 설계 목표가 "연출을 보여준다"가 아니라 **모르고 어기게 만든다** 다.

- **「[E] 대답한다」를 띄우지 않는다.** 선택지가 뜨는 순간 그건 금기가 아니라 퀴즈다.
  대신 죄 없어 보이는 조작 하나만 남긴다 — **소리 나는 쪽을 돌아보는 것**
- **돌아보면 한 번의 부름에, 버티면 두 번째 부름 뒤에** 대답한다(4.0 s / 6.6 s). 어느 쪽이든 대답한다.
  플레이어가 한 일은 "돌아본 것"뿐인데 결과가 같다는 게 이 장면의 전부다
- 시선은 `ThirdPersonCamera.pull()` 로 **끌리기만** 한다(마우스는 살아 있다). ACT 1 의 시선 저항과 같은 문법
- **목소리는 북쪽 참배로 위**에서 온다. 그 길이 곧 피안화가 핀 길이라 **二와 三이 같은 방향을 가리킨다** —
  읽자마자 둘 다 어기게 된다. 두 번째 부름은 더 가까이서(38 m → 14 m) 온다: 다가오고 있다
- 「미오는 움직임을 멈춘다」 = 이동 배율 0. **멈춤은 0.25 s, 풀림은 0.9 s** — 반대로 하면
  "얼어붙었다"가 아니라 "조작이 끊겼다"로 읽힌다

**비석을 닦는 것이 플레이가 됐다.** 스토리보드의 「플레이어가 표면을 닦으면 글자가 나타난다」는
한 번의 E 로는 성립하지 않는다 — 닦이는 걸 **봐야** 한다.
- `inspect.ts` 에 **꾹 누르기**(`hold` / `onHold`) 추가. 되감김은 진행의 2배 속도(놓쳤을 때 "조금 되돌아간" 느낌)
- 게이지는 별도 위젯이 아니라 **프롬프트 줄 자체**가 차오른다 (HUD 를 하나 더 늘리지 않는다)
- `tablet.ts` 를 단계(3상태)에서 **연속값 두 개**(`wipe` 0→1 · `stainTo` 0→1)로 바꿨다.
  이끼 블롭은 **한 번만 뽑아 두고 문턱값으로 지운다** — 매번 새로 뽑았더니 닦는 동안 이끼가
  자리를 옮겨 다녀 지지직거리는 노이즈가 됐다. 문턱값이 y 기준이라 **위에서부터** 벗겨지고,
  그래서 **마지막에 드러나는 문장이 세 번째 금기**다. 목소리가 그 위에 얹힌다

#### ACT 4 「끝나지 않은 축제」 — 상시 월드 + 방송 하나

스토리보드의 이 ACT 는 두 부분인데 성격이 다르다. 생활 흔적은 **걷다가 스치는 것**이라 월드에
상시로 놓고(`world/higasato/lifesigns.ts`), 방송만 스크립트로 둔다(`story/act4.ts`).

`lifesigns.ts` — 다섯 줄을 그대로. 앵커는 민가(`minka.ts`)에서 가져온다(한자리에 모으지 않는다):
- **찻잔의 김** — 곧게 올라가면 연기가 아니라 막대다. 위로 갈수록 흩어지게
- **빈집 텔레비전** — 광원을 **집 안**에 둔다. 그림자를 안 만드는 포인트라이트는 벽을 통과하므로
  안에서 켜도 툇마루와 땅이 같이 물든다 = 새어 나오는 빛. 밝기는 사인파가 아니라 **불규칙**해야 잡음 화면이다
- **젖은 게다** — 게다만 놓으면 오래된 신발이다. **젖은 자국**이 있어야 방금 벗은 것이 된다. 나란하지 않게
- **풍경(風鈴)** — 「바람이 없는데」라서 흔들림이 바람처럼 불규칙하면 안 된다.
  **누가 건드린 것처럼 한 번 크게 흔들리고 잦아든다**. 短冊(종이)이 있어야 밤에 보인다
- **골목 끝의 사람** — 시야에서 벗어나면 사라진다. 단 **다 보였을 때만**(α ≥ 0.85):
  스쳐 지나가느라 반쯤 떠 있던 걸 없애면 플레이어는 아무것도 못 본 채 한 번뿐인 연출을 잃는다

`speaker.ts` — **소리가 나올 물건이 화면에 먼저 있어야 한다.** 허공에서 나는 안내방송은 연출이 아니라 UI 다.
전주 둘(마을 초입·광장) + 나팔 넷. 초입 전주에 **공고판**이 달려 있고, 종이는 새것처럼 빳빳한데
**날짜만 10년 전**이다 — 그 어긋남 하나가 마을의 상태를 설명한다. 게시판은 **남쪽**을 본다(걸어오는 사람의 정면).

`act4.ts` — 방송은 **뚝 끊긴다**(페이드 아님). 「심한 잡음」은 자막으로 적지 않는다 —
그 자리를 비워 둬야 다음 문장이 무겁게 떨어진다. 비석을 안 읽고 지나쳐도 방송은 울린다
(마을의 사건이지 미오의 행동이 아니다). 단 ACT 3 이 도는 중이면 기다린다 — 자막 두 줄이 같은 자리에서 싸운다.

#### 사운드 — 위치가 있는 소리 (`sfx.ts`)

`panAt(id, …)` 패너 재사용 창구를 두고 그 위에:
- **`callName`** — 「미오야」. `voice()` 와 셋이 다르다: ① 위치가 있다(어느 쪽인지 들려야 돌아본다)
  ② 음절이 **모음으로 정해진다**(이·오·아 포먼트) — 웅얼거림이 아니라 **내 이름**이어야 한다
  ③ 거리를 세 가지로 만든다: 로우패스(공기가 고역을 먹는다) · **산울림 두 겹**(한 겹이면 그냥 에코) · 늦은 어택
- **`paOn/paOff/paChime/paVoice/paNoise`** — 확성기 색깔의 90 %는 **대역 제한**이다(300~2800 Hz).
  거기에 나팔 공진(1.4 kHz 피크)과 약한 클리핑. 방송 내내 험 + 히스가 깔린다
- **`bell(gain, far)`** — 먼 종은 **같은 종이 작아진 것이 아니다**: 고역이 먹히고, 타격의 「깡」이 사라지고,
  어택이 늦고, 골짜기가 한 번 되돌려준다
- `stoneWipe`(어택이 서면 손톱으로 긁는 소리가 된다) · `furin` · `tvStatic`

#### ⚠️ 잡은 버그 셋

| 증상 | 원인 |
|---|---|
| 비석 각인이 **돌 옆에 떠 있는 초록 판**이었다 | 비신을 폭 1.15(x) × 두께 0.26(z) 으로 세워 **넓은 면이 남북**을 보고 있었다. 각인면은 서쪽(길)에 붙는데 두께 26 cm 모서리에 0.98 m 판을 대니 양옆으로 튀어나온다 → 폭을 z 로 돌리고 기울기도 x축→z축 회전으로 |
| 프롤로그 중에 「[E] 비석을 닦는다」가 떴다 | ACT 1 은 `cine` 이 아니라 `fpOn` 인데 조사·ACT 3~4·생활 흔적 갱신이 `!cine` 만 봤다. ACT 1 은 참배로를 달리며 **비석 옆을 지나간다** — 빗속에서 빈집 TV 잡음도 같이 났다 → `!cine && !fpOn` |
| 대답 뒤 흔들림이 검증에서 순서가 어긋났다 | `setTimeout(1500)` — 실시간이라 결정적 스텝(과 가려진 탭)에서 시뮬 시계와 어긋난다 → 시뮬 시계 카운터로 |
| 나팔이 공중에 뜬 부채였다 | 원뿔을 `rotateZ→rotateY` 로 눕히며 축 부호를 손으로 맞추려 했다 → `setFromUnitVectors(+Y, −dir)` 로 한 줄. 겸사겸사 **살짝 아래를 향하게**(수평이면 하늘에 대고 방송한다) |

**검증** (결정적 스텝 `__dbg.step`, 자막 타임라인 실측)
- 닦기: 게이지 0→100 % 2.4 s · 이끼 피복률 **0.942 → 0.120**(남은 건 각인 글자)
- ACT 3 낭독 2.9 / 5.8 / 8.9 / 12.0 s (겹침 0) · 부름 → **돌아봄 15.2 s / 버팀 17.8 s** 대답 ·
  얼룩 20.2 s · 이동 배율 1 → 0(12 s) → 1(18 s) · 시선 1.03 rad 끌림 · `answered` 1 · `chapter` act04
- ACT 4 마을 진입 즉시 발화 · 방송 27.5 / 32.4 s · 「피안제……?」 37.8 s · 종 41.2 s · `chapter` act05 ·
  **비석을 건너뛴 경로에서도** 방송 발화 확인
- 공고판 → 「二〇一五年 九月 二十三日」 → 「10년 전 날짜잖아」 판독
- 새 사운드 8종 예외 0 · 마을 한복판 실렌더 **3.43 ms/frame**
**미구현(다음)**: ACT 5 는 아직 S1 자리표시자다 — 본전 문(굳게 잠김)·석판 접근 연출·「신사 바닥 아래 웃음소리」의 위치화.

### ACT 1 속도 4차 — ×1.8 (2026-08-21)

사용자 지시: **「달려」부터 「조용해진다」까지 재생속도 1.8배.** `SPEED_MUL` 2.6 → **4.68**(16.85 m/s).

**배속이 걸리는 구간을 지시대로 좁혔다.** 전에는 `setRate` 가 `play()` 첫머리에 있어서
암전 위의 「거친 숨소리. 빗소리.」까지 빨라졌다. 이제 **달리기가 시작될 때** 걸고
`finish()` 의 「모든 소리가 끊긴다」 직전에 푼다 — 암전 위의 첫 줄과 정적 뒤의 금기 세 줄은
원래 속도로 읽힌다. 첫 줄 길이는 암전 구간(1.5 + 0.7)에 맞춰 2.6 → **2.2 초**.

**시간으로 적혀 있던 것 둘을 속도에 물렸다.** 이 ACT 는 거리 기준인데 둘만 초 단위였다:

| 무엇 | 문제 | 처리 |
|---|---|---|
| **뒤돌아보기**(1.3 s) | ×2.6 에서 12 m 였던 것이 ×4.68 에서 **22 m** 가 되어 「보지 마!」가 마지막 줄(58 m)보다 **뒤에** 터졌다 — 마지막 줄이 잘렸다(실측: 66.2 m) | 거리로 고정(`LOOK_BACK_M` 12.2 m). 각속도는 거기서 역산 → 56.7 m 복귀 |
| **넘어짐**(2.2 s) | 여기만 원래 속도로 흘러 구간이 통째로 튄다 | `TRIP_S = 2.2 × 2.6 / SPEED_MUL` = **1.23 s** |

**검증** (결정적 스텝, W 홀드)
- 최고속도 **16.85 m/s** = 3.6 × 4.68 · 넘어짐 **1.23 s**(2.2 → ×1.79) · 달리기 구간 9.9 → **5.1 초**
- 비트 거리 **0.8 / 16.5 / 27.4 / 32.6 / 41.8 / 56.7 / 68.5 m** —
  v2.3(0.9 / 16.4 / 27.4 / 32.5 / 41.7 / 56.5 / 68.2)과 사실상 동일. **자막-거리 불변식이 유지된다**
- 발소리 간격은 **안 변한다**(0.274 s). `bobT` 는 `speed/topSpeed` 정규화라 배율과 무관 —
  보폭이 4.6 m 가 되지만 이 장면의 전제가 「끌려가고 있다」라 그대로 둔다

**→ 정착: ×2.5** (2026-08-21, 사용자 지시). 올린 이유였던 「느리다」의 실제 원인은 배율이 아니라
**끌기(0.9 m/s)와 보폭 고정**이었고, 그 둘을 고친 뒤에는 ×2.5 에서도 도주로 읽힌다.
×4.68 은 60.7 km/h 라 사람이 아니라 차였다. 아래 「자막 읽을 시간」 문제도 같이 사라졌다
(가장 긴 줄 0.95 → **2.30 초**).

| ×2.5 실측 | 전속력(W) | 끌려갈 때(무입력) |
|---|---|---|
| 속도 | 9.0 m/s · 32.4 km/h | 5.58 m/s · 20.1 km/h |
| 걸음 | 4.29 /초 · 보폭 **2.10 m** | 2.72 /초 · 보폭 **2.05 m** |
| 화각 | 73.8° | 70.8° |
| 72 m 구간 | 9.5 초 | 약 15 초 |

**⚠️ (×4.68 당시의 문제 — ×2.5 에서는 해소됨) 자막 읽을 시간.** 배속은 표시 시간을 줄이지 글자 수를 줄이지 않는다.
짧은 외침(3~7자)은 0.55~1.03 초로 괜찮은데 **긴 줄 둘이 한계를 넘었다**:

| 줄 | 자 | 표시 | 초당 |
|---|---|---|---|
| 누군가의 손이 나를 끌고 달린다. | 18 | 0.95 s | 19자/s |
| 붉은 피안화 길이 끝없이 이어진다. 멀리 도리이가 보인다. | 32 | 0.95 s | **34자/s** |

거리는 이미 꽉 차 있어(`dur` 을 늘리면 다음 비트를 밀어낸다) **글자를 줄이는 것 말고는 방법이 없다**
— v2.3 에서 한 번 한 처리다. 스토리보드 문장을 더 깎는 건 컨펌 사항이라 보류.

### 버스 소품 Tripo 교체 (2026-08-21) `world/bus.ts`

버스 내부가 전부 프리미티브였다. Tripo 교체 4종 (크레딧 80 · 잔액 255):
| 이름 | tris | |
|---|---|---|
| `bus-seat` | 7,644 | 청록 비닐 + 크롬 프레임, 8석 |
| `bus-wheel` | 5,709 | 핸들+컬럼 (기사 유닛이 오면 숨김) |
| `bus-strap` | 2,900 | 츠리카와 12개 — 봉 피벗에 매달려 차체 롤을 **반대로 지연**시켜 흔들린다 |
| `bus-driver` | 7,488 | "no chair, no wheel" 지시를 무시하고 **시트+핸들까지 한 유닛**으로 나왔다 → 오히려 유닛째 배치 |

- 구조: 프리미티브를 먼저 세우고 `load()` 가 도착한 GLB 로 교체 — 실패해도 장면이 비지 않는다
- **`fitFootprint`**: Tripo 는 크기 지시를 잘 안 듣는다. 벤치를 한 자리로 시켜도 2~3인용으로
  나온다(통로까지 덮었다) → 긴 수평축을 차체 가로로 눕히고 X 0.82 m · 깊이 0.6 m 로 축별 보정
- 차체 껍데기(벽·창)는 창 위치가 코드와 묶인 세트라 절차적 유지. 백미러 속 눈 연출 그대로

**사용자 리포트 셋** ("의자가 반대 · 기사가 그대로 · 통로가 하차문까지 벽"):
- 좌석 "정면"이 등받이 쪽 → 템플릿 π 회전
- **우측 핸들** — 일본 버스인데 운전석이 왼쪽이었다 → 운전석·핸들·기사 +x 로, 하차문은 앞왼쪽(인도 쪽)
- 하차문이 **벽 위에 붙은 판**이라 열려도 못 내렸다 → 왼쪽 벽을 창 3 + **개구부**(4번째 창 자리를
  바닥까지)로 재구성, 격벽도 운전석 쪽 **절반만** (통로가 문까지 뚫린다)

### 「뛰는데 걷는 것처럼 보인다」 — 보폭을 속도에 잠갔다 (2026-08-21)

사용자 지적: **속도를 올려도 뛰는 것으로 안 보인다.** 맞는 진단이었다. 배속만 올린 건 증상 처리였다.

**원인은 흔들림 주기가 상수였던 것.** `firstPerson.ts` 의 `bobT += dt * (3.0 + run * 8.5)` 에서
`run = speed / topSpeed` 인데 `topSpeed` 가 `3.6 × SPEED_MUL` 이라 전속력이면 언제나 1 이다 —
즉 **배율을 아무리 올려도 걸음 수가 11.5 rad/s(0.273 초에 한 걸음)로 고정**이었다.
×4.68 에서 그건 **보폭 4.6 m** 다. 화면은 조깅 리듬으로 흔들리는데 세상만 흐른다.
(이 상수는 일부러 그렇게 둔 것이었다 — 예전에 배율을 한 번 더 곱했다가 체감이 배로 뛰었다.
그때 문제는 **상한이 없어서**였지, 속도에 물린 것 자체가 아니었다.)

**고친 것 넷** — 전부 「발이 땅을 친다」를 만드는 장치다:

| | 전 | 후 |
|---|---|---|
| **걸음 수** | 상수 3.66/s | **보폭 고정**(2.05 m) → 걸음/초 = 속도/보폭, 상한 **6/s**. 상한에 닿으면 보폭이 늘어난다 = *끌려간다* |
| **상하 파형** | 대칭 사인 | 내려꽂히는 쪽 ×1.35 / 떠오르는 쪽 ×0.72 — 대칭이면 둥둥 뜬다 |
| **착지** | 없음 | 걸음마다 충격(−2.6 cm, 9/s 로 감쇠) + **딛는 발 쪽으로 롤**(±0.016) |
| **화각** | 66° 고정 | 속도에 따라 **66 → 74°**. 1인칭에서 "빠르다"를 만드는 가장 센 신호다 |

숙임도 속도에 얹었다(`lean += run × 0.035`) — 전력으로 달리는 사람은 고개를 세우지 않는다.

**실측** (×4.68 = 16.85 m/s, 결정적 스텝 · 전속력 구간 중앙값)
- 걸음 간격 0.273 → **0.167 초** (3.66 → **5.99 걸음/초**) · 보폭 4.6 → **2.81 m**
- 화각 66.1 → **73.8°**(전속력) → 멈추면 66 으로 복귀
- 좌우 완전 교대 (넘어짐 전후로 한 번 같은 발이 나오는 건 기존 동작 — 넘어진 동안 소리만 죽이고 계수는 돌린다)

**속도 상한의 의미**: `속도 ≤ 6 × 2.05 = 12.3 m/s`(= `SPEED_MUL` 3.42) 까지는 보폭이 **정확히 2.05 m** 로
유지된다 — 완전히 사람의 달리기다. 그 위로는 상한에 걸려 보폭이 늘어난다. 지금(×4.68)은 2.81 m 로
"사람보다 큰 보폭" 구간인데, 이 장면의 전제가 **끌려가고 있다**라 의도와 맞는다.
더 지면에 붙은 느낌을 원하면 `SPEED_MUL` 을 3.42 이하로 내리면 된다.

### 늦은 저녁 — `evening` 프리셋 (2026-08-21)

사용자 지시: **버스에서 내린 뒤 밤까지 가지 말고 늦은 저녁 조도를 유지할 것.**

`dusk`(해가 걸쳐 있다)와 `night`(캄캄하다) 사이에 프리셋을 하나 넣었다. 밤과 다른 건 밝기만이 아니다 —
**저녁의 빛은 주광원이 아니라 하늘 전체에서 온다.** 그래서 달을 세게 하지 않고(1.9) 환경광을 올렸다
(hemi 0.40 → **0.72** · env 0.42 → **0.80** · 노출 0.62 → **0.72** · 안개 0.018 → 0.012).
그림자가 흐리고 어디에도 검은 구멍이 없는, 해가 진 직후의 공기. 서쪽 지평선에만 죽은 장밋빛이 남는다.

이게 **현재 히가사토의 기본 시간대**다:
- ACT 2b 종점 컷신: 오후 3시 → `dusk` → **`evening`** (전에는 `night`)
- `TimeOfDayController` 시작값도 `evening` — `?skip=intro` 로 건너뛰어도 같은 조도
- 요괴 감지·초칭 밸런스는 시간대가 아니라 초칭 레벨에 물려 있어 손대지 않았다

부수 효과가 하나 있는데 오히려 이쪽이 맞다: **ACT 4 의 생활 흔적이 보인다.** 캄캄한 밤에서는
김 나는 찻잔도 젖은 게다도 초칭 반경 3 m 안에 들어와야 존재했다.

### ACT 1 「사요가 끌고 걷는다」 — 끌기를 달리기로 (2026-08-21)

사용자 지적: **처음 컷신에서 사요가 미오를 잡고 걷는다. 너무 느리다.**

원인은 `dragged()` 의 끌기 속도가 **0.9 m/s** 였던 것. 주석에 *"0.9 m/s 는 「질질 끌린다」이지
「이동한다」가 아니다"* 라고 적어 뒀는데, 화면에서는 그게 **걷는 것**으로 보였다.
ACT 1 은 플레이 구간이지만 손을 놓고 보고만 있으면 그대로 장면이 되고 — 그러면 이 장면은
빗속을 **산책하는** 장면이다. 사요는 뛰고 있다. 끌려가는 쪽도 뛰어야 한다.

**고친 것**: 끌기를 「조금 밀어 주는 것」에서 **「모자란 만큼 채우는 것」** 으로 바꿨다.
목표는 최고 속도의 62 %(`TOW`) — 스스로 달리면 더 빠르다는 여지를 남겨야 「뛰어!」와
팔이 늘어나는 연출(`setTug`)에 쓸 폭이 있다.

**⚠️ 여기서 프레임 진동 버그를 하나 잡았다.** 부족분을 `horizontalSpeed`(실제 이동)로 계산했더니
**한 프레임 걸러 10.1 / 0.34 m/s 로 튀었다**(실측). `externalPush` 는 그 프레임의 이동에 바로 더해지고
`horizontalSpeed` 는 *이동 결과*라, 밀어 준 다음 프레임에 "이미 충분하다"고 판단해 밀기를 멈춘다 —
1프레임 지연 피드백 루프다. 평균이 정확히 절반(5.2 m/s)이 되어 있었다.
→ 기준을 **`velocity`(입력이 만든 의도 속도, 밀기의 영향을 안 받는다)** 로 바꿔 해결.

**부수 효과가 컸다.** `FirstPerson` 의 흔들림·발소리·화각은 전부 `horizontalSpeed` 를 본다.
끌려가는 동안 그게 0.3 m/s 로 읽히고 있었으므로 **흔들림도 발소리도 화각도 사실상 꺼져 있었다** —
화면이 그냥 미끄러져 나갔다. 이게 「걷는 것처럼 보인다」의 절반이었다.

**실측** (무입력 = 보고만 있을 때)
| | 전 | 후 |
|---|---|---|
| 끌리는 속도 | 0.9 m/s | **10.45 m/s** |
| 72 m 구간 통과 | 약 80 초 | **약 8.5 초** |
| 걸음 | 사실상 없음 | **5.0 걸음/초** |
| 화각 | 66° 고정 | **70.8°** |

W 를 누르면 그대로 16.85 m/s(중첩 없음, 실측) · 「뛰어!」 재촉은 **스스로 안 달릴 때** 그대로 뜬다
(판정을 속도가 아니라 "손이 일하고 있는가"로 옮겼다 — 23.7 m 에서 발화 확인) ·
비트 거리 0.3 / 8.7 / 17.9 / 27.4 / 33 / 42.3 / 57.1 / 68.9 m 로 전부 유지.

**속도가 마음에 안 들면** `act1.ts` 의 `TOW`(0.62) 한 곳이다.

### ACT 2a 버스 — 창밖을 3D 로, 실내 설비 채우기 (2026-08-21)

사용자 지시: **버스씬에서 아직 모델링 안 된 것을 다 바꿀 것. 창밖 배경도.**

#### 창밖 — 캔버스 실루엣 → 실제 지오메트리 (`world/busOutside.ts` 신규)

전에는 캔버스로 그린 **톱니 실루엣 두 줄을 UV 로 흘렸다**. 멀리서 스치면 통하지만 창가에 앉아
35 초를 보는 장면에서는 벽지가 미끄러지는 것으로 읽힌다 — 나무에 두께가 없고, 물체 사이에
시차가 없고, 무엇보다 *지나가지 않는다*. 앞유리 너머는 아예 **그림 한 장**(정지)이었다.

**컨베이어**: 버스는 서 있고 세상이 움직인다. 물체마다 z 를 하나씩 갖고 매 프레임 뒤로 밀되,
`SPAN`(132 m) 만큼 빠지면 앞으로 되돌린다. 11 m/s 에서 한 바퀴 12 초 — 창 하나를 보는 시간보다 길다.

시차는 **배율이 아니라 거리**가 만든다. 길가·나무·논은 전부 배율 1 이고, 가까운 것이 빨리
스쳐 갈 뿐이다. 능선만 0.10 / 0.04 로 따로 두는데, 진짜 산은 몇 km 밖이라 그 거리를 세트로
만들 수 없기 때문이다.

**무엇을 세웠나** (177 오브젝트):
- 아스팔트 노면 + 갓길 자갈 + **중앙선**(끊긴 흰 선 — 속도를 가장 정직하게 말해 주는 물건)
- 가드레일(구간마다 **끊긴다** — 이어지면 벽이 된다) · 전신주(전선이 **카테너리로 처진다**) · 갓길 덤불
- 삼나무 두 줄 — 앞줄은 크고 성기게(창을 순간적으로 가린다), 뒷줄은 작고 빽빽하게(그 너머를 막는다)
- **논** — 물을 댄 배미 + 흙둑. 창밖에서 **유일하게 하늘을 비추는 밝은 면**이라 이게 있어야 시골이 된다
- 커브 반사경(주황 기둥) · 함석 농막 — 나무와 가드레일만 있으면 어느 나라 산길이든 될 수 있다
- 원경 능선 두 겹 — 판이 아니라 **굴곡을 가진 메시**. 안개가 색을 먹어 대기 원근이 생긴다

#### 실내 — 빠져 있던 설비 넷 (Tripo 신규)

기사 주변이 비어 있으면 그건 버스가 아니라 좌석이 놓인 방이다.
`bus-farebox`(요금함) · `bus-ticket`(정리권 발권기) · `bus-faredisplay`(운임 표시기) · `bus-dash`(대시보드).
좌석·손잡이와 같은 방식으로 **자리표시자를 먼저 세우고 GLB 가 오면 갈아끼운다** —
하나가 실패해도 나머지는 바뀐다(`Promise.allSettled`).

창밖 전용으로 `utility-pole`·`guardrail` 도 새로 뽑았다(프리미티브 폴백 유지).

#### 그 밖에 채운 것
- **창유리** — 지금까지 창은 *뚫린 구멍*이었다. 옅은 유리(불투명도 0.13)를 끼워 「창밖」과 「창」을 분리했다
- **백미러** — 검은 박스 하나였다. 테두리 + 비치는 면(금속성) + 천장에서 내려온 팔로 다시 만들었다.
  이 ACT 의 핵심 연출(눈 두 개)이 들어앉는 물건이라 판때기로 두면 안 된다

#### ⚠️ 잡은 것
| 증상 | 원인 |
|---|---|
| 원경 능선이 **도시 스카이라인**이었다 | 구간마다 독립된 사각형을 세웠다 → 꼭짓점 높이를 먼저 뽑고 이웃끼리 잇는 연속 프로파일로 |
| 논에 **물이 안 보이고 흙둑만** 떠 있었다 | 논이 낮다고 수면을 −0.28 에 뒀는데 들판(−0.06)에 묻혔다 → 수면을 들판 위(+0.02)로 |
| 자리표시자를 버리자 **남은 나무들이 통째로 깨졌다**(`isReady` 예외) | `disposeTree` 가 **공유 재질**까지 dispose 했다. 지오메트리만 버린다 |
| GLB 로 갈았더니 **전선이 같이 사라졌다** | 전신주 그룹을 통째로 교체했는데 전선이 그 안에 있었다 → 교체 단위를 **소켓**(그룹의 자식)으로. 소켓 밖에 둔 것은 무엇으로 갈아도 남는다 |
| 신규 프랍이 **8~9 MB · 140만 tris** 로 나왔다 | `build-props.ts` 에 `--simplify` 를 안 넘기면 단순화가 **통째로 생략**된다(기본 0). `--simplify 0.006 --error 0.012` → 230~380 KB · 8.5 k tris. 배경에서 24개씩 복제되는 가드레일·전신주는 `0.002 / 0.035 / tex 512` → **135 KB · 2.8 k tris** |

**검증**: 컨베이어 초당 이동 10.89 m(버스 속도 10.95) · SPAN 경계에서 −121.11 로 정확히 감김 ·
좌석에서 좌측 창·앞유리 양쪽 렌더 확인(논·능선·중앙선·가드레일·전신주+전선) ·
GLB 6종 교체 확인(소켓당 메시 1, 전신주 아이템에 전선 2 잔존) · 콘솔 예외 0 ·
좌석 시점 렌더 **1.03 ms/frame**. Tripo 크레딧 235 → 115.

### 공물 7종 — 발광 구슬에서 실물로 (2026-08-21)

사용자 지시: **공물부터 바꿀 것.**

지금까지 공물은 전부 **나무 받침 + 발광 20면체**였다(`rules.ts`). 색만 달라서 붉은 방울도
머리빗도 동전도 게다도 거울도 봉인패도 **화면에서는 같은 구슬**이었다 — 토스트는
「붉은 방울을 손에 넣었다」라고 말하는데 손에 든 건 구슬이다. 게임의 수집 대상 **전부**가
자리표시자였고, 각 공물이 원혼의 미련과 1:1로 묶이는 구조(§1.1)라 생김새가 곧 서사다.

**Tripo 신규 6종** — `offer-suzu`(붉은 끈 달린 놋쇠 방울) · `offer-kushi`(붉은 옻칠 나무 빗,
이가 몇 개 빠졌다) · `offer-coins`(구멍 뚫린 동전 세 닢을 끈으로 묶음) · `offer-geta`(아이 크기
나막신 한 짝) · `offer-kagami`(청동 손거울, 한 조각이 없다) · `offer-fuda`(봉인패 + 두루마리를
짚끈으로 묶음). 일곱 번째(사요)는 아이템이 아니므로 없다.

**배선**: `OfferingDef` 에 `model`·`size` 추가. 원본은 `Rules.prototype()` 이 **한 번만** 로드해
캐시하고, 획득 자리와 봉납 받침대가 같은 것을 복제해 쓴다(`cloneModel`). 좌석·손잡이와 같은
방식으로 **자리표시자를 먼저 세우고 도착하면 교체** — GLB 가 없으면 구슬이 그대로 남는다
(실제로 확인: 모델 404 상태에서 게임이 그대로 돈다).

**⚠️ 크기 함정**: `normalize()` 는 **높이** 기준이라 눕는 물건이 커진다 — 게다를 높이 17 cm 로
맞췄더니 **길이가 30 cm**, 빗이 **21 cm** 가 됐다(실측). 손에 드는 물건은 "가장 긴 곳"이 곧 크기다.
`size` 를 **가장 긴 변**으로 재정의하고 로드 뒤 한 번 더 맞춘다(원점이 바닥이라 스케일을 곱해도 바닥은 그대로).
결과(실측): 방울 12 · 빗 12 · 동전 9 · 게다 18 · 거울 20 · 봉인패 28 cm — 전부 목표치와 일치.

**연출을 하나 바꿨다**: 실물은 **떠서 돌지 않는다.** 받침대 위에 놓이고 아주 느리게만 돈다
(0.35 rad/s). 이 게임에서 공물은 전리품이 아니라 **누가 놓고 간 물건**이다.
대신 찾게 만드는 일은 옆의 **표식 불빛**이 맡는다 — 실물에는 자체 발광이 없으므로
그 맥동이 유일한 눈길이다. 봉납된 공물 옆에도 작은 불빛이 남아 멀리서 개수를 셀 수 있다.

### 시간대 시스템 (2026-08-20) `world/timeOfDay.ts`

사용자 요청: "배경 조도도 상황에 따라 낮/밤/새벽/저녁을 구분해달라".

스토리보드는 시간대를 계속 옮겨 다닌다 — 10년 전 **비 오는 밤**(ACT 1) → **저녁**(16-1 오후 5시 10분) →
**한밤**(16-3 밤 11시 47분) → **붉은 달**(24 피안의 밤) → **새벽**(21·33) → **봄 낮**(34).
장면마다 조명값을 손으로 만지면 톤이 어긋나므로 **한 곳에 정의하고 이름으로 부른다.**

프리셋 하나가 다섯 층을 한꺼번에 정한다: ① 하늘 돔 색(천정·지평선·지면) ② 주광원 고도·방위·세기·색
③ 환경광(hemi/env) ④ 안개 색·밀도 ⑤ 톤매핑·채도·대비·비네트 (+ 논 안개층).

| 이름 | 쓰이는 곳 | 성격 |
|---|---|---|
| `rainNight` | ACT 1 | 구름이 달을 먹었다. 별 거의 없음, 안개 0.030 |
| `night` | 현재의 히가사토(기본) | 달·별이 있고 논에 반사된다 |
| `dusk` | ACT 16-1 | 서쪽만 주황, 동쪽은 벌써 남색. 마지막으로 평화로웠던 시간 |
| `dawn` | ACT 21·33 | 푸른 시간(blue hour) + 가장 두꺼운 물안개 |
| `day` | ACT 34 에필로그 | 이 게임에서 유일하게 밝고 색이 있는 장면 |
| `bloodMoon` | ACT 24 | 거대한 붉은 달, **별이 없다**(하늘이 이미 저쪽이다) |

전환은 **보간**된다(조명이 한 프레임에 튀면 컷이 잘린 것처럼 보인다). 하늘 굽기(PMREM)는 비싸므로
보간 중에는 6 Hz 로만 다시 굽고 끝날 때 한 번 확실히 굽는다. 값은 `settings.night` 를 경유하므로
기존 튜닝 패널(H)과 어긋나지 않는다.

**검증**: dusk·dawn·day·bloodMoon·night 모두 인게임 확인. 55~60 fps.

### ⚠️ 요괴 전면 비활성 (2026-08-20, 사용자 지시)

"내가 다시 얘기할 때까지 귀신·요괴는 안 나오게" — **숨기는 것으로는 부족했다.**
`Scares.load()` 가 놋페라보를 **로드 시점에 씬에 세워 둔다**(`scene.add(wrap)`). 갱신을 멈춰도
광장에 계속 서 있어서 프롤로그 도중에도 보였다(사용자 리포트: "아직도 act1에서 귀신이 나와").

→ `YOKAI_ON` 상수로 **생성 자체를 건너뛴다**: 팔척귀신·여우 요괴·도로타보·Scares(놋페라보·초칭오바케·
움직이는 지장·로쿠로쿠비) 전부. 되살리려면 `?yokai=on`.
나브그리드·Senses·Matsuri 는 그대로 만든다(시각물이 없고 소금·돌 던지기가 Senses 를 쓴다).
까마귀는 **요괴가 아니라 새**라서 남겨 뒀다 — 빼야 하면 말씀해 주시면 같이 끕니다.
**검증**: `hunters 0 · dorotabo false · scares false`.

### 프롤로그 구조 (`story/prologue.ts`)
- **ACT 1** 1인칭(어린 미오 눈높이 1.05 m, 각색 8) — 빗속 참배로 질주, 뒤에서 겹치는 주민 목소리, 「보지 마!」에 시선이 앞으로 꺾이고, 앞에 피안화 길과 도리이. 모든 소리가 끊기고 **댕—**(범종 합성: 86/86.6 Hz 맥놀이) → 암전 위 사요의 금기 → 타이틀 《彼岸花》
- **ACT 2** 3인칭 — 미오를 정류장에 실제로 세우고(초칭이 유일한 광원) 카메라가 돌아 나가며 "도로가 있어야 할 자리"를 보여준다 → 금줄 게이트 안쪽에서 플레이 시작
- 신규: `world/rain.ts`(LineSegments 650방울, 드로우콜 1) · `sfx.setRain()` · `sfx.bell()` · 타이틀 카드
- 버그 수정: 자막이 페이드 암전(z 40)에 묻혀 프롤로그 핵심 대사가 안 보였다 → 자막 z-index 42
- `?skip=intro` 로 건너뛴다

## S1 구현 노트 — 스토리 구조물·7공물 (2026-08-20, 1차)

**맵**: 갈래길 5개 끝에 스토리 목적지를 실제로 세웠다 (§2.1 그대로).
- `ground.ts` — 평탄화 패드 시스템 일반화: `STORY_PADS` 5곳(폐교·여관·저택·사당·우물), y 미지정 시 원지형 평균으로 자동 산출(산중턱 부지는 선반처럼 깎인다). 삼나무 식재가 부지를 피해 가는 `padClear()`
- `kit.ts` — shrine.ts 의 parts/box/collide/맞배지붕 패턴을 공용 빌더로 추출 + 캔버스 텍스트 텍스처
- **본품질**: `tablet.ts` 돌비석(이끼→닦음→三禁→세 번째 문장 얼룩, 캔버스 3상태) · `hokora.ts` 오래된 사당(실내 4.2m·대들보=로쿠로쿠비 대비·촛불·끊어진 금줄) · `pedestals.ts` 받침대 7 반원+석판(봉납 시 하나씩 채워지고 일곱 번째는 영원히 빈다)
- **블록아웃**: `blockouts.ts` 폐교(15×7, 문패 彼ヶ里小学校)·폐여관(旅館 ひがん荘)·촌장 저택 셸(출입 가능, 실내는 S2~S3 에서 전용 모듈로 승격) + 공동우물 + 버스 종점(금줄 게이트 너머 — 닿을 수 없는 "온 길")

**규칙**: `rules.ts` 전면 재작성 — 신도 5공물 → **스토리 7공물**(각색 1). 순서 게이팅(방울→머리빗→동전→{게다·거울}→봉인패, 각색 5), **운반 상태 = 감지 ×1.5**(§3.2), 봉납 수 = 긴장 곡선 입력(§1.2), 사망 = 강탈(들던 것만 원위치·봉납 유지, §6.1~6.2), **봉인패는 받침대가 거부 → UI 3단 변조 → 1부 종료 카드**(ACT 17 캡스톤). 위치 랜덤화는 폐기(스토리 고정).

**스토리 플로우**(S0 시스템 첫 실전): `inspect.ts` 조사 레지스트리 — 비석(三禁 낭독→「미오야.」→강제 위반 answered=1→얼룩) → 석판(퀘스트 개시「7개의 공물을 찾아라」) → 사당 획득(기억 파편 대사) → 봉납(「……하나.」 카운팅 + 단계별 월드 문구 + 봉납 3 방송 충돌 + 봉납 4 여우 하강 + 체크포인트).

**검증**: tsc 통과 · 비석→석판→방울 획득→운반→봉납→게이팅(머리빗 개방)→봉인패 거부→글리치→종료 카드까지 전 구간 인게임 확인. 미검증: 셸 3동 실내 근접 확인, 파수꾼이 새 부지를 순찰에서 어떻게 다루는지(내브그리드 재확인 필요).

## S0 구현 노트 (2026-08-20) `src/story/`

- `flags.ts` — 엔딩 분기 플래그(§7.1) + `StorySave`(localStorage 체크포인트, 시크릿 모드 조용한 실패). 봉납 시 자동 저장 연결, 재개 흐름은 스토리 챕터에서
- `dialogue.ts` — 화자색·타자기 자막. 시퀀서와 필드 연출(A등급)이 같은 창구를 쓴다. `say()` 는 Promise — 스크립트에서 await 가능
- `quests.ts` — 목표 문구 3보이스(gm 명조 = 히간누시 / sayo 손글씨 = 사요 / none) + `glitchTo()`(문자 스크램블 → 정착, DOM 텍스트라 셰이더 없이 처리). 서체는 Google Fonts(Noto Serif KR·Nanum Pen Script), 오프라인 폴백 있음
- `sequencer.ts` — B등급 컷신: 카메라 카트멀롬(위치·시선·FOV), 자막/페이드/fn 이벤트, 레터박스, **Space 홀드 0.7 s 스킵**(남은 fn 을 전부 실행 — fn 은 멱등이어야 한다), **DEV 스크러버**(일시정지·타임 슬라이더·배속 — 스크럽은 카메라 전용, 이벤트는 전진 재생만)
- `demo.ts` — T 키(debug): 참배로 상공 스윕 + 화자 자막 + 글리치 + 사요체 미리보기. 시퀀스 종료 시 `renderHud()` 가 목표 복원
- main.ts 통합: 시퀀스 중 이동·점프·상호작용·카메라 차단(`cine`), HUD 숨김(`#hud.cine`), 죽음 카운터·봉납 체크포인트 연결
- 검증: tsc 통과 · 데모 풀 재생(레터박스/자막/글리치/사요체/페이드 잔류 없음) · 세이브 라운드트립. **주의: 브라우저 탭이 hidden 이면 rAF 가 멈춘다** — 헤드리스 검증은 `__dbg.step(dt)` 수동 스텝으로

## ACT 5 작업 계획 — 5 페이즈 (2026-08-22)

> 목표: **프레임부(1~5)를 닫는다.** ACT 5 는 아직 S1 자리표시자(석판 E 한 번 = 대사 4줄)다.
> 여기에 각색 6 **C안**(초칭 단독)이 ACT 2·4 에 떨구는 몫과, 튜토리얼 표가 약속해 놓고 안 지킨
> 「사진 확인」을 같은 묶음으로 넣는다 — 따로 하면 같은 파일을 두 번 만진다.
>
> 각 페이즈는 **그 자체로 플레이해 볼 수 있는 상태**로 끝난다.

### 페이즈 순서의 근거

ACT 5 는 「밤의 신사에 도착하는 장면」인데, **플레이어가 어떤 빛을 들고 도착하는지**가 안 정해져
있으면 그 장면의 조도·연출을 잡을 수 없다. 그래서 광원 재배치(P1)가 먼저다.
P2(사진 인벤)는 P4 의 석판과 **같은 창구**(`inspect` + 인벤)를 쓰므로 그 앞에 둔다.
P3(본전 문)은 ACT 5 의 물건이지만 **1부 전체가 읽는 계기판**이라 석판(P4)보다 먼저 세운다.

---

### P1 — 손이 비어 있는 시작 (ACT 2 · ACT 4)

각색 6 을 **C안**으로 확정한다: 손전등은 만들지 않는다. 초칭 하나로 가되 **처음부터 들고
있지는 않는다**. 「전기가 죽는다」는 광원이 아니라 **폰**이 가져간다 — 라이트도 셰이더도 안 늘어난다.

**P1-1. 폰** (`src/story/phone.ts` 신규 · `act2.ts` 비트 추가)
- 광원이 **아니다**. HUD 오버레이 한 장(DOM)이고 씬에는 아무것도 안 넣는다.
- 3연타로 규칙 한 줄을 세운다:
  ① 버스 안 — 시각 확인 **15:04**
  ② 언니에게 전화 → 「신호가 없습니다」
  ③ 마을 진입 후 다시 보면 **시계가 15:04 에서 안 움직인다**
- ②는 흔하다(산골이니까). ③이 규칙이다 — **전기로 된 것이 이 마을에서 작동하지 않는다.**
  그 한 줄이 서야 종이등이 유일한 빛인 게 설명된다.
- ③의 발화는 `act4.ts` 의 마을 진입 상자(`VILLAGE`)를 재사용한다. 방송과 겹치지 않게
  `ready()` 로 미룬다(ACT 3·4 가 이미 쓰는 방식).

**P1-2. 초칭 획득** (`act4.ts` · `world/higasato/minka.ts` · `light/chochin.ts`)
- 자리는 **민가 처마 초칭**(`minka.ts:113`). 광장의 초칭 줄(`matsuri.ts:28`)이 아니다 —
  줄은 광장이라 늦게 지나가고, 처마는 **손이 닿는 높이**에 남의 집 물건이다.
- 「[E] 초칭을 든다」 (hold 없음). 떼는 순간 처마에서 하나가 사라지고 미오 손에 켜진다.
- 대사 두 줄. 「……빌릴게요.」 / 「돌려줄 사람이 없다는 건 알고 있다.」
  — 마을 상태를 설명하는 문장을 여기서 한 번만 쓴다.
- 획득 직후 **Q 튜토리얼(빛 3단)이 처음 뜬다.** 지금은 시작부터 들고 있어서 가르칠 자리가 없었다.
- 코드: `Chochin.held` 플래그. 미소지면 메시·라이트 숨김 + `detectionMul` 을 **끔(0.6) 고정**.
  `main.ts` 는 지금처럼 생성하되 `setHeld(false)` 로 시작한다(초칭 시스템 코드는 100 % 유지).

**P1-3. 그 전까지 걸을 수 있는가** (`world/timeOfDay.ts`)
- ACT 2~4 는 `evening`. 초칭 없이 버스 종점 → 흙길 → 비석 → 마을 초입을 **길이 보이는 채로**
  걸을 수 있어야 한다. 참배로 노면 조도를 실측해 모자라면 `evening` 을 조금 늦추거나
  획득 지점을 마을 초입 쪽으로 당긴다.

**검증 결과 (2026-08-22 · ✅ 구현 완료)**

| 항목 | 결과 |
|---|---|
| 시작 상태 | 프리워밍 8 프레임 뒤 `held false` · `level 2` 대기 · `detectionMul` **0.6 고정** · 메시 숨김 |
| 무광원 통행 | `evening` 에서 종점 → 마을 초입 **참배로·도리이·지장·민가 전부 판독 가능**(인게임 스크린샷) |
| 폰 ③ | ACT 4 t **20.6 s 표시 → 28.0 s 숨김** — 비트 표와 정확히 일치. 자막 「……아직 세 시 사 분?」 |
| 획득 | 처마 등불 소멸 + **그 집이 실제로 어두워짐** · `held true` · `detectionMul` 3.0 · 플래그·체크포인트 |
| 손 위치 | 초칭 ↔ `R_Hand` **0.226 m** — `gripPos` 주석의 22.6 cm 그대로. v3.8(팔 길이 변경) 회귀 없음 |
| Q 게이트 | 미소지 중 `KeyQ` 무시(`chochin?.held`) |

**미검증**: ACT 2 의 폰 비트 ①②(버스 안). 비트 표는 ACT 4 와 **같은 기구**이고 그쪽이 초 단위로
맞았지만, 100 초짜리 프롤로그를 실시간으로 통과시켜 눈으로 본 적은 없다 —
이 폴더에 다른 세션의 개발 서버가 5 개 떠 있어 브라우저 패널이 간헐적으로 뷰포트 0 을 보고하고,
그때 로딩 중 씬 렌더(`preparePhoto`)가 0 크기 프레임버퍼로 실패해 95 % 에서 멈춘다.

---

### P2 — 사진을 손에 쥔다 (ACT 2 말미 ~ ACT 3)

튜토리얼 표(§4.2)는 ACT 2~3 의 학습 목표를 「사진 확인(인벤 튜토리얼)」로 잡아뒀는데
**가족사진이 아이템이 아니다** — 인벤에는 `sword` 와 `apple` 뿐이다(`items/items.ts`).

- `items.ts` 에 `photo` 추가. 무기도 소모품도 아니므로 **「기록물」 종류**가 필요하다
  (뒤에 명부·일기·문서가 전부 이 종류로 들어온다 — 지금 만들어 둔다).
- ACT 2 사진 시퀀스 끝(`story/photo.ts`)에서 `inventory.add('photo')` + I 튜토리얼 1 회.
- 「사진 확인」은 **이미 있는 것을 다시 띄우면 된다** — `preparePhoto()` 가 실제 씬에서 찍은
  텍스처를 만들어 두므로 전체화면으로 여는 뷰어만 붙인다.
- 왜 인벤에 있어야 하나: ACT 30 에서 **얼룩이 걷히며 사요의 얼굴이 나오는 것**이 같은 물건을
  다시 여는 연출이다. 그때 「어디서 꺼내는가」가 정해져 있어야 한다.

**검증 결과 (2026-08-22 · ✅ 구현 완료)** — 인벤 여는 키는 **Tab** 이다(I 아님, 기존 `InventoryUI`).

| 항목 | 결과 |
|---|---|
| 획득 | ACT 2b 「사진을 가방에 넣고」 t 6.0 에서 `give('photo')` · 토스트 「가족사진을 가방에 넣었다 · Tab 으로 열어 볼 수 있다」 |
| 프롤로그 스킵 | `?skip=intro` 에서도 로케 촬영 + 지급 — 가방 안이 같아야 한다 |
| 열람 | Tab → 🖼️ 클릭 → 뷰어. 앞면 = **로케 촬영본**(도리이 앞·사요 얼굴 얼룩) |
| 뒤집기 | 클릭 → 뒷면 「彼ヶ里 彼岸祭 / 미오 여섯 살 / 사요 열여섯」 — **사요의 줄 위에도 얼룩** |
| ACT 30 | `photoViewer.show(0)` → **얼룩이 걷히고 사요의 얼굴이 나온다**(인게임 확인) |
| 중복 | `give` 가 `has()` 로 막는다 — 인벤은 localStorage 라 재시작하면 2 장이 됐다 |

**잡은 것 둘**
- `.pv-card` 의 `filter: drop-shadow` 가 **3D 렌더링 컨텍스트를 평탄화**해서, 뒤집으면 뒷면이 아니라
  **앞면이 좌우로 뒤집혀** 나왔다 → 그림자를 면의 `box-shadow` 로 옮겼다
- 「카드=뒤집기 · 바깥=닫기」로 뒀더니 **뒤집힌 상태의 클릭이 창을 닫았다** —
  `backface-visibility: hidden` 인 면은 히트 테스트에서도 빠져 클릭이 배경으로 떨어진다 →
  판정을 루트 하나로 합쳐 **어디를 눌러도 뒤집고, 닫기는 Esc 뿐**으로

---

### P3 — 신사 경내: 1부의 계기판 (`world/village/shrine.ts` · `audio/sfx.ts`)

**P3-1. 본전 문**
- 지금 본전은 문이 없는 흰 상자다(`shrine.ts:68`, `box(3.0, 2.6, 2.2, …)`).
  정면(남쪽)에 **두 짝 문 + 가로지른 금줄**을 넣는다. 금줄이 봉인의 시각화다.
- **5 단계**로 만들고 `rules.onOffer` 에 물린다:

  | stage | 계기 | 무엇이 보이나 |
  |---|---|---|
  | 0 | 시작 | 굳게 잠김. 금줄 온전 |
  | 1 | 봉납 2 | 안에서 긁는 소리. 금줄 한 가닥 삭음 |
  | 2 | 봉납 4 | 문 틈 2 cm |
  | 3 | 봉납 4 이후 | 그 틈에 **흰 손** |
  | 4 | ACT 17 | 개방 |

- ACT 5 에서 만드는 건 **stage 0 하나**다. 나머지는 값만 올리면 되게 API 를 낸다
  (`shrine.honden.setStage(n)`).
- 「[E] 문을 밀어본다」는 **꾹 누르기**(`inspect.hold` 1.2 s)다. 한 번의 E 로 「잠겨 있다」
  자막이 뜨면 그건 정보고, 게이지가 **끝까지 찼는데 안 열리는 건 경험**이다.
  게이지 만충 → 문이 2 cm 흔들리고 멈춘다 → 「안에서 잠겨 있다.」

**P3-2. 마루 밑 웃음소리**
- `sfx.underfloorLaugh(x, y, z)` — `panAt()` 재사용(ACT 3·4 에서 만든 창구).
- 위치는 **배전 마루 밑**: `shrine.altar` 에서 북쪽 2 m, y = 지면 + 0.3.
  이 좌표가 곧 **ACT 18 지하 입구**다 — 지금 정해 두면 나중에 어긋나지 않는다.
- 음색 셋: ① 저역만(마루가 고역을 먹는다) ② 0.4 초 ③ **한 번만**.
  두 번이면 유령의 소리고, 한 번이면 잘못 들은 것이다. 이 장면은 후자여야 한다.

**검증 결과 (2026-08-22 · ✅ 구현 완료)**

`world/village/hondenDoor.ts` 신규 · `shrine.ts` 에 석단 계단 4 단 + 콜라이더 분리 ·
`sfx.underfloorLaugh` / `sfx.doorPush` 신규.

| 항목 | 결과 |
|---|---|
| 석단 접근 | 예전엔 석단까지 **한 덩어리 콜라이더**라 본전에 다가갈 수조차 없었다 → 몸통만 막고 정면에 계단 4 단(0.35 m = 오토스텝 한계). 실측 **y 9.30 → 11.08** 로 걸어 올라간다 |
| 조사 | 「[E] 본전 문을 밀어본다」. 판정 중심을 문에서 1.1 → **0.6 m** 로(석단 턱이 0.4 m 뿐이라 실제로 서는 곳은 맨 윗단이다) |
| stage 0 | 두 짝 · 가로 띠 3 · 금줄 · 시데 4 · 문고리 2 |
| stage 2 | 시데 2 · **이음매가 검은 세로선으로 읽힌다**(2 cm) |
| stage 3 | 시데 1 · 흰 손 페이드 최대 0.55 (두 느린 주기의 곱 — 규칙이 안 읽힌다) |
| 배선 | `rules.onOffer` → `setStage` · `onCreak` → `doorPush` · 석판 조사 → **소리 먼저, 0.7 s 뒤 자막** |

**⚠️ 테스트 조건을 틀리면 판단이 틀린다.** 조명 없이 찍은 첫 컷에서 문이 새까매
「2 cm 틈이 안 보인다」고 결론 내렸다. **초칭을 들고 문 앞에 서면 전혀 다르다** — 널문의 가로 띠,
놋쇠 문고리, 금줄이 다 살아나고 이음매가 선명한 검은 선이 된다. 이 문은 등불 앞에서만
존재하는 물건이라, 검증 장면에 등불이 없으면 잘못된 수정을 하게 된다(문 색을 밝힐 뻔했다).

**미검증**: 흰 손을 문 **앞**으로 옮긴 뒤의 그림(뒤에 두니 2 cm 슬릿 너머라 정면에서도 안 보였다 →
문짝 모서리를 잡는 손으로 재배치, stage 3 의 틈도 4.5 cm 로) · stage 4(개방) · 미는 동안의
들썩임·삐걱임 · 마루 밑 웃음소리 청감. 이유는 P1 과 같다 — 이 폴더에 다른 세션 개발 서버가
5 개라 로딩이 95 % 에서 40 초 이상 걸리고 패널이 간헐적으로 뷰포트 0 을 보고한다.

---

### P4 — 석판: UI 가 처음 뜨는 순간 (`story/act5.ts` 신규 · `story/quests.ts`)

등급 A(조작 유지). 대신 **빛과 소리로** 연출한다.

1. **접근** — 받침대 반원 안(반경 4 m)에 들어서면 표식 7 개가 **서→동으로 0.18 초 간격**으로
   켜진다. **일곱 번째는 안 켜진다.** 그 공백을 여기서 처음 보여준다(§4.3 의 복선).
2. 「[E] 석판을 읽는다」
3. 제문 3 줄. 낭독하는 동안 `pedestals.altarLight` 가 서서히 오른다.
4. **퀘스트 UI 첫 등장** — 페이드인이 아니라 `Quests.glitchTo()` 의 **역방향**이다.
   글리프가 모여 문장이 된다: 「7개의 공물을 찾아라 0/7」
   - 왜: 이 UI 는 히간누시다. 처음부터 얌전히 나타나면 ACT 23 의 반전이 안 산다.
     **등장부터 이미 이상해야** 한다. 단 플레이어는 이걸 「멋있는 연출」로 읽는다 —
     그게 함정의 조건이다.
   - 신규 `Quests.assemble(html, voice, dur)`. `glitchTo` 의 보간을 방향만 뒤집어 재사용한다.
5. 목표 목록이 붙는다(붉은 방울만 활성, 나머지 회색 — HUD 에 이미 있다).
6. **마루 밑 웃음소리**(P3-2). 자막은 지금 것 그대로.
7. `rules.begin()` · `storyFlags.chapter = 'act06'` · 체크포인트.

**검증** — 표식 점등 순서와 **7 번째 미점등** · UI 조립 애니 · `rules.begin()` 후 방울 목표 활성 ·
체크포인트 저장/로드 재개

---

### P5 — 통합 검증 + 회귀

- **프롤로그 ~ ACT 5 통짜** 결정적 재생(`__dbg.step`). ACT 전환 시각 실측, 자막 겹침 0.
- 세이브/로드: `act05` 체크포인트에서 재개.
- ⚠️ **v3.8 회귀 점검** — 클립의 position 트랙을 버리면서 **팔 길이가 바뀌었다**.
  손에 붙는 것들을 전부 다시 봐야 한다:
  - `light/chochin.ts` `gripPos` (등불이 손바닥에 있는가)
  - `world/bus.ts` 버스 **손잡이**를 잡는 손 (ACT 2a)
  - `story/photo.ts` 사진을 **쥐는 포즈**
  - `character/equipment.ts` 무기 마운트(샌드박스 전용이지만 같은 원인)
- 성능: 신사 경내·마을 한복판 실렌더 ms (ACT 3·4 때 3.43 ms/frame 이 기준선)

---

### 이 계획에 **넣지 않은** 것

- 기사 목소리 실제 샘플 · 버스가 떠나는 것을 3인칭으로 (ACT 2 미구현 목록, 급하지 않다)
- 셸 3동 실내 근접 확인 · 파수꾼 순찰 나브그리드 재확인 — **요괴가 전면 비활성**이라
  (`main.ts:239`, `?yokai=on`) 지금은 검증 자체가 불가능하다. ACT 6~7 이 로쿠로쿠비 첫 추격이므로
  **그 전에 요괴를 다시 켤지 정해야 한다.**
- ACT 1 의 「마우스로 뒤돌아볼 때의 저항감·오디오 청감」 — 스크립트로 못 잰다. 사람이 해야 한다.

---

## 변경 이력
- v4.2 (2026-08-22) **폰 위의 자막 · 가족사진의 언니** (사용자 리포트 2건)
  - **자막이 폰을 가로질렀다** — 폰은 화면 오른쪽(left 57 %, 세로로 화면의 45 %)에 서고 자막은 가운데 정렬 86vw 라, 실측 670×663 창에서 가로 156 px · 세로 49 px 가 겹쳤다. 게다가 폰 z-index 43 > 자막 42 라 **자막 글자가 폰 뒤로 잘려 들어갔다**. `Phone.show/hide` 가 `body.phone-up` 을 토글하고, 그동안만 자막을 왼쪽으로 비켜 세운다(left 28 % · 최대 46vw). 검증: 1280×720 에서 간격 108 px, 670×663 에서 30 px, 겹침 0
  - **사진 속 언니를 사요 모델로 다시 찍는다** — 예전엔 미오 모델을 두 번 렌더해 언니 자리에 세웠다(「자매가 닮았다」는 핑계였지만 사요 모델이 없었다). 이제 언니는 언니로 찍는다: 사요(1.49 m) + 어린 미오(미오 모델 **0.71 배** = 1.15 m, 여섯 살). 물얼룩도 사요의 얼굴 좌표에서 낸다 — **ACT 30 에서 얼룩이 걷히며 드러나는 얼굴이 그때부터 진짜 사요의 얼굴**이다. 프롤로그를 건너뛰어도 같은 사진이 나오게 사요를 읽고 촬영 직후 정리한다
  - **인벤토리 아이콘 = 손에 쥔 그 물건** — 캔버스 사진을 줄여 넣었더니 「이전 느낌 그대로」였다(사용자). 그럴 수밖에 — 가방에 든 것은 도판이 아니라 **쥐는 물건**이고 그 물건은 이미 모델로 있다(`props/photo-hands.glb`). 정면에서 직교 카메라로 한 컷 찍어 정사각으로 자른다(`photoThumbFromModel`). 정면 축은 **바운딩 박스의 가장 얇은 축**에서 찾는다 — 소품 규약(+Z 앞면)에 기대면 소품 교체 때 먼저 깨진다. 배경은 투명, 크롭 0.55 + 위로 6 %(모델 전체를 담으면 아래 4분의 1 이 손 잘린 빈자리가 된다)
  - ⚠️ **그 모델 텍스처에는 언니의 얼굴이 그대로 있다** (실측). 아이템 설명(「언니의 얼굴만 물에 번진 것처럼 지워져 있다」)과 ACT 30 의 복원이 그 얼굴에 걸려 있으므로 아이콘 위에 얼룩을 얹는다. `damaged` 로 끄면 그대로 드러나므로 ACT 30 은 이 인자만 0 으로 주면 된다
  - 얼룩 좌표는 **모델 기준**(`planeSize` 배)으로 잡는다 — 화면 비율로 박아 두면 크롭을 조일 때마다 얼굴에서 벗어난다(실제로 손을 잘라내려 조였더니 10 % 어긋났다). 위치는 머리카락 픽셀 검출로는 배경(처마·나무)까지 물어 실패했고, **얼굴을 4 배로 확대해 눈으로 찍었다**(0.46 배 크롭에서 화면 44 % · 22 %)
  - **뷰어 앞면은 원본 사진 한 장** (`public/textures/photo-front.webp`, 1400×1005, 153 KB) — 사용자가 원판 이미지를 직접 주었다. 회색 배경만 잘라내고 인화지의 말린 가장자리는 남겼다. 카드 비율도 사진에 맞췄다(1400/1005 = 1.39 — 3:2 로 두면 인화지가 눌린다. 뒷면 연필 글씨만 8 % 늘어나는데 손글씨라 티가 안 난다). 얼룩 좌표는 원판에 10 % 격자를 얹어 눈으로 읽었다: 언니 얼굴 (0.445, 0.245), 반지름 5.2 %. **두 겹**으로 얹는다 — 원판이 밝아서 한 겹으로는 얼굴이 비쳐 「지워졌다」가 아니라 「덧칠했다」가 된다
  - (이전 시도) **뷰어도 같은 모델에서 뜬다** — 아이콘만 바꿨더니 「클릭하면 옛날 사진이 나온다」는 리포트. 가방 속 그림과 펼친 그림이 다른 물건일 수는 없다. `captureModelPhoto()` 가 모델을 **손까지 통째로** 정면에서 담아 3:2 원판(768×512)을 만들고, 뷰어가 그걸 앞면에 쓴다(뒷면 연필 글씨는 그대로 캔버스). 원판은 **깨끗하게** 보관하고 얼룩은 훼손도별로 얹어 캐시한다 — ACT 30 은 `show(0)` 이면 같은 원판이 얼룩 없이 나온다
  - 한 번은 **종이만** 잘라내 봤다(격자 레이캐스트로 면이 정확히 2 개 뚫리는 칸 = 가려지지 않은 종이. 실측 56×56 에서 종이 1706 칸 · 손 142 칸). 그런데 그건 「쥐고 있는 물건」이 아니라 **도판**이 된다 — 사용자 지시로 손을 다시 넣었다. 배경은 뷰어와 같은 계열의 어두운 판(투명하면 카드 그림자가 허공에 뜬다)
  - ⚠️ **사요의 나이가 두 곳에서 다르다** — 사진 뒷면과 이 문서(§P2 표)는 「사요 **열여섯**」, `character/config.ts` 주석은 「사요(**12세**)」. 사요 모델 키는 1.49 m(열두 살 기준)로 들어가 있다. 열여섯이면 1.58~1.60 m 라 ACT 1 구도와 사진 속 키 대비가 같이 움직인다 — 결정 필요
  - **ACT 2 의 손에 든 사진도 덮었다** — 텍스처를 다시 굽는 대신 얼굴 자리에 판 한 장(`makeFaceBleed`)을 띄운다. 아이콘과 **같은 상수**(`FACE`)를 쓰므로 둘이 어긋날 수 없고, ACT 30 은 이 메시(`face-bleed`)를 감추기만 하면 얼굴이 드러난다. 사진면의 깊이는 짐작하지 않고 **레이캐스트로 잰다** — 손가락이 사진 앞뒤로 감겨 있어 바운딩만 보면 종이가 아니라 손끝에 붙는다. 기하 검증(오프라인): 얼굴 지점의 레이가 만나는 면이 **정확히 2 개**(종이의 앞·뒤)라 손이 아니고, 그 앞면은 bbox 앞에서 5.9 % 안쪽 — 정규화 후 z = +0.0525 m
  - **아이콘 = 찍은 사진** (폴백) — 🖼️ 이모지는 「액자 그림」이지 「가방에 든 그 사진」이 아니다. `photoThumb()` 이 완성본 캔버스를 정사각으로 잘라 data URL 로 주고, `InventoryUI` 가 이미지 아이콘(`.inv-img`)을 렌더한다. 인벤은 **열 때마다 다시 그린다** — 아이콘이 런타임에 바뀌기 때문(촬영 완료 시, ACT 30 의 복원 시)
- v4.1 (2026-08-22) **P3 — 본전 문이 1 부의 계기판이 됐다** (`world/village/hondenDoor.ts` 신규). 5 단계(잠김 / 금줄 삭음 / 틈 2 cm / 흰 손 / 개방)를 지금 다 세워 두고 6~15 는 `setStage(n)` 값만 올린다. 「밀어본다」는 **꾹 누르기**다 — E 한 번에 「잠겨 있다」가 뜨면 정보고, 게이지가 끝까지 찼는데 안 열리는 건 경험이다. ⚠️ **본전이 접근 불가였다**: 콜라이더가 석단까지 한 덩어리라 다가갈 수 없었다 → 몸통만 막고 정면에 계단 4 단(오토스텝 0.35 m). `sfx.underfloorLaugh` — 저역만·0.4 초·**한 번만**(두 번이면 유령의 소리, 한 번이면 잘못 들은 것). 좌표는 배전 마루 밑이고 **그게 곧 ACT 18 의 지하 입구**다. 석판 조사에서 소리가 먼저, 0.7 초 뒤 자막
- v4.1 (2026-08-22) **사요가 화면에 섰다 — ACT 1 「앞서 달리는 언니」** (사용자 지시). 오전에 걷어냈던 언니를 새 모델(`girl+in+dress`)로 다시 세웠다. 규칙은 기획 그대로 **손·뒷모습만** — 몸은 늘 진행 방향, 재촉할 때 도는 것은 목뿐이고 그것도 51°까지(얼굴은 ACT 30 을 위해 아낀다).
  - ⚠️ **Tripo 프리셋 애니메이션은 정면 +X 를 가정한다.** 이 모델은 정면 +Z 로 생성돼 있었고, 그대로 리깅했더니 **몸은 12시를 보는데 다리는 9시–3시로 흔들렸다**(사용자 리포트: 「달리는 축이 10시인데 무빙은 12시」). 실측으로 확인: 발 궤적의 주축이 모델 **X**(진폭 0.84 m, Z 변위 6 cm) · 몸통이 기우는 방향도 **+X**. 클립 데이터는 정상 산출물(`character.glb`)과 **거의 같은 값**이었다 — 즉 클립이 아니라 **리그의 방향**이 문제였다. 한 번 「Hip 에 상시 23° 롤이 박혔다」로 오진해 롤을 깎는 코드를 넣었다가 되돌렸다(어깨 15 cm 처짐은 롤이 아니라 **앞으로 숙인 것을 옆에서 본 것**)
  - → **리깅 전에 메시를 +90° 돌려 굽는다**(`scripts/dev/rotate-glb.ts`) → 재리깅·재리타겟. 리깅 전 확인 도구 `scripts/dev/axis.ts`(bind 발가락 방향이 +X 인가). 크레딧 55 낭비를 포함해 총 110
  - **잡힌 손** — 달리기 클립 위에 **왼팔만** 2본 IK 로 덮어써 뒤(미오)로 뻗는다. 회전만 걸고, 목표가 팔 길이(0.41 m)를 넘으면 **목표를 사거리 안으로 자른다**(늘리지 않는다 = 메시가 안 뒤틀린다). 팔꿈치 방향은 **고정 폴**로 잡는다 — 처음엔 애니메이션의 굽힘 축을 따라갔는데 사이클마다 팔꿈치가 뒤집혔다. 쇄골 보정은 10° 로 자른다(22 % 로 뒀더니 왼쪽 어깨가 15 cm 내려앉았다)
  - **구도** — 1.3 → 0.98 → 0.42 → **0.29 m**(사용자가 `dev/sayo-run.html` 에서 직접 맞췄다). 오른쪽 0.17 m. 실측(달리는 중, 넘어짐·강제 뒤돌아보기 제외 79 프레임 중앙값): 몸 중심 화면 x **69 %** · 머리 y 34 % · 손 49/116 % · 손~카메라 0.24 m · 등~카메라 0.36 m(근평면 0.1). 미오의 팔은 1인칭이라 안 그리므로 손이 안 닿아도 성립한다
  - 튜닝대(`dev/sayo-run.html`)는 **게임 카메라와 같은 조건**이어야 값이 옮겨진다 — 흔들림·앞으로 숙임(2~3°)·화각 66~74·근평면 0.1. 이걸 안 맞췄을 때 튜닝대에서 잡은 손 위치가 게임에서 화면 밖(y 105 %)으로 나갔다
  - ⚠️ **감쇠는 목표가 달리면 지연이 된다** — 사요 위치를 λ 16 으로 감쇠했더니 5.58 m/s 에서 **0.35 m 뒤처져**(정상 상태 지연 = v/λ) 리드 0.43 이 화면에서는 0.26 으로 나왔다. 수평은 감쇠를 걷어내고 그 자리에 놓는다(길 표본은 스플라인이라 원래 매끄럽다). 높이만 λ 14 로 남긴다(지면이 계단처럼 뛴다). 같은 이유로 끌림 리드 보정도 0.3 → **0.05**: 조작을 안 하면 `tug` 가 계속 1 이라 변주가 아니라 상시 오프셋이 된다
  - ⚠️ **발소리가 절반만 나고 있었다** — `STRIDE_M` 2.05 는 한 걸음이 아니라 **보폭(두 발)** 이다. 사요의 발이 화면에 서면서 어긋남이 드러났다(사용자 리포트). 1.02 로 고치고 상한 6 → 7.5 걸음/초 — 실측 2.4 → **4.69 걸음/초**, 좌우 완전 교대. 표본도 **왼발 짝수·오른발 홀수**로 갈라 서로 다른 소리가 번갈아 나게 했다(무작위였을 때는 같은 표본이 연달아 나 한 발로 뛰는 것처럼 들렸다)
  - **재생속도 절반** (2026-08-22, 사용자 지시): `SPEED_MUL` 2.5 → **1.25**. 끌려갈 때 5.58 → **2.79 m/s**, 72 m 구간 무입력 통과 13 → **29 초**, 걸음 4.69 → **2.72 걸음/초**(실측). 「왜 이렇게 빨라졌나」의 원인은 배율이 아니라 ① 걸음 주기를 두 배로 고친 것(흔들림 주기가 같이 두 배가 된다)과 ② 사요를 0.29 m 앞으로 당긴 것(코앞의 시차 = 속도감)이었다 — 그 둘은 그대로 두고 배율만 내려 균형을 맞췄다. 넘어짐 길이는 배율 반비례 공식이 4.6 초를 뱉어서 **3 초 상한**을 걸었다(엎어져 있는 4.6 초는 연출이 아니라 정지 화면이다). 자막·추격자·시선·화각은 전부 이 배율에서 역산되므로 따라온다
  - **끝맺음**: 손이 빠지고 → 언니가 두어 걸음 앞서고(1.2 m/s) → **종소리와 같은 프레임에 사라진다** → 종이 울린 뒤 0.7 초 동안 **빈 길**만 남고 → 암전 위에서 「언니……?」. 처음엔 4.5 m/s 로 멀어지며 투명도로 지웠는데, 그건 사람이 없어지는 게 아니라 **화면 효과**로 보였다(사용자 지적: 「연출 너무 이상해」) — 0.3 m 앞에 있던 사람이 그 속도로 빠지면 사라지는 게 아니라 빨려 나가고, 페이드는 유령이 아니라 디졸브다. 소리가 가장 큰 순간에 한 프레임으로 걷어내면 「없어지는 것」을 못 보고 **없다는 것**만 남는다. 팔이 풀리는 속도도 λ 3.2 → 9(손가락이 풀리는 데는 시간이 안 걸린다). ACT 1 이 끝나면 GPU 메모리를 돌려준다
- v4.0 (2026-08-22) **P2 — 가족사진이 아이템이 됐다.** 튜토리얼 표가 ACT 2~3 의 학습 목표로 「사진 확인」을 잡아 뒀는데 인벤에는 `sword` 와 `apple` 뿐이었다. `ItemType` 에 **`record`(기록물)** 신설 — 장착이 아니라 **여는** 물건이고, 뒤에 명부·사요의 일기·저택 문서 3종이 전부 이 종류로 들어온다. `story/photoViewer.ts` 신규: 새로 그리지 않고 `makePhoto()` 의 **로케 촬영 캔버스를 그대로** 띄운다. ⚠️ `photo.ts` 가 **완성본만 캐시**하고 있어서 훼손도를 바꾸면 로케 사진이 사라지고 그려진 폴백이 나왔다 — **원판(`shot`)을 들고 있도록** 고쳤다. ACT 30 의 「얼룩이 걷히며 사요의 얼굴이 드러나는」 연출이 바로 그 재생성이라, `photoViewer.show(0)` 한 줄로 오늘 이미 동작한다(인게임 확인). 획득은 ACT 2b 의 「사진을 가방에 넣고」 자막과 **같은 순간**
- v3.9 (2026-08-22) **P1 — 미오는 빈손으로 내린다** (각색 6 **C안** 확정). 손전등은 만들지 않는다. **① 폰**(`story/phone.ts` 신규 · DOM 한 장, 씬 오브젝트 0): 버스에서 15:04 확인 → 「姉」 발신 → 圏外 → 미오 「……산속이니까」 / 마을에서 다시 보면 **같은 15:04 인데 해는 넘어가 있다**. 시계를 흐르게 만들지 않은 이유 — 플레이어는 1 분이 가는 걸 못 본다. **하늘이 증인**이면 된다. 화면의 **9月23日** 은 공고판의 「二〇一五年 九月 二十三日」과 같은 날이고, 폰을 본 사람에게만 공고판 대사 한 줄이 더 붙는다(`storyFlags.phone`). **② 초칭 획득**(`world/higasato/eaveChochin.ts` 신규): `Chochin.setHeld()` 추가 — 시스템을 끄지 않고 **가시성과 감지 배율만** 막는다(프리워밍이 끝난 뒤에 뗀다 — 먼저 숨기면 「불 켜진 초칭」 셰이더 변형이 안 구워져 하필 튜토리얼 순간에 히치가 난다). 등불은 `makeLantern()` 을 그대로 써서 **떼기 전과 든 뒤가 같은 물건**이고, 자리는 **상시 처마 등불이 걸린 집을 물려받는다** — 피했더니 참배로에서 13 m 옆으로 밀려났고(실측), 물려받으면 반드시 지나치는 자리에 걸리는 데다 **떼면 그 집이 정말로 어두워진다**. 획득 직후 **Q 튜토리얼이 처음** 뜬다
- v3.8 (2026-08-22) ⚠️ **클립이 미오의 골격을 덮어쓰고 있었다** — 「어깨가 움츠러들어 있다」(사용자 지적)를 파고들다 v3.1·v3.5·v3.7 이 전부 한 원인의 증상이었다는 걸 찾았다. 클립에는 42 본 **전부의 position·scale 트랙**이 들어 있어서, 재생하는 순간 미오의 본 오프셋이 **원본 리그(`character.glb`)의 비율로 교체**된다. 실측(미오 rest 대비): `L_Upperarm` **0.635** · `R_Upperarm` 0.696 · `Head` 0.727 · `L_Forearm` 0.898 / `L_Clavicle` 1.094 · `NeckTwist01` 1.079 · `Spine02` 1.079 — **어깨 관절이 목 쪽으로 36 % 당겨져** 있었다. → `model.ts addClip` 에서 **회전 트랙만 남긴다**(예외: `Root`·`Hip` 이동 = 상하 바운스·루트 모션. Hip 을 원점에 두는 Tripo 관례는 `calibrateOffset()` 이 이미 흡수한다). 트랙 126 → 44.
  - 어깨 폭(Hip→Spine02 로 정규화) **0.797 → 1.10** (미오 rest 1.17). 팔·목·머리 뼈 길이가 rest 와 소수점까지 일치
  - 여파 1 — `torsoRoll` **−0.117 → −0.133**: 팔뼈가 길어지니 같은 쇄골 각도가 만드는 높이차가 커졌다(보정 0 에서 −27.7 mm · 209 mm/rad)
  - 여파 2 — **발소리가 깨졌다**: 다리가 제 길이를 찾아 발목이 통째로 올라왔다(walk 최소 0.083 → 0.115 · run 0.17 → 0.202). 옛 문턱은 여유가 5·10 mm 뿐이라 왼발이 두 번씩 울렸다(6 초에 12/7). 문턱을 「한 걸음에 정확히 한 번」 구간으로 재산정 — `footContactWalk` 0.12 → **0.13**, `footContactRun` 0.24 → **0.25**. 검증 **walk 6/6 · run 10/10**
  - `character.glb`(구 캐릭터, 샌드박스)는 자기 리그에서 만든 클립이라 트랙 값이 곧 rest — 버려도 결과가 같다
- v3.7 (2026-08-21) **고개 좌우 기울기 + 초칭 손 교체** (사용자 지적 2건). **① 고개가 상시 오른쪽으로 기울어 있었다** — 클립별 실측(위상 맞춘 5 초 평균) idle −6.5 / walk −5.4 / run −7.1 / look_around −2.6 / standing_relax −2.8°. 다섯이 전부 같은 방향이면 편향이다 → `settings.character.headRoll` **−0.085** (≈57°/rad 로 역산). 축은 `torsoRoll` 과 같은 이유로 **컨트롤러 yaw 의 전방**이고 `neckShare` 로 목·머리에 나눠 건다. 검증: idle −1.6 / walk −0.5 / run −2.1 / look_around +2.2 / standing_relax +2.1 (최대 잔차 2.2°). **② 초칭을 왼손 → 오른손**(`light/chochin.ts`): 통학가방이 오른어깨→**왼허리** 로 걸려 있어 등불과 같은 자리에서 겹쳤다. `follow()` 의 「바깥쪽」이 골반→손 방향에서 계산되므로 손 본만 바꾸면 `gripPos` 는 그대로 대칭이 맞는다
- v3.6 (2026-08-21) **고개 보정을 목 전체에 나눠 걸기** — v3.5 후속. `model.ts` 의 목 본 정규식이 `NeckTwist01` 만 잡아서 목 몫 17° 를 **한 관절이 다 받고** 있었다(`NeckTwist02` 는 딸려 갈 뿐). `neckBone` 단수 → **`neckBones` 배열**(`NeckTwist\d+`), 척추와 같은 방식으로 관절 수만큼 나눈다. 결과(같은 애니 위상에서 비교, 머리 각은 −0.8° / −1.1° 로 동일):
  - 목이 **곧은 막대에서 곡선으로** — 한 마디일 때 −7.7° / −7.7° / 머리 −0.8° 였던 것이 **−13.1° / −8.0° / −1.1°** 가 됐다. 아래 목이 앞으로 기울고 위 목이 펴서 머리를 세우는 것이 실제 목의 자세다
  - 어깨(Spine02) 대비 머리의 앞뒤 위치 **−11 → −15 mm**(뒤로 4 mm) — 거북목이 그만큼 준다
  - 달리기에서도 −20.5 / −12.6 / 머리 **−2.0°** 로 같은 곡선. 총 보정량은 그대로라 `neckShare` 재조정 불필요
- v3.5 (2026-08-21) **고개 숙임 보정 재산정** — 「미오의 고개가 구부정하다」(사용자 지적). 보정을 0 으로 두고 머리 본의 월드 피치를 재니 클립 원본이 **idle −18.0° / walk −19.1° / fall −40.6° / run −57.9°** 였다. **idle 과 walk 가 1° 안에서 일치한다** — 따로 만든 두 클립이 같은 값을 내면 연기가 아니라 리그 오차다(v3.1 어깨 처짐과 같은 종류: 클립은 `character.glb`, 게임은 `mio.glb`). 옛 값(0.16)은 그 −18° 를 **절반만** 폈다 → 실측 머리 −9.2°. `headPitchIdle` 0.16 → **0.30** · walk 0.22 → **0.32** · run 0.5 → **0.60** · air 0.12 → **0.28**. 그리고 **`neckShare` 0.4 → 0.6**: 숙임의 원인이 머리 관절이 아니라 **목뿌리**여서(측정 내내 Head 와 NeckTwist01 의 각이 같다 = 목·머리가 통째로 기운다) 0.4 에서는 턱만 들리고 목은 앞으로 뽑힌 채라 「거북목」으로 읽혔다(머리 −9.2° / 목 −14.7°). 검증(재로드 후 실측): **idle 머리 −1.5° / 목 −8.4°** · walk −2.2° · run ≈ 0° · look_around −4.4° · standing_relax 정상. 측정법은 바인드 자세의 「얼굴 축」을 **발목→발가락** 방향에서 뽑아 현재 쿼터니언에 태우는 것 — 본 로컬 축은 리그마다 제멋대로라 눈으로도 오일러로도 판정이 안 된다. 바인드 자세 자체는 머리·척추가 일치(0.0°)라 rest 는 죄가 없다
- v3.4 (2026-08-21) **스태미나 3배** — `stamina.max` 6 → **18 초**(사용자 지시). `recover` 는 *시간*이라 회복 속도(`max/recover`)가 같이 올라가 빈 게이지가 차는 데는 그대로 8 초(정지 5.3 초). 실측: 소진까지 **17.95 초**, 게이지 선형 감소. 소진 해제선은 30 %(=5.4 초분)로 자동 스케일. ⚠️ 추격 밸런스가 완화된다 — 파수꾼 3.2~3.9 m/s 대 플레이어 3.6 m/s 에서 **뿌리칠 수 있는 시간이 3배**다
- v3.3 (2026-08-21) **카메라 어깨 오프셋 0** — 미오가 화면 왼쪽으로 치우쳐 있었다(사용자 지적). `camera.shoulderOffset` 0.5 → **0**. 2.6 m 거리에서 화면 반폭의 약 19 % 만큼 밀려 있던 것. 검증: 캐릭터 NDC x −0.19 → **0.0000**
- v3.2 (2026-08-21) **공물 7종을 실물로** — 나무 받침 + 발광 20면체(색만 다름)에서 Tripo 신규 6종으로. `OfferingDef.model/size` · `Rules.prototype()` 단일 로드 캐시 · 획득 자리와 봉납 받침대가 같은 원본을 복제. 실물은 **떠서 돌지 않고 받침대에 놓인다**(공물은 전리품이 아니다) — 찾게 만드는 건 표식 불빛의 맥동. GLB 없으면 구슬 자리표시자 유지(폴백 확인). `size` 는 **가장 긴 변** — `normalize()` 의 높이 기준을 그대로 쓰면 게다가 30 cm 가 된다(실측 → 18 cm 로 교정)
- v3.1 (2026-08-21) **어깨선 상시 기울기 보정** — 애니 클립은 `character.glb` 리그에서 만들었는데 게임은 `mio.glb` 를 쓴다. 두 리그의 rest 가 Hip 4~7° · 쇄골/상완 10~16° 달라 **모든 클립에 왼쪽 어깨 처짐**이 남아 있었다(실측 편향 idle −20.4 / walk −11.4 / run −13.2 / standing_relax −17.9 / look_around −19.0 mm). **척추가 아니라 쇄골**에 −0.117 rad 를 건다(`settings.character.torsoRoll`) — 척추에 걸면 머리까지 옆으로 끌려간다(실측 −27 mm). 결과: 편향 −16.4 → **+0.2 mm**(잔차 ±5 mm = 클립 고유 비대칭), 머리 가로 위치는 불변. 회전축은 노드 쿼터니언이 아니라 **컨트롤러 yaw** 에서 구한다 — 리그 rest 가 돌아가 있어 노드로 잡으면 옆구리·수직 축이 나온다
- v3.0 (2026-08-21) **손목 롤 180° 보정** — 캐릭터 리그의 손이 팔뚝 축으로 뒤집혀 있었다(손바닥이 뒤를 본다). 믹서 뒤에 손 본 로컬 **+Y**(= 팔뚝→손 방향, 실측 0.97)로 π 를 곱해 바로잡는다(`settings.character.handRoll`). 판정은 눈이 아니라 **위에서 본 엄지 방향**으로 했다 — 늘어뜨린 팔의 엄지는 앞을 향해야 하는데 뒤·바깥을 향하고 있었다. 무기 마운트는 손 본의 자식이라 같이 돌므로 `equipment.ts` 에서 되돌려 기존 그립을 유지(샌드박스 전용)
- v2.9 (2026-08-21) **ACT 2a 버스 — 창밖을 실제 3D 로** (`world/busOutside.ts` 신규): 캔버스 실루엣 UV 스크롤 → **132 m 컨베이어**에 노면·중앙선·가드레일·전신주·삼나무 두 줄·**논**·커브반사경·농막·능선(굴곡 메시). 앞유리 너머의 정지 그림도 제거. 실내에 **요금함·정리권 발권기·운임 표시기·대시보드**(Tripo 신규 4종) + **창유리** + 백미러 재작성. 잡은 것 4: 능선이 도시 스카이라인이었다 · 논 수면이 들판에 묻혔다 · 공유 재질 dispose 로 나무가 깨졌다 · **`--simplify` 없이 빌드해 프랍이 8 MB/140만 tris 로 나왔다**(→ 8.5 k tris)
- v2.8 (2026-08-21) **ACT 1 배율 ×2.5 로 정착** (9.0 m/s · 32.4 km/h). ×1.8(4.68)은 60 km/h 라 사람 범위를 벗어났고, 애초에 「느리다」의 원인이 배율이 아니라 **끌기와 보폭**이었다. 전속력 4.29 걸음/초·보폭 2.10 m / 끌려갈 때 2.72 걸음/초·보폭 2.05 m — 둘 다 사람의 달리기. 긴 자막 표시 시간도 0.95 → **2.30 초**로 복구
- v2.7 (2026-08-21) **ACT 1 의 끌기가 걷기였다** — 사요가 끌고 가는 속도 0.9 → **최고 속도의 62 %**(10.45 m/s). 무입력 통과 시간 약 80 → **8.5 초**. 끌기를 「밀어 주기」에서 **「모자란 만큼 채우기」**로 재작성. **1프레임 지연 피드백 진동 수정**(실제 이동으로 부족분을 재면 10 / 0.34 m/s 로 튄다 → 의도 속도 기준으로). 부수 효과: 끌려가는 동안 `horizontalSpeed` 가 0.3 으로 읽혀 **흔들림·발소리·화각이 꺼져 있던 것**이 같이 해결 — 5.0 걸음/초 · 화각 70.8°
- v2.6 (2026-08-21) **「뛰는데 걷는 것 같다」의 실제 원인 수정** — 흔들림 주기가 상수(3.66 걸음/초)라 배속을 올릴수록 **보폭이 4.6 m** 가 되고 있었다. **보폭(2.05 m)으로 잠그고** 걸음 수를 속도에서 낸다(상한 6/초) · 상하 파형 비대칭(내려꽂힘 ×1.35) · **착지 충격 + 딛는 발 쪽 롤** · **속도 화각 66 → 74°** · 속도 숙임. 실측 0.273 → 0.167 초/걸음, 보폭 2.81 m. **`evening` 프리셋 신설** — 버스 하차 후 밤까지 가지 않고 **늦은 저녁**에서 멈춘다(사용자 지시). 현재 히가사토의 기본 시간대
- v2.5 (2026-08-21) **ACT 1 재생속도 ×1.8** (`SPEED_MUL` 2.6 → 4.68 · 16.85 m/s · 달리기 구간 9.9 → 5.1 초). 배속 구간을 **달리기 시작 ~ 「모든 소리가 끊긴다」** 로 좁혔다(암전 위 첫 줄·정적 뒤 금기 세 줄은 원래 속도). 시간으로 적혀 있던 둘을 속도에 물림: **뒤돌아보기를 거리로 고정**(안 그러면 「보지 마!」가 22 m 밀려 마지막 줄을 잘라먹었다) · 넘어짐 2.2 → 1.23 초. 비트 거리 불변 확인. **남은 문제: 긴 줄 둘의 읽을 시간 0.95 초**
- v3.1 (2026-08-21) **봉납 지점: 신사 → 마을 정 가운데 제단**. 공물 7개가 갈래길 5개의 **끝**에 흩어져 있는데 봉납은 마을 북쪽 끝(신사)이라, 하나 주울 때마다 전 구간을 왕복해야 했다 — 채집이 아니라 심부름이 된다. `SITES.altar`(7, 24) 신규: 참배로 **동편 7 m**(길 위에 두면 받침대 콜라이더가 통행을 끊는다), 부지로 잡아 민가·나무가 자동 회피. `Pedestals` 가 `shrine.altar` 상대 배치 → 중심 좌표 직접 수령으로. **신사·금줄 게이트(출구)는 그대로** — 지도의 양 끝은 이야기가 쓰는 자리다. 퀘스트·방송 문구 4곳 「신사로」 → 「제단으로」, 이정표에 제단 2개 추가
- v3.0 (2026-08-21) **길 안내 + 공물 상호작용 복구** (사용자 리포트 2건). ⚠️ **`rules.update` 가 요괴 블록 안에 있었다** — `hunters.length && yokaiActive` 가 거짓이면(기본값: 요괴 꺼짐) 아예 안 돌아서, 붉은 구에 다가가도 프롬프트가 안 뜨고 E 가 먹지 않았다. 공물 규칙을 요괴와 무관하게 매 프레임 돌리도록 분리(요괴 난이도 입력만 안쪽에 남김). 그리고 `world/higasato/signposts.ts` — 갈래길 입구 4곳 + 묘지 갈림 1곳에 **나무 도표**(판자의 +X 끝이 실제 경로를 가리키게 yaw 계산, 밤에 겨우 읽힐 자발광 0.22). 「오래된 사당」이 뒷산길 끝이라는 걸 미니맵이 아니라 팻말로 알려 준다
- v2.9 (2026-08-21) **버스 실내 마감** (사용자 리포트: 격벽이 기사 의자를 가름 · 벽·바닥이 민짜). 격벽을 기사 유닛(z 2.05~3.25) **뒤**(z 1.85)로 + 위에 크롬 난간 · 바닥 = 고무 리브 + 통로 닳음 텍스처(박스 하나라 UV 0..1 정합) · 벽 = 이음매·리벳 패널 텍스처(**조각 길이에 맞춰 repeat** — 이음매 간격 0.62 m 균일) + 창밑 크롬 몰딩 + 킥플레이트 + 창틀 세로 프레임 · 천장 형광등 두 줄(발광 재질만, 대낮이라 라이트 없음)
- v2.8 (2026-08-21) **사진 = 로케 촬영 + 기사 재생성** (사용자 리포트 4건). 사진: 그림 배경 합성 → **실제 씬에서 촬영**으로 — 시간대를 '낮'으로 잠깐 돌리고 참배로 도리이 앞에 본체+복제(0.62배)를 `standing_relax` 로 세워 메인 렌더러 RT 한 컷(2배 해상도 다운스케일 AA, RT 에는 포스트FX 노출이 안 걸려 밝기는 세피아 필터에서 보정). 기사: 실사 비율로 재생성(8,593 tris) + 정면 90° 보정(`DRIVER_YAW`). **부수 발견 — 진짜 버그**: `HigasatoGround.roadAt` 이 out 파라미터를 안 받아 `ToriiPath` 의 `roadAt(s, rp)` 가 무시됨 → **센본토리이 30개가 전부 원점(0,0)에 겹쳐 서 있었다**(콜라이더 포함). 시그니처를 구 `VillageGround` 와 맞춰 수정 — 참배로 터널이 처음으로 제자리에 섰다
- v2.7 (2026-08-21) **가족사진에 실제 캐릭터** (`preparePhoto`) — 로드된 미오 GLB 를 오프스크린 씬으로 빌려 idle 포즈로 **두 번 렌더**(사요 = 현재 모델 그대로 244 px · 어린 미오 = 0.62 배 158 px), 발·정수리를 카메라 투영해 지면선·목표 크기에 정확히 앉히고 세피아 필터로 캔버스 배경과 한 장으로 만든다. 물얼룩은 **투영된 사요 머리 위치**에. 사요 = 미오 모델인 것은 의도다(자매가 닮았다는 암시, ACT 30 에서 얼룩이 걷히면 그 얼굴). 렌더 실패 시 그린 인물 유지
- v2.6 (2026-08-21) **버스 소품 Tripo 교체** — 좌석·손잡이·핸들·**기사**(Tripo 가 시트+핸들까지 한 유닛으로 생성 → 유닛째 배치). `fitFootprint`(생성 벤치가 2~3인용으로 나와 자리 폭 보정) · 손잡이 관성 흔들림(피벗 그룹) · 로드 실패 시 프리미티브 유지. 사용자 리포트 반영: **좌석 방향 π 반전 · 우측 핸들(일본 버스)로 운전석 이전 · 왼쪽 벽 4번째 창 자리를 하차문 개구부로 + 격벽은 운전석 쪽 절반만**. 크레딧 80(잔액 255)
- v2.4 (2026-08-21) **ACT 3·4 제작** — 「세 가지 금기」를 *모르고 어기는* 장면으로 설계(선택지를 띄우지 않고 **돌아보는 것**만 남긴다 · 돌아보면 한 번의 부름에, 버티면 두 번째 부름 뒤에 대답한다) · **비석 닦기 = 꾹 누르기**(`inspect.hold`, 이끼가 **위에서부터** 벗겨져 세 번째 금기가 마지막에 드러난다) · `world/higasato/lifesigns.ts` 생활 흔적 5종(김·TV·젖은 게다·풍경·골목 끝의 사람) · `world/higasato/speaker.ts` 방송탑 + **공고판(10년 전 날짜)** · `story/act4.ts` 마을 방송 · 위치 있는 소리 8종(`callName` 모음 포먼트·산울림 / 확성기 대역제한 / `bell(far)`). 잡은 버그: 비석 각인면이 **두께 26 cm 모서리에 붙어 튀어나와 있었다** · 프롤로그 중에 조사·생활 흔적이 돌았다 · `setTimeout` 이 시뮬 시계와 어긋났다 · 나팔 축 부호
- v2.3 (2026-08-21) ACT 1 **심장 소리 제거**(저역이 숨·비·발소리를 먹는다) · 재생속도 ×2.0 → **×2.6**(9.36 m/s) · **발소리를 카메라 흔들림 위상에 물렸다** — 애니 클립 배속이 1.8 배에서 잘려 어긋나 있었다. 간격 편차 0.010 초, 좌우 완전 교대
- v2.2 (2026-08-21) ACT 1 달리기 ×1.7 → **×2.0**(7.2 m/s) · **자막 진행 속도를 달리기 배율에 묶었다**(`Dialogue.setRate`) — 한 줄이 먹는 거리가 고정되어 앞으로 속도를 올려도 비트 표를 다시 짤 필요가 없다. 남은 한계는 가독성(가장 긴 줄의 읽을 시간 1초)
- v2.1 (2026-08-21) ACT 1 달리기 ×1.5 → **×1.7**(6.12 m/s) · 늘어난 속도만큼 비트 간격 재조정(자막 잘림 0, 도착 대기 0.2 초)
- v2.0 (2026-08-21) **ACT 2 제작** — 스토리보드가 통째로 빠져 있던 **버스 내부**를 만들었다: `story/photo.ts`(캔버스 가족사진, **사요의 얼굴만 지우고 머리카락만 다시 그린다**) · `world/bus.ts`(지형 밖 자립 세트, 버스는 서 있고 **창밖이 흐른다**, 기사는 **백미러 속 눈 두 개**로만 존재) · `story/act2.ts`(1인칭, 고개를 들면 눈과 마주친다) · `FirstPerson.configure/anchor` · `sfx.voice/busEngine/busBrake/busDoor`. 잡은 버그: 뒷벽을 보고 앉음 · 「고개를 든다」가 시선을 안 옮김 · 오른쪽 yaw 부호 반대 · `hemiGround` 가 실내 천장을 초록으로
- v1.10 (2026-08-21) **ACT 1 조도 2차** — `rainNight` 을 한 단계 더 올렸다(달빛 1.05 → 1.9 · 노출 0.74 → 0.90 · 안개 0.024 → 0.017, 안개색도 들어 올림). 「시간대를 밝게」가 ACT 2 가 아니라 **처음 뛰는 컷** 얘기였다
- v1.9 (2026-08-21) **템포 2차**: 일어나는 동작 0.8 → 1.2 초(넘어짐은 0.29 초 그대로 — 단계 비율을 상수로) · 달리기 ×1.5(5.4 m/s)·구간 72 m · **흔들림 주기에 속도 배율을 한 번 더 곱하던 것 제거**(체감이 곱으로 뛰었다) · 출발점이 금줄 게이트(의도된 벽)에 막히던 것 수정 · **`afternoon` 프리셋 신규 — 오후 3시 하차 → 컷신 중 두 단계로 해가 무너진다**(afternoon → dusk → night)
- v1.8 (2026-08-21) **프롤로그 템포**: 달리기 ×1.2(4.32 m/s, 추격·숨·리듬이 전부 역산) · 구간 62 → 70 m · 도입 5.4 → 2.2 초(암전·페이드를 겹침) · 끝맺음 17.5 → 12 초 · 겹쳐서 잘리던 「보지 마!」/「일어나!」 분리 · 두 이미지 문장을 스토리보드 원문대로 한 줄로 · **ACT 2 를 해질녘 도착 → 컷신 중 밤으로**(길이 사라지는 줄에서 해도 사라진다, 초칭 점등 순간 확보) · ACT 2 마지막 두 컷 재프레이밍(게이트·표지가 화면을 막고 있었다)
- v1.7 (2026-08-20) **ACT 1 2차 — 스토리보드의 두 그림을 화면에 올렸다**: 피안화 길 복원(터널 제외 범위가 구 마을 좌표였다) · `rainNight` 대비가 6.9 % 이하를 순검정으로 뭉개던 것 수정 · **번개**(`world/lightning.ts`, 광원 추가 없이 달빛·반구광 스파이크 + 천둥 시차) · 숨·흐느낌·심장 · 손의 **끌림/넘어짐/놓기** · 정적→손 빠짐→종 재구성. 잡은 버그: 시선 기준 이동으로 길에서 8 m 이탈 · `nearestRoad` 재사용 버퍼를 `heightAt` 이 덮어써 플레이어를 뒤로 끌던 것 · 끝점이 도리이 코앞 · 암전 중 추격 진행
- v1.6 (2026-08-20) **시간대 시스템**(rainNight/night/dusk/dawn/day/bloodMoon, 보간 전환) · **요괴 전면 비활성**(생성 자체를 건너뜀 — Scares 가 로드 시점에 놋페라보를 세워 두던 것이 원인) · ACT 1 손 잘림 3원인 수정
- v1.5 (2026-08-20) **ACT 1 을 플레이 가능한 1인칭 도주로 재작성** — 1인칭 리그(눈높이 1.05 m·잡힌 손·시선 저항) · 보이지 않는 추격자(횃불·거리 압박) · 거리 기반 비트 · 합성 외침 · 잠식 오버레이. 방침: **ACT 를 하나씩 제대로**
- v1.4 (2026-08-20) **마을 골목 6줄 + 민가 모델링**(`minka.ts`, 폐가 PBR 재질 재사용·외관 전용·드로우콜 7) · **나브그리드 버그 수정**(건물 콜라이더가 브로드페이즈에 없어 요괴가 집을 통과하던 문제)
- v1.3 (2026-08-20) **스토리 맵을 새 패키지로 재설계**(`world/higasato`, 기존 맵 무손상) — 사이트 우선 지형·길에서 온 선반 높이·전 구역 도달 검증 · **프롤로그 ACT 1~2**(1인칭 빗속 질주 → 타이틀 → 종점) · 비/범종/타이틀 카드
- v1.2 (2026-08-20) **S1 1차 — 스토리 맵 개편**: STORY_PADS 5곳·비석·사당·받침대 7·블록아웃 3동·우물·버스 종점, 7공물 규칙 재작성(게이팅·운반 상태·봉인패 거부), 비석→봉납 1 스토리 플로우 인게임 검증
- v1.1 (2026-08-20) 각색 13건 잠정 채택("일단 진행") · **S0 1차 구현**(story/ 4모듈 + 데모 + main 통합) · §9.4 에 실내 다층 내비 리스크 추가
- v1.0 (2026-08-20) 최초 작성 — 스토리보드 fin 통합 설계. 각색 13건 컨펌 대기
