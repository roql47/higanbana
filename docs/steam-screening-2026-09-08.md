# Steam 출시를 위한 추가 개발 스크리닝 — 2026-09-08

> 후속 업데이트: Windows/Mac 실행 파일과 오프라인 폰트 패키징은 [2026-09-09 패키징 기록](steam-packaging-2026-09-09.md)을 참고한다. 아래 내용은 당시 스크리닝 시점의 상태이며, 현재 스토리 구현 범위는 ACT 18까지다.

> 아래는 당시의 스크리닝 기록이다. 후속 작업으로 ACT 18까지 플레이를 연결했으며 최신 범위·84개 테스트·실게임 확인은 [재점검 및 ACT 18](act18-review-2026-09-08.md)을 따른다.

스크리닝 후 사용자의 진행 요청에 따라 **저장 보호·메뉴 입력·ACT 17 복구·저장 실패 안내와 CI 연결을 구현했다.** 렌더링 최적화 내역은 [이전 코드 리뷰](/Users/jay/Claude/3D_motion/docs/review-2026-09-08.md)에 정리되어 있다. 이번 변경의 GPU/RAM 절감률을 측정하거나 추정하지 않았다.

후속 진행으로 **오디오 구간 로딩·가구 스트리밍·공간 검색·AI 갱신 분산**도 적용했다. 상세 범위와 52개 테스트 결과는 [추가 리소스 최적화](/Users/jay/Claude/3D_motion/docs/resource-optimization-2026-09-08.md)에 기록했다. 아래 39개 테스트 내역은 저장·메뉴 안정성 작업 당시의 검증 기록이다.

## 이번에 해결한 결함

| 항목 | 발견한 문제 | 적용한 수정 |
| --- | --- | --- |
| R 키 저장 삭제 | 직전 리셋 수정과 기존 R 단축키가 결합해 일반 플레이에서도 체크포인트를 지울 수 있었다. | 일반 마을 플레이의 R 리셋을 제거했다. 개발 sandbox에서만 유지한다. 타이틀·일시정지·종료 화면의 새 게임은 삭제 대상을 명시한 확인창을 거친다. 취소가 기본 포커스이며 Esc도 취소한다. 삭제 실패 시 새 게임을 중단하고 안내한다. |
| 메뉴 입력 누출 | 실제 Dialogue + PauseMenu 재현에서 해상도 슬라이더의 방향키 → Enter가 뒤의 대화 선택지까지 확정했다. | ModalInput으로 가장 위 창에 키 입력과 포커스를 한정했다. 타이틀 설정·크레딧, 일시정지, 인벤토리, 기록, 사진, 새 게임 확인, 종료 화면에 연결했다. 게임 전역 핸들러와 대화 선택·스킵도 열린 UI를 확인한다. 창 전환 시 남은 게임 입력을 초기화한다. |
| ACT 17 이어하기 정지 | 봉인패 거절 플래그만 복원되어 단발 이벤트가 재실행되지 않고 종료 화면도 나타나지 않았다. | 연출을 unseen/pending/complete 상태로 저장한다. 미완료 및 구버전 거절 저장은 연출을 재개하고, 완료 저장은 종료 화면으로 바로 복귀한다. 완료 상태를 저장한 뒤 종료 화면을 표시하며 중복 실행을 막는다. |
| 저장 실패 은폐 | 저장 실패 반환값이 UI로 이어지지 않고 최신 저장 하나만 보관했다. | 이전 정상 저장을 백업으로 보존하고 최신 저장을 읽지 못하면 백업을 복원한다. 저장 실패·마지막 저장 시각·재시도 버튼, 백업 복구 안내를 추가했다. 손상된 최신 데이터로 정상 백업을 덮지 않는다. |
| 로딩 중 저장 덮어쓰기 — 실게임 검증 중 추가 발견 | `?skip=intro`의 ACT 4 초기화가 타이틀에서 기본 ACT 2 상태를 저장했다. ACT 17 테스트 저장이 이어하기 전에 ACT 2로 바뀌는 것을 실제 게임에서 재현했다. 복원 중 콜백도 부분 상태를 저장할 수 있었다. | 게임 시작 전과 체크포인트 복원 중에는 저장을 허용하지 않는다. 복원이 끝나고 started=true가 된 뒤에만 체크포인트를 기록한다. |
| 자동 검사 누락 | 배포 작업은 Node 22에서 빌드만 실행했고 새 테스트·scripts 타입 검사를 실행하지 않았다. | .nvmrc를 Node 24.12.0으로 맞추고 PR 검사와 기존 배포 빌드에 test → typecheck → build를 연결했다. 원격 CI 실행·배포는 수행하지 않았다. |

주요 구현:

- [새 게임 확인과 리셋](/Users/jay/Claude/3D_motion/src/main.ts:3082), [저장 시점 보호](/Users/jay/Claude/3D_motion/src/main.ts:1174)
- [UI 입력 소유권](/Users/jay/Claude/3D_motion/src/ui/modalInput.ts), [확인창](/Users/jay/Claude/3D_motion/src/ui/confirmNewGame.ts)
- [ACT 17 상태와 연출](/Users/jay/Claude/3D_motion/src/story/act17.ts), [이어하기 연결](/Users/jay/Claude/3D_motion/src/main.ts:3775)
- [체크포인트와 백업](/Users/jay/Claude/3D_motion/src/story/flags.ts), [저장 상태 UI](/Users/jay/Claude/3D_motion/src/ui/saveStatus.ts)
- [PR 검사](/Users/jay/Claude/3D_motion/.github/workflows/ci.yml), [배포 전 검사](/Users/jay/Claude/3D_motion/.github/workflows/deploy.yml)

## 확인한 결과

- `npm test`: **39개 통과**. 기존 최적화 회귀 테스트에 ACT 17 재개·중복 방지·완료 복원, 게임 시간 대기, UI 포커스/입력 소유권, 저장 손상·쓰기 실패·백업·삭제 테스트를 추가했다.
- `npm run typecheck`: 게임 코드와 scripts 타입 검사 통과.
- `BASE_PATH=/higanbana/ npm run build`: 통과. main 청크가 약 4.86 MB라 기존 큰 청크 경고는 남아 있다. 이것은 RAM 실측값이 아니다.
- `git diff --check`: 통과.
- 실제 Dialogue + PauseMenu 컴포넌트: 메뉴에서 해상도만 바뀌고 대화는 선택 대기 상태를 유지한다. 메뉴를 닫은 뒤 숫자키를 눌러야 선택된다.
- 실제 게임, 별도 `127.0.0.1:5187` 테스트 저장: ACT 17 미완료 저장 → 연출 → 종료 화면, 타이틀 복귀 → 완료 저장 즉시 종료 화면을 확인했다.
- 같은 격리 주소에서 최신 저장 JSON을 손상시킨 뒤 이어하기: 백업 복구 안내와 완료 종료 화면을 확인했다.
- 타이틀 설정의 Shift+Tab 포커스 순환, 새 게임 취소 후 원래 버튼으로 복귀, 일시정지 위 확인창 취소 후 정지 상태 유지, 명시적 새 게임 확정 후 이어하기 비활성화를 확인했다.
- 새 게임의 일반 마을 플레이에서 R이 리셋하지 않으며, 정지 메뉴의 R/E도 게임 동작을 실행하지 않았다. 확인한 게임 세션에서 브라우저 경고·오류 로그는 없었다.
- 저장 실패 UI는 실제 저장소에 손대지 않는 컴포넌트 페이지에서 지속 표시와 재시도 성공 동작을 확인했다. 저장소 쓰기 예외·용량 실패 자체는 단위 테스트에서 주입했다.

로컬 브라우저 재현 페이지는 빌드에 포함하지 않는 `artifacts/screening/`에 있다. `checkpoint-fixture.html`의 저장 생성·손상 버튼은 **5187 포트에서만 활성화**된다. 다른 개발 서버의 저장을 테스트 대상으로 삼지 않기 위한 제한이다.

## 남은 개발 항목

| 우선순위 | 현재 상태 | 다음 완료 기준 |
| --- | --- | --- |
| P2 · 연출·오디오 일시정지 | 새 GameClock은 ACT 17의 대사 사이 대기에만 적용했다. 기존 프롤로그의 타이머, 예약 효과음, 오디오 재생 시계는 전체적으로 통합하지 않았다. | 취소 가능한 게임 시간 대기와 연출 종료 처리를 도입하고 오디오 정지/복귀를 연결한다. 컷신 중 Esc·Alt-Tab 후에도 카메라·대사·효과음 순서가 유지되어야 한다. |
| P2 · 긴 배경음의 상주 RAM | 구간별 선로딩·해제, 동시 디코딩 제한, 재생 중인 버퍼 보호는 후속 작업에서 구현했다. 현재 구간의 긴 앰비언스는 여전히 전체 PCM으로 보관한다. | 스트리밍 재생으로 전환할 긴 음원을 선정하고 루프·페이드·예약 시점·일시정지 동작을 유지한다. 64MiB 캐시 목표는 보호된 현재 구간·활성 보이스를 포함한 강제 상한이 아니다. |
| P2 · 오프라인 Windows 패키징 | 웹 빌드만 있고 Windows 실행 파일 빌드 설정이 없다. Google Fonts 원격 로드와 웹 저장소를 사용한다. | 패키징 방식·리소스 URL·저장 경로 계약을 정하고 폰트를 로컬 포함한다. 네트워크 차단, 창 전환, 실제 GPU, 장시간 플레이를 패키지에서 검증한다. Steam API·업적·Cloud는 이후 별도 기능이다. |
| P2 · 설정·게임패드 | 인게임 음량은 재실행 시 기본값으로 돌아간다. 키보드·터치 입력은 있지만 게임패드 경로는 없다. | 음량·감도·축 반전·흔들림 설정 저장을 정리한다. 게임패드 지원 시 행동 단위 바인딩, 메뉴 조작, 버튼 안내까지 구현한다. |
| P2 · 스토리 복원 회귀 확대 | 이번 복구 구현·브라우저 검증은 ACT 17을 대상으로 했다. | 나머지 ACT의 대표 시작/중간/완료 체크포인트와 추격 실패 후 복귀를 검증한다. 39개 단위 테스트 통과가 전체 스토리 완주를 의미하지는 않는다. |
| 출시 범위 · 후반 본편 | 현재 플레이 구간은 ACT 17 데모 종료 화면까지다. 후속 [서사 점검](story-density-2026-09-08.md)에서 ACT 16~17의 부분 구현과 ACT 18~35의 설계 상태를 메타데이터에 반영했다. | 완성판의 ACT 18~35 진상·복원·선택·엔딩은 별도 큰 개발 범위다. |

다음 개발 순서는 **연출/오디오 일시정지 → 설정 저장 → Windows 오프라인 패키징**을 권장한다. 추가 리소스 최적화 후보는 별도 문서에 정리했다. 이는 현재 코드의 상태를 기준으로 한 개발 순서이며 Steam의 필수 정책 목록이 아니다. Windows 실행 파일과 Steam 클라이언트에서의 동작은 아직 검증하지 않았다.
